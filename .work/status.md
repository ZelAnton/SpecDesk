# Оркестр — обзор
- Обновлено: 2026-07-17T09:58:00Z
- Оркестратор: processor — этап: батч архивирован, переход к следующей когорте
- Provider: claude
- Codex: отключён на остаток сессии (инцидент с процессами/jj restore) — вся работа через Claude

Восемь батчей этой сессии полностью опубликованы и завершены:
- B-20260716T112303Z: T-027, T-028, T-030, T-032, T-033 (5 задач)
- B-20260716T205419Z: T-029, T-031, T-034, T-035, T-036, T-037, T-038, T-039, T-040, T-041,
  T-043, T-045, T-052, T-053, T-063 (15 задач)
- B-20260716T231924Z: T-042, T-044, T-046, T-047, T-049, T-054, T-055, T-056, T-062, T-065
  (10 задач; 2 волны приёма + пустая доборка wave 3, закрыта по причине
  только-конфликты-с-готовыми)
- B-20260717T002700Z: T-048, T-051, T-057, T-058, T-064, T-066, T-067, T-069, T-082, T-083
  (10 задач; 2 волны приёма + пустая доборка wave 3, закрыта по причине
  только-конфликты-с-готовыми)
- B-20260717T023700Z: T-050, T-060, T-061, T-072 (4 задачи; 1 волна приёма + пустая доборка
  wave 2, закрыта по причине только-конфликты-с-готовыми)
- B-20260717T041744Z: T-059, T-068 (2 задачи; capacity 2 из 5 в самой первой волне — внутри-
  батчевые конфликты доменов через монолитный узел webview/src/index.ts)
- B-20260717T053456Z: T-070, T-081, T-084, T-085, T-087, T-088, T-089 (7 задач; 2 волны
  приёма + пустая доборка wave 3, закрыта по причине только-конфликты-с-готовыми)
- B-20260717T073849Z: T-073, T-076, T-079, T-086 (4 задачи; 1 волна приёма + пустая доборка
  wave 2, закрыта по причине только-конфликты-с-готовыми — оставшиеся кандидаты (T-074,
  T-075, T-077, T-078) блокировались доменом хотя бы одной из готовых задач батча)

Итого 57 задач архивированы в Tasks_Done.md. main = `44b8d40ee6ce`, запушен. CI батча 8
завершился, но красен на dotnet(ubuntu/windows/macos-latest) и один e2e-тест
(`formatting-toolbar.e2e.ts`, timeout) — **пятый батч подряд с тем же широким
пре-существующим паттерном K-003** (файловые локи/clone-branch-delete гонки на CI-раннерах).
Явно подтверждено: ни один новый/изменённый тест батча 8 не в списке падений
(SpecDesk.Ai.Tests 41/41, SpecDesk.GitHub.Tests 161/161, новый e2e-гейт T-086
`accessibility-audit.e2e.ts` тоже прошёл). Обязательный набор CI-проверок публикации в
`.work/constraints.md` не задан — гейт архивации не применяется, архивация прошла как обычно.

**Внутрибатчевый инцидент батча 8 (T-076, обнаружен и исправлен):** коммит реализации был
случайно запечатан в чужом worktree из-за VCS cwd-carryover между раздельными Bash-вызовами
(та же известная категория багов, что и в T-055/T-056, T-058/T-062/T-065 ранее в сессии) —
bookmark `task/T-076` указывал на пустой коммит поверх `task/T-073`, что нашёл reviewer как
R-01. Исправлено processor'ом напрямую (repoint bookmark → describe → abandon пустышки →
seal forward), без делегирования coder'у — VCS-механика коммита, не дефект кода задачи.
Дано на свежее полное ревью реального diff — 2 прогона, 0 находок по существу.

**jj-специфичный технический инцидент батча 8 (обнаружен и исправлен):** прямая правка
`.work/status.md` перед `jj new main` не была закоммичена в реальный commit заранее
(нарушение установленной процедуры). Пострадали два из шести исторически отслеживаемых
`.work/*.md`-файлов — `status.md` (контент восстановлен из stranded-ревизии `02b0a615` через
`jj file show`) и `Tasks_Queue.md` (нёс Фаза-1.6-пометки захвата для 4 задач, откаченные до
`не начата` — не потребовали восстановления, поскольку архивация всё равно удаляет
опубликованные задачи из очереди целиком по дескриптору, не по промежуточной пометке).
`Tasks_Done.md`/`Active_Task.md`/`Checkpoint.md`/`Review.md` не пострадали. Прочие рабочие
файлы батча (gitignored/untracked) не затронуты. Митигация: фиксировать
`status.md`/`Tasks_Queue.md`-правки в реальный commit на main НЕМЕДЛЕННО перед любым
`jj new main`.

**Дополнительно обнаружен и исправлен jj↔git line-ending дефект (за пределами known
`.cmd`-паттерна из прежних батчей):** `scripts/update-contract-fixtures.cmd`
(`*.cmd eol=crlf`) был закоммичен ранее этой сессии с сырыми LF-байтами напрямую в blob (jj
не применяет git clean-фильтр при коммите) — обычный `git checkout -- '*.cmd'` не
восстанавливал материализацию, т.к. git считал working tree уже «чистым» по индексу/
stat-кешу; потребовался `rm` файла перед повторным `git checkout --` для форсирования
настоящего smudge-прохода. Восстановленное CRLF-содержимое закоммичено (реальное исправление
blob, не только рабочей копии).

**Обнаруженная инфраструктурная проблема резолвера очереди (остаётся актуальной, не
устранена мной — вне моих полномочий менять tools/queue-tx.ps1 самостоятельно):**
`queue-tx ready` ожидает заголовки `Tasks_Done.md` в форме `### [T-NNN]`, но фактический
формат архива этого проекта — `Статус: выполнена` / `Исходная задача: [T-NNN] ...`. T-071
(зависит от T-050, давно архивированной) продолжает считаться не готовой каждую когорту.
Рекомендую оператору завести отдельную задачу на исправление формата `Tasks_Done.md` или
регэкспа резолвера.

Примечание (approval-requests, аудиторские записи, не блокируют публикацию, консистентно с
документированной политикой `.work/constraints.md`): всего теперь 5 открытых
approval-request'ов за сессию — T-057, T-072, T-070, T-089, T-073.

База знаний (`.work/knowledge/`) обновлена в батче 8: 10 записей переподтверждены, 1
помечена `stale-suspect` (K-014), добавлено 2 новых (K-017 accessibility KNOWN_EXCEPTIONS,
K-018 dialogs.ts inline-prompt IPC-роундтрип). Итого 18 записей в базе.

Очередь: 6 задач остаются «не начата» — T-071 (блокирована инфраструктурной проблемой
резолвера выше), T-074, T-075, T-077, T-078, T-080 (зависит от T-079, теперь архивированной —
должна разблокироваться следующей когортой). Готов приступить к следующей когорте.
