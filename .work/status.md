# Оркестр — обзор
- Обновлено: 2026-07-17T05:25:00Z
- Оркестратор: processor — этап: батч завершён, переход к следующей когорте
- Provider: claude
- Codex: отключён на остаток сессии (инцидент с процессами/jj restore) — вся работа через Claude

Шесть батчей этой сессии полностью опубликованы и завершены:
- B-20260716T112303Z: T-027, T-028, T-030, T-032, T-033 (5 задач)
- B-20260716T205419Z: T-029, T-031, T-034, T-035, T-036, T-037, T-038, T-039, T-040, T-041,
  T-043, T-045, T-052, T-053, T-063 (15 задач)
- B-20260716T231924Z: T-042, T-044, T-046, T-047, T-049, T-054, T-055, T-056, T-062, T-065
  (10 задач; 2 волны приёма + пустая доборка wave 3, закрыта по причине
  только-конфликты-с-готовыми)
- B-20260717T002700Z: T-048, T-051, T-057, T-058, T-064, T-066, T-067, T-069, T-082, T-083
  (10 задач; 2 волны приёма + пустая доборка wave 3, закрыта по причине
  только-конфликты-с-готовыми)
- B-20260717T023700Z: T-050, T-060, T-061, T-072 (4 задачи; 1 волна приёма + пустая доборка
  wave 2, закрыта по причине только-конфликты-с-готовыми — реально активных задач не было
  вовсе, все 4 сразу дошли до готова к слиянию)
- B-20260717T041744Z: T-059, T-068 (2 задачи; capacity 2 из 5 в самой первой волне — не
  из-за пустой очереди, а из-за внутрибатчевых конфликтов доменов через монолитный узел
  webview/src/index.ts; wave 2 top-up вернула только-конфликты-с-готовыми)

Итого 46 задач архивированы в Tasks_Done.md и удалены из Tasks_Queue.md. main =
`56994e892e58fdf081fb269fb91d3e908d996597`, запушен. CI по шестому батчу завершился, но красен
на dotnet(macos/windows/ubuntu-latest) и e2e Layer 1 — третий батч подряд с тем же широким
пре-существующим паттерном (HostControllerFilesystemTests и смежные классы). Явно подтверждено,
что ни PathIdentityTests, ни новый case-variant регресс-тест T-068 не входят в список падений.
Обязательный набор CI-проверок публикации в `.work/constraints.md` не задан — гейт архивации не
применяется, архивация прошла как обычно.

**Обнаружена инфраструктурная проблема (не блокирует текущую работу, но требует отдельного
исправления):** резолвер готовности очереди (`queue-tx ready` → `Get-ArchiveHeaderIds`) ожидает
заголовки `Tasks_Done.md` в форме `### [T-NNN]`, но фактический формат архива этого проекта
(используемый до и во время всей этой сессии) — `Статус: выполнена` / `Исходная задача: [T-NNN]
...`. Из-за этого задачи с `Предпосылки:`, указывающими на уже архивированные T-ID (например
T-071 → T-050), не резолвятся резолвером как «предпосылка закрыта» и будут неопределённо долго
откладываться planner'ом. planner корректно не переопределял решение резолвера сам (как
предписано контрактом) — просто пропускал такие задачи. Нужно либо привести `Tasks_Done.md` к
формату `### [T-NNN]`, либо расширить регэксп `Get-ArchiveHeaderIds` под фактически
используемый в этом проекте формат — рекомендую оператору завести отдельную задачу на это.

**Аномалия merger в шестом батче (единичный случай, не воспроизводился в других батчах этой
сессии):** merger пропустил собственную проверку сборки/тестов («сборка: не проверялась») и не
создал bookmark `integration/B-id`. processor поймал оба пробела сам: независимо прогнал
`dotnet build`/`dotnet test` (0 errors/0 failed, включая новые PathIdentityTests) и
`npm run typecheck`/`npm test` (72 файла/1148 passed) в интеграционном worktree, затем создал
недостающий bookmark вручную перед full_reviewer. Итог зафиксирован в journal.md; рекомендую
иметь в виду при разборе логов merger'а в будущих батчах.

Примечание (T-072, human-review категория, второй прецедент): planner классифицировал T-072
Риск: high (мутирующая запись в реальный GitHub PR через существующие auth-токены). Подан
approval-request `apr-f4382d27fa390343f5adf887c1712505` (аудиторская запись, `pending` на момент
публикации) — не блокировал публикацию, консистентно с T-057 и документированной политикой
`.work/constraints.md` этого проекта. Оба approval-request'а (T-057, T-072) остаются открытыми
для оператора.

База знаний (`.work/knowledge/`) обновлена в шестом батче: добавлена запись K-011 (унифицированная
политика идентичности путей PathIdentity.cs), обновлены K-003 (confidence med→high, третье подряд
подтверждение CI-нестабильности) и K-005.

Важное техническое наблюдение сессии (остаётся в силе): каждый `jj new main` после успешной
публикации материализует рабочую копию default-воркспейса из дерева коммита main, откатывая любые
прямые правки шести исторически отслеживаемых `.work/*.md`-файлов (Tasks_Queue.md, Tasks_Done.md,
status.md, Active_Task.md, Checkpoint.md, Review.md), если они не были закоммичены в main ДО этого
вызова. Митигация соблюдена и в шестом батче — правки очереди/архива этого батча коммитятся
отдельным коммитом сразу после архивации, до следующего `jj new main` (следующей публикации).

Дополнительное наблюдение (jj-специфичное, устойчивый паттерн во всех четырёх последних батчах):
интеграционный воркспейс (`_integration`), созданный через `jj workspace add -r <base>`, порождает
пустой безымянный коммит без описания в основании цепочки слияний — блокирует `jj git push`, пока
его явно не описать непосредственно перед push. Стандартный шаг для этой схемы, не разовая аномалия.

Очередь: 17 задач остаются «не начата» (было 19 на старте этого батча, 63 на старте сессии).
Готов приступить к следующей когорте.
