# Оркестр — обзор
- Обновлено: 2026-07-17T09:47:00Z
- Оркестратор: processor — этап: батч опубликован, переход к учёту/архивации
- Provider: claude
- Codex: отключён на остаток сессии (инцидент с процессами/jj restore) — вся работа через Claude
- Батч: B-20260717T073849Z · слито 4/4 · опубликовано, main=`2dca56aa599a7e3e4c906c2256e7588a312337ac`

Батч B-20260717T073849Z (T-073, T-076, T-079, T-086) прошёл интеграционное ревью (2 прогона,
0 открытых) и локально ff-влит в main (`cad18d2cedbe724a47f8448d88ecf620b690e4ea`). T-076 в
этом батче потребовал внутрибатчевой коррекции: коммит реализации был случайно запечатан в
чужом worktree из-за cwd-carryover между раздельными Bash-вызовами (та же известная категория
багов jj-сессии) — bookmark `task/T-076` указывал на пустой коммит поверх `task/T-073`, что и
нашёл reviewer как R-01. Исправлено processor'ом напрямую (repoint bookmark → describe на
настоящей ревизии → abandon пустышки → seal forward), без делегирования coder'у — это
VCS-механика коммита, а не дефект кода задачи. Новый bookmark `task/T-076` =
`bdea133c8320b52b4f0dfe32f35ade0b78720bfd`, дан на свежее полное ревью (2 прогона, 0
открытых) — готово к слиянию по существу. Wave-2 top-up (capacity 4) вернул 0 задач — все
готовые по зависимостям кандидаты (T-074, T-075, T-077, T-078) блокированы доменом хотя бы
одной из T-073/T-079/T-086; приём закрыт причиной `только-конфликты-с-готовыми`.

T-073 (Риск: high — расширяет публичный native↔webview IPC-контракт новым видом сообщения
`action.publish` + необратимое git-действие merge/удаление ветки) — категория
constraints.md «архитектурные границы и публичные контракты/API»: подан
approval-request `apr-80ccd6b305cf5f7b64db73433f19d55a` как аудиторская запись (не блокирует
публикацию, консистентно с документированной политикой `.work/constraints.md` и прецедентом
T-057/T-072/T-070/T-089). Всего теперь 5 открытых approval-request'ов за сессию.

**Публикация закреплена.** main = `2dca56aa599a7e3e4c906c2256e7588a312337ac` (ff-влит из
integration/B-20260717T073849Z, запушен). CI на опубликованном SHA красен на
dotnet(ubuntu/windows/macos-latest) и e2e Layer 1 — **пятый батч подряд с тем же широким
пре-существующим паттерном** (K-003): все падающие тесты — HostControllerFilesystemTests/
HostControllerLifecycleTests/HostControllerGitTests/HostControllerAccountTests (файловые
локи, clone/branch-delete гонки на CI-раннерах) + один флейки e2e `formatting-toolbar.e2e.ts`
(timeout на toBeEnabled). Явно подтверждено: ни один новый/изменённый тест этого батча
(SpecDesk.Ai.Tests — 41/41, SpecDesk.GitHub.Tests — 161/161, HostControllerChatTests/
HostControllerWorkspaceTests/HostControllerReviewTests — не в списке падений) не затронут;
новый e2e-гейт T-086 `accessibility-audit.e2e.ts` в списке падений тоже отсутствует (прошёл).
Обязательный набор CI-проверок публикации в `.work/constraints.md` не задан — гейт архивации
не применяется, архивация проходит как обычно (best-effort CI-наблюдение, не жёсткий гейт).

Семь предыдущих батчей этой сессии полностью опубликованы и завершены:
- B-20260716T112303Z: T-027, T-028, T-030, T-032, T-033 (5 задач)
- B-20260716T205419Z: T-029, T-031, T-034, T-035, T-036, T-037, T-038, T-039, T-040, T-041,
  T-043, T-045, T-052, T-053, T-063 (15 задач)
- B-20260716T231924Z: T-042, T-044, T-046, T-047, T-049, T-054, T-055, T-056, T-062, T-065
  (10 задач; 2 волны приёма + пустая доборка wave 3, закрыта по причине
  только-конфликты-с-готовыми)
- B-20260717T002700Z: T-048, T-051, T-057, T-058, T-064, T-066, T-067, T-069, T-082, T-083
  (10 задач; 2 волны приёма + пустая доборка wave 3, закрыта по причине
  только-конфликты-с-готовыми)
- B-20260717T023700Z: T-050, T-060, T-061, T-072 (4 задачи; 1 волна приёма + пустая доборка
  wave 2, закрыта по причине только-конфликты-с-готовыми)
- B-20260717T041744Z: T-059, T-068 (2 задачи; capacity 2 из 5 в самой первой волне — внутри-
  батчевые конфликты доменов через монолитный узел webview/src/index.ts)
- B-20260717T053456Z: T-070, T-081, T-084, T-085, T-087, T-088, T-089 (7 задач; 2 волны
  приёма + пустая доборка wave 3, закрыта по причине только-конфликты-с-готовыми)

Итого 53 задачи архивированы в Tasks_Done.md к началу этого (восьмого) батча. main ДО этого
батча = `55d3c5e7d094353a5799166d19ca5a7becf60cbd`. CI по седьмому батчу завершился, но красен
на dotnet(macos/windows/ubuntu-latest) и e2e Layer 1 — четвёртый батч подряд с тем же широким
пре-существующим паттерном. Обязательный набор CI-проверок публикации в `.work/constraints.md`
не задан — гейт архивации не применяется, архивация идёт как обычно.

**Обнаруженная инфраструктурная проблема резолвера очереди (остаётся актуальной, не устранена
мной — вне моих полномочий менять tools/queue-tx.ps1 самостоятельно):** `queue-tx ready`
ожидает заголовки `Tasks_Done.md` в форме `### [T-NNN]`, но фактический формат архива этого
проекта — `Статус: выполнена` / `Исходная задача: [T-NNN] ...`. T-071 (зависит от T-050, давно
архивированной) продолжает считаться не готовой каждую когорту. Рекомендую оператору завести
отдельную задачу на исправление формата `Tasks_Done.md` или регэкспа резолвера.

**Важное техническое наблюдение (повторилось в этом батче — процедура восстановления
применена):** `jj new main` после публикации материализует рабочую копию default-воркспейса из
дерева коммита main, снимая working copy с прежней scratch-цепочки (снапшоты auto-commit'ов в
корневом воркспейсе поверх старой базы). Из шести исторически отслеживаемых git-путей
(`.work/{Active_Task,Checkpoint,Review,Tasks_Done,Tasks_Queue,status}.md`) реально пострадал
только `status.md` (единственный из них, отредактированный до вызова в этом батче) — контент
восстановлен из stranded-ревизии `02b0a615` (`jj file show -r 02b0a615 .work/status.md`) и
переписан этим же обновлением. Все прочие рабочие файлы батча (`batch.md`, `cohort_state.md`,
`merge_report.md`, `integration_state.md`, `tasks/*/task.md`, `tasks/*/review.md`) —
gitignored, untracked, `jj new` их не трогает вовсе, потерь не было. Митигация на будущее:
фиксировать `status.md`/`Tasks_Queue.md`/`Tasks_Done.md`/`journal.md`-правки в реальный commit
на main НЕМЕДЛЕННО перед любым `jj new main`, а не полагаться на промежуточные снапшоты
scratch-цепочки корневого воркспейса.

Дополнительное наблюдение (jj-специфичное, устойчивый паттерн во всех батчах): интеграционный
воркспейс (`_integration`), созданный через `jj workspace add -r <base>`, порождает пустой
безымянный коммит без описания в основании цепочки слияний — блокирует `jj git push`, пока его
явно не описать непосредственно перед push. В этом батче описан сразу при создании воркспейса.

Очередь: к началу этого батча оставалось 10 задач «не начата». T-071 остаётся блокированной
инфраструктурной проблемой резолвера выше. После публикации этого батча (T-073/T-076/T-079/
T-086) ожидается разблокировка T-074/T-075/T-077/T-078/T-080 доменами.
