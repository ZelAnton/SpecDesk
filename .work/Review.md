# Review Log

### [SUMMARY-R-2026-07-05 04:23] Итог ревью задачи — статус: готово к следующей задаче
- Активная задача: [T-001] Исправить S-01: PushBranch сообщает об успехе при отклонённом push
- Прогонов выполнено: 2
- Открытых проблем: 0
- Вывод: все критерии готовности выполнены. `OnPushStatusError` добавлен в `PushOptions`,
  результат push проверяется через `ThrowIfRejected`, `PushBranch` бросает
  `InvalidOperationException` при отклонении любого ref; вызывающая сторона
  (`HostController.RunReviewPublish`) корректно ловит исключение через `SendError` и НЕ
  продвигает жизненный цикл; doc-комментарий `GitPublishing.cs` приведён в соответствие;
  добавлены тесты и запись в `CHANGELOG.md`. Новых серьёзных проблем не найдено. Можно
  переходить к следующей задаче.

### [SUMMARY-R-2026-07-05 04:32] Итог ревью задачи — статус: готово к следующей задаче
- Активная задача: [T-002] Исправить S-02: не-GitHub push-эндпоинт получает Windows SSO-креды
- Прогонов выполнено: 2
- Открытых проблем: 0
- Вывод: все критерии готовности выполнены. `CredentialsProvider` больше не возвращает
  `DefaultCredentials()` для не-GitHub URL — логика вынесена в `ResolveCredentials`, которая для
  HTTPS `github.com` отдаёт `UsernamePasswordCredentials` с токеном, а для любого другого эндпоинта
  (look-alike, `http://`, `ssh://`, локальный файл, `null`) бросает `InvalidOperationException` ещё
  до предъявления креденшелов, поэтому NTLM/Negotiate-хэндшейк не инициируется, а push безопасно
  проваливается (исключение пробрасывается через `Network.Push` в `PushBranch`, вызывающая сторона
  его ловит и не продвигает жизненный цикл). Поведение push на GitHub не изменилось. Добавлены 6
  юнит-тестов на `ResolveCredentials` (единственная тестируемая точка решения — локальный bare-repo
  фикстур не вызывает `CredentialsProvider`), doc-комментарий `GitPublishing.cs` и комментарий у
  кода приведены в соответствие с реальным поведением, запись добавлена в `CHANGELOG.md`
  (`## [Unreleased]` → `### Fixed`). Других путей с `DefaultCredentials` в продакшене нет. Новых
  серьёзных проблем не найдено. Можно переходить к следующей задаче.

### [SUMMARY-F-2026-07-05 05:05] Итог интеграционного ревью — статус: готово к следующей задаче
- Проверено задач (по Tasks_Done.md): 3 (T-001, T-002, T-003)
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Вывод: интеграционных проблем не найдено, решение в целом согласовано и
  соответствует накопленным требованиям. Три исправления (S-01 отклонённый push,
  S-02 SSO-креды на не-GitHub push, S-03 потеря автосейва при принудительном
  checkout) локализованы и корректно сочетаются: T-001 и T-002 сосуществуют в одном
  `PushBranch`/`PushOptions` (OnPushStatusError + ThrowIfRejected рядом с
  ResolveCredentials, оба пути гарантированно бросают исключение), T-003 бросает
  `DirtyWorkingTreeException` в `BeginEdit` до любых деструктивных операций.
  Единственный вызывающий код (`HostController.OnEdit` под `_repoGate`,
  `RunReviewPublish`) ловит исключения раньше продвижения жизненного цикла и не
  протекает git-лексикой в сообщения автору. Контракты (`IDocumentVersioning`,
  `IGitPublishing`, doc-комментарии) и тестовый двойник `FakeVersioning` приведены
  в соответствие. Прежний тест, утверждавший старое (теперь неверное) поведение
  BeginEdit, обновлён — регрессий нет. Сборка чистая (0 warnings при
  warnings-as-errors), все тест-проекты зелёные (Git 32, Host 132, GitHub 115 [1
  ожидаемо пропущен — реальный device-flow], Core 79, Markdown 58, Diff 24,
  Contracts 7). Открытых R-записей нет — детальное ревью закрыто по всем трём
  задачам. Можно продолжать (чекпоинт с push).

### [SUMMARY-R-2026-07-05 05:20] Итог ревью задачи — статус: готово к следующей задаче
- Активная задача: [T-004] Исправить S-04: запрос app:// с закодированным NUL валит процесс
- Прогонов выполнено: 2
- Открытых проблем: 0
- Вывод: все критерии готовности выполнены двухслойной защитой. (1) `AppAssetResolver.ResolveRelative`
  отбраковывает путь с недопустимыми символами (встроенный NUL из `%00`) через
  `Path.GetInvalidPathChars()` ДО вызова `Path.GetFullPath`, устраняя `ArgumentException` в источнике;
  containment- и reparse-проверки не затронуты (проверка `../`-траверсала и percent-encoded траверсала
  по-прежнему проходит). (2) `Program.ServeAsset` (нативный WebView2-колбэк) стал `internal` для
  тестируемости и получил catch-all `catch (Exception)`, который логирует через `Serilog.Log.Error` и
  падает в тот же broken-resource ответ (`Stream.Null` + `text/plain`), гарантируя, что никакое
  исключение не уйдёт в message pump независимо от причины. Порядок catch-клауз корректен
  (фильтр IOException/UnauthorizedAccessException раньше общего), `out contentType` присваивается на всех
  путях, поведение File.OpenRead-ошибок сохранено. Запрос `app://repo/a%00.png` возвращает безопасный
  broken-resource ответ, а не валит процесс. Добавлены тесты на некорректные символы пути
  (`Resolve_PercentEncodedNul_...`, `ResolveRelative_EmbeddedNul_...`) и на инвариант "ServeAsset никогда
  не бросает наружу" (`ProgramServeAssetTests`: адверсариальные/отсутствующие входы + happy path);
  `InternalsVisibleTo` для доступа к internal-методу присутствует, `Program` доступен из
  `SpecDesk.Host.Tests` по вложенности пространств имён. Cross-language parity-тест (`ContainmentParityTests`)
  не затронут — он проверяет `IsInside`, а не `ResolveRelative`. Запись добавлена в `CHANGELOG.md`
  (`## [Unreleased]` → `### Fixed`). Новых серьёзных проблем не найдено. Можно переходить к следующей задаче.

### [SUMMARY-R-2026-07-05 04:56] Итог ревью задачи — статус: готово к следующей задаче
- Активная задача: [T-003] Исправить S-03: принудительный checkout в BeginEdit уничтожает
  автосохранённую работу другого документа
- Прогонов выполнено: 2
- Открытых проблем: 0
- Вывод: все критерии готовности выполнены. `BeginEdit` (`LibGit2DocumentVersioning`) перед
  принудительным checkout проверяет рабочее дерево через `RetrieveStatus(IncludeUntracked=false)`
  и, если оно грязное на ветке, отличной от целевой, бросает новое `DirtyWorkingTreeException`
  ещё до любых деструктивных операций — незакоммиченный автосейв документа A остаётся на диске
  нетронутым (тест `BeginEdit_RefusesToStartADifferentDraftWhileAnotherBranchsWorkingTreeIsDirty`
  подтверждает: контент файла и текущая ветка не меняются). Возобновление того же документа/ветки
  (`alreadyOnTargetBranch`) не блокируется (тест `...ResumingTheSameBranch...`), а неотслеживаемые
  файлы не принимаются за чужой черновик (тест `...UntrackedFile...`). Хост ловит исключение
  раньше общего `InvalidOperationException`-обработчика, показывает plain-language сообщение без
  git-лексики и НЕ продвигает жизненный цикл — состояние остаётся `Опубликовано` (тест
  `Edit_WhenAnotherDraftsWorkingTreeIsDirty_...`). Запись в `CHANGELOG.md` под `## [Unreleased]` →
  `### Fixed`. Единственный вызов `BeginEdit` (в `HostController.OnEdit`, под `_repoGate`) —
  новых гонок нет; данные не теряются, состояние согласовано. Затронутые тесты зелёные (Git.Tests
  13, Host.Tests 15), сборка проходит при warnings-as-errors. Известный смежный зазор (сброс
  собственного автосейва при возобновлении того же документа) явно задокументирован в коде и
  вынесен за рамки данной задачи. Новых серьёзных проблем не найдено. Можно переходить к
  следующей задаче.

### [SUMMARY-R-2026-07-05 06:40] Итог ревью задачи — статус: готово к следующей задаче
- Активная задача: [T-005] Исправить S-05: автосохранение не перепроверяется под _repoGate — может воскресить отменённый черновик
- Прогонов выполнено: 2
- Открытых проблем: 0
- Вывод: все критерии готовности выполнены; R-001 (пройдя прогоны 1–5) проверен и закрыт (корректно
  исправлен). Финальный дизайн: авторитетный монотонный `_draftGeneration` бампается ИСКЛЮЧИТЕЛЬНО под
  `_repoGate` сразу после git-мутации (`OnEdit` после `BeginEdit`, `OnDiscard` после `Discard`);
  `CancelAutosave` намеренно не бампает. Введён `_sync`-guarded компаньон `_textGeneration`,
  присваиваемый значением `_draftGeneration` в ТОЙ ЖЕ критической секции `lock(_sync)`, что и КАЖДОЕ
  присвоение `_text` (`OnEditorChanged`, `LoadFile`, `OnDiscard`-reset) — проверено: ни одно присвоение
  `_text` не оставляет `_textGeneration` позади. Снимок `RunDiskAutosave` захватывает `_textGeneration`
  (согласованный с `_text`), а перепроверка под `_repoGate` сравнивает его с ЖИВЫМ `_draftGeneration`.
  Тем самым торн-пара «новое поколение + старый текст» из прогона 4 стала невозможной: любой снимок
  черновика несёт поколение, строго меньшее авторитетного после отката, и перепроверка отбраковывает
  его при ЛЮБОМ порядке блокировок — остаточное окно `_repoGate`→`_sync` закрыто без нарушения
  lock-ordering (`_sync` не берётся под `_repoGate`). Прежний бесполезный «второй бамп» (прогон 3)
  убран. Два детерминированных регрессионных теста (`HostControllerAutosaveRaceTests`) зелёные и
  осмысленно защищают инвариант, включая торн-снимок «bump-без-reset». Проверено локально: `dotnet build`
  SpecDesk.slnx — 0 warnings (warnings-as-errors), `SpecDesk.Host.Tests` — 145 passed / 0 failed.
  `CHANGELOG.md` под `## [Unreleased]` описывает исправление. Новых серьёзных проблем за 2 прогона не
  найдено (замеченный edge-case: сбой перечитывания файла сразу после успешного `Discard` оставляет
  рассинхрон lifecycle-состояния, но это НЕ приводит к порче данных — устаревшая запись по-прежнему
  корректно пропускается — и такой же зазор существовал в прежнем пути через `LoadFile`, т.е. это не
  регрессия уровня Critical/High). Можно переходить к следующей задаче.

### [SUMMARY-R-2026-07-05 12:25] Итог ревью задачи — статус: готово к следующей задаче
- Активная задача: [T-006] Исправить S-06: гейт жизненного цикла в OnDiscard не атомарен с проверкой publish-in-flight
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Вывод: все критерии готовности выполнены; R-002 (CHANGELOG удалял заголовок релиза 0.1.0) проверен
  и закрыт как корректно исправленный — заголовок `## [0.1.0] - 2026-07-04` и весь его контент снова
  под своим заголовком, новый пункт добавлен только в `## [Unreleased]` → `### Fixed`. Основное
  исправление: в `OnDiscard` вычисление `Lifecycle.tryStep(_state, "discard")` перенесено ВНУТРЬ той же
  критической секции `lock(_sync)`, что и проверка `_publishInFlight` (по образцу
  `OnSendForReview`/`OnUpdateReview`); заодно `_repoRoot`/`_currentPath`/`_branch`/`_baseBranch`
  снапшотятся под тем же `_sync`, а null-проверка перенесена на локальные копии. Прежнее окно закрыто:
  раньше расщеплённое чтение (несинхронизированный `tryStep`, затем отдельный вход в `_sync` только ради
  флага) позволяло устаревшему `next`, вычисленному по до-push состоянию `Draft`, пережить полный
  round-trip `Send for review` (фоновый `RunReviewPublish` → `TryAdvanceReview` ставит `InReview` →
  `ClearPublishInFlight` снимает флаг в двух отдельных `lock(_sync)`), и проверка «только флаг» пропускала
  Discard, удаляя ветку под только что открытым PR. Теперь состояние и флаг читаются вместе под одной
  блокировкой: если send успел полностью осесть, `tryStep("inReview","discard")` возвращает "" (перехода
  `InReview→Discard` нет в `Lifecycle.fs`) и Discard корректно не выполняется; если send ещё в полёте —
  ловит проверка `_publishInFlight`. IPC-сообщения диспетчеризуются серийно на однопоточном насосе
  WebView2 (`OnMessage`→`DispatchMessage`), поэтому `OnDiscard` и `OnSendForReview` не конкурируют между
  собой; единственная параллельность — с фоновым `RunReviewPublish`, что и закрывает фикс. Регрессия по
  порядку блокировок отсутствует: `_sync`-гейт, `_repoGate`-блок и финальный `_sync`-reset идут
  последовательно (не вложены), правило «`_sync` не берётся под `_repoGate`» не нарушено. Добавлен
  детерминированный регрессионный тест `Discard_reads_the_lifecycle_state_and_the_publish_flag_atomically`
  (через рефлексию удерживает `_sync`, имитирует «только что осевший send», проверяет, что `Discard` не
  вызывается и состояние остаётся `inReview`); имена wire-состояний ("draft"/"inReview") и отсутствие
  перехода `InReview→Discard` сверены с `Lifecycle.fs`. Запись в `CHANGELOG.md` под `## [Unreleased]` →
  `### Fixed`. Новых серьёзных проблем за 2 прогона не найдено. Можно переходить к следующей задаче.

### [SUMMARY-F-2026-07-05 12:41] Итог интеграционного ревью — статус: готово к следующей задаче
- Проверено задач (по Tasks_Done.md): 6 (T-001…T-006 — исправления S-01…S-06)
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Вывод: интеграционных проблем не найдено; шесть исправлений согласованно сочетаются
  друг с другом и с накопленными требованиями. Разбор по направлениям:
  1. Git-слой (`LibGit2DocumentVersioning.cs`) чисто вмещает три задачи сразу: T-001
     (`OnPushStatusError` + `ThrowIfRejected`), T-002 (`ResolveCredentials`, отказ для
     не-GitHub-эндпоинта) и T-003 (`BeginEdit` бросает `DirtyWorkingTreeException` до
     любого форс-checkout при грязном дереве чужой ветки). Дублирования нет,
     противоречий нет; `PushOptions` совмещает колбэки T-001 и T-002 в одном месте.
  2. Host-слой (`HostController.cs`) согласованно вмещает T-004 (вызов ServeAsset/
     AppAssetResolver), T-005 (токен поколения `_draftGeneration`/`_textGeneration` +
     перепроверка автосейва под `_repoGate`) и T-006 (атомарный гейт discard). T-006
     переиспользует механизм поколения, введённый T-005 (бамп `_draftGeneration` после
     `Discard`), — не дублирует, а надстраивает. Токен поколения бампается ровно в двух
     checkout-меняющих точках (OnEdit после BeginEdit:530, OnDiscard после Discard:626),
     обе строго под `_repoGate`; иных checkout-операций в решении нет — инвариант полон.
     Правило порядка блокировок (`_sync` не берётся под `_repoGate`) выдержано во всех
     трёх задачах.
  3. Регрессий между задачами нет: бамп поколения в OnEdit пропускается при
     `DirtyWorkingTreeException` (T-003) — ложного бампа на отказанном редактировании нет;
     T-003 и T-005 сочетаются (легитимный автосейв документа A корректно ложится на его же
     ветку, а форс-checkout документа B затем отклоняется T-003); настроечный край T-006
     (state продвигается в `TryAdvanceReview` раньше снятия флага в `finally`
     `ClearPublishInFlight`) сверен с реальным `RunReviewPublish` — Discard больше не может
     осиротить только что открытый PR.
  4. Контракты выровнены: `IGitPublishing`/`GitPublishing.cs` и `IDocumentVersioning`/
     `DocumentVersioning.cs` (включая doc-комментарий `BeginEdit` и тип
     `DirtyWorkingTreeException`) соответствуют реализациям; тест-двойник `FakeVersioning`
     зеркалит оба интерфейса и умеет воспроизводить все новые пути (throw
     `DirtyWorkingTreeException`, `PushGate`/`SaveGate`).
  5. Сквозные аспекты в норме: все новые сообщения автору простоязычны, без git-лексики
     (только продуктовые слова draft/version/discard/review); `ServeAsset` имеет catch-all
     с логированием после узкого IOException/UnauthorizedAccessException; CHANGELOG содержит
     все шесть записей под `## [Unreleased] → Fixed`, а заголовок `## [0.1.0]` цел (прежняя
     R-002 остаётся закрытой).
  6. Технический долг: маркеров TODO/FIXME/заглушек в изменённых областях нет. Пустое
     jj-изменение 27b9f54 («no description») трогает только файлы `.work/` — процессный
     артефакт, не код. Известный смежный край (сбой перечитывания файла сразу после
     успешного `Discard` рассинхронизирует lifecycle-состояние) уже зафиксирован
     reviewer'ом как не-Critical и порчи данных не даёт (токен поколения по-прежнему
     отбраковывает устаревший автосейв).
- Проверка сборки/тестов: `dotnet build SpecDesk.slnx` — 0 ошибок при warnings-as-errors;
  `dotnet test SpecDesk.slnx` — все зелёные (Contracts 7, Markdown 58, Core 79, Diff 24,
  GitHub 115, Git 32, Host 146 = 461 passed / 0 failed / 0 skipped). Изменений в webview
  эти шесть задач не вносят, поэтому TS-набор данной партией не затрагивается.
- Открытых R-записей на момент ревью нет (только закрытые SUMMARY-R). Можно продолжать
  (следующие задачи очереди T-007… / чекпоинт с push).

### [SUMMARY-R-2026-07-05 13:31] Итог ревью задачи — статус: готово к следующей задаче
- Активная задача: [T-007] Исправить S-08: семантический diff не видит правки чекбоксов, сносок и
  списков определений
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Вывод: все критерии готовности выполнены; ранее открытая R-003 (CHANGELOG сносил заголовок релиза
  `## [0.1.0] - 2026-07-04`) проверена и закрыта как корректно исправленная — теперь в CHANGELOG ровно
  два заголовка версий (`## [Unreleased]` на строке 8 и `## [0.1.0] - 2026-07-04` на строке 64), контент
  0.1.0 снова под своим заголовком, новый пункт про семантический diff добавлен только в
  `## [Unreleased]` → `### Fixed`. Основное решение: `Projection.fs` больше не роняет инлайны
  `TaskList`/`FootnoteLink` и блоки `DefinitionList`/`FootnoteGroup` в `| _ -> None` — добавлены
  `inlineOf`-случаи (`Ast.TaskListMarker`, `Ast.FootnoteRef`) и `blockOf`-случаи
  (`definitionListOf`, `footnoteGroupOf`); DU `Ast` расширен кейсами `TaskListMarker`/`FootnoteRef`
  (инлайны) и `DefinitionList`/`Footnotes` с записями `DefinitionItem`/`Footnote`. Модель Markdig
  сверена рефлексией по реальному дереву разбора: `DefinitionList → DefinitionItem → (DefinitionTerm…
  + блоки тела)` — проекция терминов и тела совпадает с фактической структурой, поэтому правки и
  термина, и определения детектируются; `FootnoteGroup → Footnote → Paragraph(+backlink FootnoteLink)`
  — тело сноски (включая авто-бэклинк) проецируется, правка тела видна diff'у. Проверено, что
  переключение чекбокса меняет `Content` (`TaskListMarker false`≠`true`), и `Inlines.flatten` отдаёт
  `[x]`/`[ ]`, из-за чего similarity падает и узел классифицируется как `Changed` (в тесте ровно на
  пороге 0.5); даже вырожденный край (пустой todo) даёт `Removed`+`Added`, т.е. `hasChanges = true` —
  «нет изменений» невозможно ни при каком переключении. NRE на `fl.Footnote.Label` исключён:
  неразрешённая ссылка `[^x]` вообще не порождает `FootnoteLink` (остаётся литералом), а у
  разрешённой `Footnote` всегда не-null. Исчерпывающие сопоставления по `Ast.Block` обновлены
  (`AstDiff.kindTag`/`blockText` покрывают оба новых кейса), `DiffWire.childTexts` имеет catch-all и
  отдаёт изменённые `DefinitionList`/`Footnotes` как whole-block `Changed` — сборка при
  warnings-as-errors не ломается, новые кейсы не теряются молча. Регрессий по существующим тестам нет
  (прежде синтаксис task/footnote/definition в тестах не использовался). Добавлены целевые тесты в
  `ProjectionTests.fs` и `AstDiffTests.fs` (проекция маркера/лейбла/термина/тела + diff-детект
  переключения чекбокса, правки тела сноски и определения). Новых серьёзных проблем за 2 прогона не
  найдено. Можно переходить к следующей задаче.

### [SUMMARY-R-2026-07-05 13:47] Итог ревью задачи — статус: готово к следующей задаче
- Активная задача: [T-008] Исправить S-09: некорректный {date:FMT} в .spectool.toml молча отключает
  "Редактировать"
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Вывод: все критерии готовности выполнены; открытых R-записей на входе не было. Решение полностью
  локализовано в `WorkflowConfig.fs`. (1) Разворачивание токенов вынесено в `tryExpand`, обёрнутое в
  `try … with _ -> None` — тот же guard-стиль, что и `ImageEngine.insertForHost` (единственный
  источник исключения — `ctx.Date.ToString(fmt, …)` в `Tokens.expand`, `{date:q}` даёт
  `FormatException`), поэтому `branchNameForHost`/`commitMessageForHost` больше не бросают. (2)
  Добавлен `isValidGitRef` — консервативная проверка git-ref-символов (нет whitespace/control,
  `~^:?*[\`, `..`, `@{`, ведущего/финального/двойного `/`); ветка `{date:}` (пустой формат) даёт
  space/colon-содержащую строку общего формата и корректно отбраковывается — причём результат
  одинаков независимо от того, бросает `ToString("")` или возвращает "G"-строку (в обоих случаях
  откат на дефолт). (3) `expandOrDefault` теперь параметризован предикатом валидности: ветка
  использует `isValidGitRef`, commit-шаблон — `anyText` (свободный текст, но по-прежнему защищён от
  исключения, unexpanded `{` и пустоты). Хостовые вызовы проверены: `OnEdit:518` (генерируемое имя
  ветки), `OnSuggestBranchName`→`WorkflowSeeds.SuggestedBranchName`→`branchNameForHost` и
  `OnSuggestVersionNote`→`SuggestedVersionNote`→`commitMessageForHost` больше не бросают
  `FormatException` и не отдают git-нелегальный ref в `BeginEdit` — молчаливого no-op/зависания
  webview на 30s больше нет. Регрессий для валидных паттернов нет (поведение неизменно); прежде
  «проходившие» битые паттерны (spaces/colons/`//`) теперь детерминированно деградируют к дефолту, что
  и есть цель S-09. Добавлены 4 теста (`{date:q}`, `{date:}`, прямые ref-нелегальные символы в
  branch-паттерне, и что commit-шаблон не подпадает под ref-проверку). `CHANGELOG.md`: запись только в
  `## [Unreleased]` → `### Fixed`, заголовок `## [0.1.0] - 2026-07-04` цел (прежний повторяющийся
  дефект не воспроизведён). Изменения `release.cmd`/`.gitignore` в рабочем дереве — только нормализация
  EOL, к T-008 не относятся и безвредны. Новых серьёзных проблем за 2 прогона не найдено. Можно
  переходить к следующей задаче.

### [SUMMARY-R-2026-07-05 14:18] Итог промежуточного ревью Этапов 1-3 — статус: готово к следующей задаче
- Активная задача: [T-009] Исправить S-13: ведущая пустая строка ломает выравнивание блок↔нода 1:1
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Область ревью: промежуточная — по «Плану выполнения» выполнены и отмечены `- [x]` Этапы 1-3
  (`md-blocks.ts`, `md-splice.ts` + их unit-тесты). Этапы 4-5 (`formatted.ts`/`index.ts` и
  CHANGELOG/`npm`-прогон) отмечены `- [ ]` — это ещё не сделанная работа, а не дефект, поэтому в
  область данного ревью не входят.
- Вывод: изменения Этапов 1-3 корректны и покрывают свои критерии готовности. (1) `md-blocks.ts`
  больше не делает `boundaries.unshift(0)` (лишний синтетический ведущий блок): при непустом
  наборе токенов первый реальный boundary СДВИГАЕТСЯ на 0 (`boundaries[0] = 0`, а его прежнее
  значение сохраняется в `firstContentStart`) — длина массива boundaries не меняется, поэтому
  `blocks.length` теперь строго равна числу top-level токенов (= `childCount`), а ведущий контент
  (пустые строки/ref-defs) едет вместе с первым блоком. Пустой/whitespace-only документ (нет ни
  одного токена) по-прежнему даёт один whole-document блок (`boundaries.push(0)`), где
  `contentLineStart` остаётся неустановленным. Ключи `contentEndByLine`/`childStartsByLine`
  (по реальной строке токена) корректно читаются через `contentKey = firstContentStart` для первого
  блока, так что `contentLineEnd`/`childLineStarts` не смещаются. Инвариант
  `joinBlocks(splitTopLevelBlocks(md)) === md` сохранён (boundaries остаются возрастающей
  партицией [0, lines.length)). (2) `md-splice.ts`: добавлен `headLines`, симметричный `tailLines`
  (`lines.slice(block.lineStart, block.contentLineStart)`), и переприсоединяется перед
  пере-сериализованной нодой при правке блока; на no-op пути первый блок сохраняется дословно
  через `lines.slice(lineStart, lineEnd+1)` (`lineStart=0` включает голову) — байт-идентичность
  восстановлена, whole-document fallback по `childCount !== blocks.length` для ведущего контента
  больше не срабатывает. Прослежены вручную оба целевых сценария (ведущая пустая строка и ведущий
  ref-def): no-op → байт-в-байт, правка первого блока → голова сохранена дословно. (3) Тесты:
  `topLevelTokenCount` использует независимый экземпляр `markdown-it` с тем же фильтром
  (`level===0 && map!==null`), что делает проверку `blocks.length === числу токенов` осмысленной, а не
  тавтологичной; фикстуры покрывают ведущие пустые строки, ведущий ref-def, единственный блок,
  whitespace-only (contentLineStart остаётся undefined) и RICH с двумя ведущими пустыми строками;
  md-splice-тесты трассированы вручную и дают ожидаемые строки. Существующие тесты (RICH и др. без
  ведущего контента) не затронуты: `firstContentStart` остаётся undefined, поведение неизменно.
  Регрессия в текущем (немодифицированном на этом этапе) `formatted.ts` не вносится — он индексирует
  `blocks[i]` параллельно детям ProseMirror (`blocks[$pos.index(0)]`, `Math.min(doc.childCount,
  blocks.length)`), и фикс восстанавливает 1:1-маппинг без компенсирующего off-by-one, который
  пришлось бы «раскомпенсировать». Замеченный узкий, НЕ-Critical край: `diff-marks.ts` ключует
  `childLineStarts` по `block.lineStart`, ставшему 0 для первого блока с ведущим контентом — если
  самый первый top-level блок является таблицей/списком И ему предшествует ведущий контент, per-child
  подсветка diff деградирует до заливки всего блока (graceful fallback по `starts === undefined`), без
  краха и потери данных; сценарий крайне узкий и вне заявленной области задачи. Новых серьёзных
  проблем за 2 прогона не найдено. Этапы 1-3 приняты; можно продолжать с Этапами 4-5.

### [SUMMARY-R-2026-07-05 14:29] Итог финального ревью задачи (все 5 этапов) — статус: готово к следующей задаче
- Активная задача: [T-009] Исправить S-13: ведущая пустая строка ломает выравнивание блок↔нода 1:1
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Область ревью: финальная — по «Плану выполнения» все 5 этапов отмечены `- [x]`. С момента
  промежуточного ревью (Этапы 1-3) исходники `md-blocks.ts`/`md-splice.ts` не менялись, добавлены
  только тесты Этапа 4 (`formatted.test.ts`) и запись Этапа 5 (`CHANGELOG.md`); `formatted.ts`/
  `index.ts` не потребовали правки — 1:1-маппинг восстанавливается автоматически (см. ниже).
- Вывод: все критерии готовности выполнены.
  1. Этап 4 (маппинг) закрыт БЕЗ изменений `formatted.ts`/`index.ts`: `sourceLineForPos` индексирует
     `this.blocks[$pos.index(0)]`, `blockGeometry`/`topVisibleSourceLine` — `this.blocks[i]` параллельно
     детям ProseMirror. Так как фикс `md-blocks.ts` сделал `blocks.length === doc.childCount`, все эти
     параллельные индексы теперь совпадают 1:1 без компенсирующего off-by-one. Новый тест
     «maps a source line to the correct block when the document has leading blank lines» подтверждает
     отсутствие смещения на один блок: активная подсветка ведущей пустой строки (line 0) → нода H1
     (ведущие пустые строки свёрнуты в первый блок), line 4 → параграф «para» (НЕ список — прежний
     off-by-one), line 6 → первый list-item «a». `getText()` неизменённого документа с ведущей пустой
     строкой / ведущим ref-def байт-идентичен исходнику (фикстуры в byte-identical-цикле) — переключение
     «Форматированный»→«Код» (`index.ts` applyMode строка 400 `formatted.getText()`) больше не
     переразбирает документ.
  2. Этап 5: запись добавлена только в `## [Unreleased]` → `### Fixed`; заголовок релиза
     `## [0.1.0] - 2026-07-04` цел (проверено: два и только два version-заголовка, строки 8 и 85) —
     повторяющийся у прошлых задач дефект «снос заголовка 0.1.0» не воспроизведён. Локально прогнано в
     `webview/`: `npm run typecheck` — чисто (tsc --noEmit), `npm run lint` — чисто (biome, 69 файлов,
     0 замечаний), `npm test` — 280 passed / 30 файлов, включая все новые фикстуры S-13
     (md-blocks: `blocks.length === topLevelTokenCount` для 4 фикстур, свёртка головы, whitespace-only
     держит `contentLineStart` undefined; md-splice: no-op байт-идентичность двух фикстур без
     whole-doc fallback + правка первого блока сохраняет голову дословно; formatted: byte-identity и
     маппинг без off-by-one).
  3. Все прочие потребители `splitTopLevelBlocks` проверены. `md-splice.ts` — симметричный `headLines`
     (`lines.slice(lineStart, contentLineStart)`) переприсоединяется перед пере-сериализованной нодой
     при правке; на no-op пути первый блок сохраняется дословно (`lineStart=0` включает голову).
     `formatted.ts` — 1:1 (см. выше). Замеченный ранее узкий НЕ-Critical край (`diff-marks.ts` ключует
     `childStartsByLine` по `block.lineStart`, ставшему 0; при этом `entry.lineStart` из нативного
     diff-пейлоада несёт РЕАЛЬНУЮ строку контейнера, поэтому для таблицы/списка, являющегося САМЫМ
     ПЕРВЫМ top-level блоком И имеющего ведущий контент, per-row/item подсветка review-оверлея
     деградирует до заливки всего блока через graceful fallback `starts === undefined`) остаётся
     косметическим: без краха, без потери данных, корректный блок, вне явных критериев задачи (те
     касаются active/hover/scroll-sync, а не гранулярности compare-оверлея) и вне заявленного набора
     затронутых файлов. Это не Critical/High, в отдельную R-запись не выносится.
- Новых серьёзных проблем за 2 прогона не найдено; открытых R-записей нет. Задача решена полностью —
  можно переходить к следующей задаче.

### [SUMMARY-F-2026-07-05 14:41] Итог интеграционного ревью — статус: готово к следующей задаче
- Проверено задач (по Tasks_Done.md): 9 (T-001…T-009 — исправления S-01…S-06, S-08, S-09, S-13)
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Область: инкремент с прошлого интеграционного ревью (SUMMARY-F 12:41 покрыл T-001…T-006) —
  фокус на трёх новых задачах T-007 (семантический diff), T-008 (`{date:FMT}`), T-009
  (ведущий контент в webview block-splice, изменения не закоммичены — в рабочем дереве) и их
  стыковке друг с другом и с ранее исправленным.
- Вывод: интеграционных проблем не найдено; девять исправлений согласованно сочетаются.
  1. Архитектурная целостность. T-007 расширил `Ast` DU четырьмя кейсами (инлайны
     `TaskListMarker`/`FootnoteRef`, блоки `DefinitionList`/`Footnotes`). ВСЕ потребители DU
     обрабатывают новые кейсы без «глотающего» catch-all: `Inlines.flatten` (общая для
     Markdown и Diff — чекбокс даёт `[x]`/`[ ]`, ссылка сноски — `[^label]`, поэтому
     переключение реально меняет текст), `AstDiff.kindTag`/`blockText`, а `DiffWire.childTexts`
     осознанно роняет `DefinitionList`/`Footnotes` в `| _ -> None` (whole-block Changed с
     base-text для word-diff). Сборка при warnings-as-errors чиста → все match'и исчерпывающи.
     Ключевая проверка на регрессию: `Projection.toAst` потребляется ТОЛЬКО `AstDiff.diffText`;
     карта строк (`Renderer.stampLineAnchors`) строится отдельным путём и намеренно пропускает
     сноски/ref-defs — поэтому добавление `Footnotes` в AST НЕ трогает scroll-sync/preview/
     comment-anchoring (инвариант из фикса 0.1.0 сохранён).
  2. Согласованность контрактов между слоями. Wire-значения diff (`added/removed/changed/moved`)
     не менялись: F# `DiffWire.DiffKind.all`, TS `DIFF_KINDS` и фикстура
     `webview/tests/contract/diff-kinds.json` совпадают (order-independent guard),
     `DiffKindContractTests` зелёный. Новые AST-кейсы — внутренние, границу C#↔F#↔TS не двигают.
  3. Регрессии между задачами отсутствуют. T-008 (`WorkflowConfig`): `docSlug` всегда
     слугифицирован (`WorkflowSeeds.DocSlug` → `Slug.slugify Kebab`), поэтому дефолтный паттерн
     `spec/{docSlug}-{date:yyyyMMdd}` всегда разворачивается и проходит `isValidGitRef` —
     «сырой» последний фолбэк с `{` на практике недостижим; валидные паттерны ведут себя как
     прежде, битые детерминированно деградируют к дефолту, «Редактировать»/suggest больше не
     молча зависают. T-009 восстановил `blocks.length === childCount` без компенсирующего
     off-by-one — потребители `formatted.ts` (`this.blocks[i]` параллельно детям PM,
     `this.blocks[$pos.index(0)]`, `Math.min(doc.childCount, blocks.length)`), `md-splice`
     (симметричные `headLines`/`tailLines`) и `applyMode` (no-op `getText()` байт-идентичен)
     теперь 1:1.
  4. Полнота относительно цели (README: автор правит спеки, не касаясь git; без потери данных;
     простой язык). Все девять фиксов служат этой цели и не противоречат ей. Порядковое
     ограничение соблюдено: T-009 (S-13) выполнен ДО ещё не начатых T-010/T-011 (S-11/S-12),
     как и требовала постановка.
  5. Сквозные аспекты в норме: новых пользовательских строк с git-лексикой нет; guard-стиль
     T-008 повторяет `ImageEngine.insertForHost`; CHANGELOG содержит ровно 9 записей под
     `## [Unreleased] → Fixed`, заголовок `## [0.1.0] - 2026-07-04` и его контент целы
     (прежний повторяющийся дефект «снос заголовка релиза» не воспроизведён).
  6. Технический долг: новых заглушек/TODO в изменённых областях нет (единственный TODO —
     пред-существующий `Pipeline.fs:11` PoC-3+, не из этой партии). Известный узкий НЕ-Critical
     край: `diff-marks.ts` ключует per-child подсветку по `block.lineStart` (ставшему 0 у
     первого блока с ведущим контентом), тогда как нативный `entry.lineStart` несёт реальную
     строку контейнера — если САМЫМ ПЕРВЫМ top-level блоком является таблица/список И перед ним
     есть ведущий контент, review-оверлей грациозно деградирует до заливки всего блока (без
     краха, без потери данных, корректный блок). Уже зафиксирован reviewer'ом как косметический
     и вне явных критериев T-009; в масштабе решения это НЕ Critical/High и, более того, чистое
     улучшение относительно полностью сломанного до-T-009 состояния.
- Проверка сборки/тестов на текущем состоянии (включая незакоммиченный T-009):
  `dotnet build SpecDesk.slnx` — 0 warnings (warnings-as-errors); `dotnet test` — 476 passed /
  0 failed / 1 skipped (ожидаемый real device-flow): Contracts 7, GitHub 115, Diff 27,
  Core 83, Markdown 66, Git 32, Host 146. Webview: `npm run typecheck` чисто, `npm run lint`
  чисто (69 файлов), `npm test` — 280 passed / 30 файлов.
- Открытых R-записей нет (все SUMMARY-R закрыты); открытых F-записей нет. Можно продолжать
  (следующие задачи очереди T-010… / чекпоинт с push).

### [SUMMARY-R-2026-07-05 15:10] Итог промежуточного ревью Этапов 1-3 — статус: готово к следующей задаче
- Активная задача: [T-010] Исправить S-11: CRLF-документы переписываются в LF при автосохранении
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Область ревью: промежуточная — по «Плану выполнения» выполнены и отмечены `- [x]` Этапы 1-3
  (хостовое определение/хранение/повторное применение стиля EOL + host-тест). Этапы 4-6
  (webview-фикстуры `md-blocks`/`md-splice`, запись в `CHANGELOG.md`) отмечены `- [ ]` — это ещё
  не сделанная работа, а не дефект, поэтому в область данного ревью не входят.
- Вывод: изменения Этапов 1-3 корректны и покрывают свои критерии готовности; ранее открытая R-004
  (`ApplyLineEnding` удваивал `\r` на сыром CRLF-тексте до первой правки, порождая `\r\r\n`) проверена
  и закрыта как корректно исправленная — удалена.
  1. Определение стиля. `DetectLineEnding(rawText)` считает пары `\r\n` против одиночных `\n` и отдаёт
     доминирующий стиль (равенство и «нет переводов строки» → дефолт `\n`); граничные случаи (пустая
     строка, ведущий одиночный `\n`, ничья, смешанный) прослежены и покрыты параметризованным тестом
     `DetectLineEnding_PicksTheDominantStyle`. Заполняется из СЫРОГО содержимого в `LoadFile` (:1847,
     до отправки в webview) и переопределяется из перечитанного файла в `OnDiscard` (:651); поле
     `_lineEnding` дефолтит `"\n"` для нового/безпереводного документа (задокументировано).
  2. Хранение как отдельное состояние. `_lineEnding` — самостоятельное поле, НЕ выводимое из `_text`;
     оно намеренно не обновляется в `OnEditorChanged` (где `_text` становится LF из webview), сохраняя
     исходный он-диск стиль. Публикуется согласованным набором вместе с `_text`/`_currentPath` под
     `lock(_sync)` в обоих сайтах присвоения (`LoadFile`, `OnDiscard`).
  3. Повторное применение при записи. Все три сайта записи — `OnSave` (:481), `RunDiskAutosave` (:777),
     `OnSaveVersion` (:843) — пишут `ApplyLineEnding(text, lineEnding)`, где `lineEnding` снят под тем же
     `lock(_sync)`-снимком, что `text`/`path` (для автосейва — синхронно с захватом `generation`), так
     что торн-пары «текст одного документа + EOL другого» не возникает. `ApplyLineEnding` защитно
     нормализует вход к LF перед применением стиля (идемпотентно и для сырого CRLF из окна «до первого
     editorChanged», и для LF-текста webview) — фикс R-004.
  4. Round-trip. CRLF-документ → «Редактировать» → правка → автосейв/OnSave/OnSaveVersion сохраняет CRLF
     на неизменённых строках; покрыто тестами `RunDiskAutosave_PreservesCrlfWhenOneLineIsEdited`,
     `OnSave_PreservesCrlf`, `OnSaveVersion_PreservesCrlf`, LF-инвариант (`LfDocument_StaysLfAfterAnEdit`),
     переопределение стиля после Discard (`Discard_ThenAFreshEdit_RedetectsCrlfFromTheRevertedFile`) и
     двумя R-004-регресс-тестами на no-op запись без единого EditorChanged. `OnReady`→`LoadFile` наполняет
     `_text`/`_lineEnding` сырым содержимым, поэтому эти тесты реально проверяют pre-edit окно.
- Замеченные НЕ-Critical/High края (в отдельную R-запись не выносятся, вне явных критериев Этапов 1-3):
  файл со смешанными EOL при no-op-записи до первой правки уни­фицируется к доминирующему стилю (потеря
  байт-идентичности только для редких mixed-EOL файлов; после первой правки уни­фикация неизбежна — webview
  схлопывает все переводы строк в LF на своей границе); CR-only (старый Mac) вход конвертируется в LF;
  `text.Length` в лог-сообщениях считает LF-длину, а не записанную. Ни один не даёт порчи данных.
- Новых серьёзных проблем за 2 прогона не найдено; открытых R-записей нет. Этапы 1-3 приняты — можно
  продолжать с Этапами 4-6.

### [SUMMARY-R-2026-07-05 15:25] Итог финального ревью задачи (все 6 этапов) — статус: готово к следующей задаче
- Активная задача: [T-010] Исправить S-11: CRLF-документы переписываются в LF при автосохранении
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Область ревью: финальная — по «Плану выполнения» все 6 этапов отмечены `- [x]`. С момента
  промежуточного ревью (Этапы 1-3, SUMMARY-R 15:10) хостовые исходники не менялись; добавлены
  webview-фикстуры Этапов 4-5 (`md-blocks.test.ts`, `md-splice.test.ts`) и запись Этапа 6
  (`CHANGELOG.md`). Открытых R-записей на входе не было (R-004 закрыта ранее).
- Вывод: все критерии готовности выполнены.
  1. Хостовая часть (перепроверена целиком). `DetectLineEnding` считает пары `\r\n` против одиночных
     `\n` и отдаёт доминирующий стиль (ничья и «нет переводов строки» → дефолт `\n`). Заполняется из
     СЫРОГО содержимого в `LoadFile` (:1847, до отправки в webview) и переопределяется из перечитанного
     файла в `OnDiscard` (:651). `_lineEnding` — самостоятельное поле (дефолт `\n`), НЕ выводимое из
     `_text` и намеренно НЕ трогаемое в `OnEditorChanged` (:378-382, там `_text` становится LF из
     webview) — исходный он-диск стиль сохраняется. Все пять точек доступа к полю согласованы: записи
     (:651, :1847) и чтения-снимки (`OnSave` :471, `RunDiskAutosave` :743, `OnSaveVersion` :825) идут
     строго под `lock(_sync)` одним набором с `text`/`path`/`_textGeneration`, так что торн-пары «текст
     одного документа + EOL другого» невозможны. Все три сайта записи пишут
     `ApplyLineEnding(text, lineEnding)`.
  2. Фикс R-004 (удвоение `\r`) на месте и защищён тестами: `ApplyLineEnding` безусловно нормализует
     вход к bare `\n` (`Replace("\r\n","\n").Replace("\r","\n")`) перед применением стиля, поэтому и
     сырой CRLF из окна «до первого EditorChanged», и LF-текст webview дают корректный результат без
     `\r\r\n`; идемпотентно. Регресс-тесты `OnSave_BeforeAnyEditorChanged_DoesNotDoubleUpCr` и
     `OnSaveVersion_BeforeAnyEditorChanged_DoesNotDoubleUpCr` проверяют no-op запись без единого
     EditorChanged.
  3. Round-trip и дефолты покрыты host-тестами (`HostControllerLineEndingTests`, 14 прошли):
     CRLF-round-trip через автосейв/OnSave/OnSaveVersion, LF-инвариант, переопределение стиля после
     Discard, параметризованный `DetectLineEnding` (пусто/без-переводов/LF/CRLF/смешанный/ничья →
     дефолт). Тесты драйвят `RunDiskAutosave` детерминированно (таймер 10 мин), как
     `HostControllerAutosaveRaceTests`.
  4. Webview-фикстуры Этапов 4-5 корректны и осмысленны. `md-blocks`: `joinBlocks(splitTopLevelBlocks)`
     байт-идентичен для CRLF-входа (сплит по `\n`, `\r` едет как хвост строки), `childLineStarts`
     ключуются по номерам строк markdown-it (внутренне LF-нормализованный разбор) и не смещаются на
     CRLF. `md-splice`: no-op CRLF-документ байт-идентичен (без whole-doc fallback), неизменённые блоки
     сохраняют `\r` дословно; шов переэмитированного ИЗМЕНЁННОГО блока (LF из общего ProseMirror-
     сериализатора) явно зафиксирован тестом как ожидаемый — в проде он moot, т.к. на реальном пути
     сохранения весь текст проходит через CodeMirror (LF) и `ApplyLineEnding` один раз в конце.
  5. Этап 6: запись добавлена только в `## [Unreleased]` → `### Fixed`; заголовок релиза
     `## [0.1.0] - 2026-07-04` цел — повторяющийся у прошлых задач дефект «снос заголовка релиза» не
     воспроизведён.
- Проверка сборки/тестов на текущем состоянии: `dotnet build`/`dotnet test --filter
  HostControllerLineEndingTests` — 14 passed / 0 failed, сборка чистая при warnings-as-errors; webview
  (`webview/`): `npm run typecheck` чисто, `npm run lint` чисто (69 файлов), `npm test` — 288 passed /
  30 файлов (включая новые CRLF-фикстуры md-blocks/md-splice).
- Замеченные НЕ-Critical/High края (в отдельную R-запись не выносятся, порчи данных не дают, вне явных
  критериев): mixed-EOL файл при no-op-записи до первой правки уни­фицируется к доминирующему стилю;
  CR-only (старый Mac) вход конвертируется в LF; `text.Length` в лог-сообщениях считает LF-длину, а не
  записанную. Все уже отмечены в промежуточном ревью.
- Новых серьёзных проблем за 2 прогона не найдено; открытых R-записей нет. Задача решена полностью —
  можно переходить к следующей задаче.

### [SUMMARY-R-2026-07-05 15:39] Итог ревью задачи — статус: готово к следующей задаче
- Активная задача: [T-011] Исправить S-12: откатной полный ресериализатор удаляет link-reference-definitions
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Область ревью: изменение в рабочей копии jj (`webview/src/md-splice.ts`, `webview/tests/md-splice.test.ts`,
  `CHANGELOG.md`, плюс служебный `.work/status.md`). Открытых R-записей на входе не было (R-001…R-004
  закрыты ранее), исправленных/отклонённых записей для перепроверки на Фазе 1 не было.
- Вывод: все критерии готовности выполнены.
  1. Откатной путь сохраняет reference-definitions дословно. Оба fallback-возврата
     `serializeWithSplice` (originalDoc === null и рассинхрон `childCount`) теперь обёрнуты в
     `withPreservedNonNodeContent(original, serializer.serialize(edited))`, который через `nonNodeLines`
     собирает каждую строку исходника, не покрытую ни одним ProseMirror-узлом, отбрасывает пустые
     (`trim().length === 0`) и до-присоединяет остаток (реально — только `[id]: url`) отдельной хвостовой
     секцией через пустую строку-разделитель (обязательную, чтобы def не слился с последним параграфом
     как lazy continuation). Ref-def переносится в конец файла — один из двух разрешённых задачей путей.
  2. `nonNodeLines` корректно вычисляет узловые интервалы `[contentStart, contentEnd)` из
     `splitTopLevelBlocks`: `contentStart = contentLineStart ?? lineStart`,
     `contentEnd = contentLineEnd ?? contentStart`. Ложных срабатываний нет — строки внутри
     fence/indented-code/html-блоков покрыты картой своего токена (`token.map` покрывает весь спан),
     поэтому `[a]: url`-подобная строка внутри код-блока НЕ выносится в хвост и не дублируется; блоки
     образуют непрерывную партицию `[0, lines.length)`, каждая строка рассматривается ровно раз, так что
     ни одна узловая строка не классифицируется как non-node (дублирования содержимого нет). Вырожденный
     случай «один ref-def и ничего больше» (нет ни одного top-level токена → `contentLineEnd` не задан →
     покрытие пустое) обрабатывается: весь текст считается non-node и сохраняется.
  3. Нажатие Enter для нового абзаца больше не теряет ref-def — покрыто acceptance-тестом
     (`children.splice(1,0,paragraph(...))` → fallback по `childCount` mismatch → def сохранён).
  4. Тесты осмысленны и падали бы без фикса (проверено вручную по каждому: add-block, remove-block,
     press-Enter, degenerate single-def — во всех первый `toContain("[a]: http://x")` без фикса не
     проходит). Существующие whole-doc-fallback-фикстуры не затронуты: без non-node non-blank контента
     `withPreservedNonNodeContent` возвращает `serialized` байт-в-байт (ранняя ветка `preserved.length===0`).
  5. Регрессий в основном (1:1) splice-пути нет — `withPreservedNonNodeContent` вызывается ТОЛЬКО на
     двух fallback-возвратах; `headLines`/`tailLines` не тронуты. Единственный потребитель
     `serializeWithSplice` (`formatted.ts:582 getText()`) работает на LF-нормализованном `this.original`
     (webview-граница), а хост при записи всё равно прогоняет текст через `ApplyLineEnding` (T-010), так
     что CRLF/EOL-порчи не возникает.
  6. `CHANGELOG.md`: запись добавлена только в `## [Unreleased]` → `### Fixed`; заголовок релиза
     `## [0.1.0] - 2026-07-04` цел — повторяющийся у прошлых задач дефект «снос заголовка релиза» не
     воспроизведён.
- Проверка качества (локально, `webview/`): `npm run typecheck` чисто (tsc --noEmit), `npm run lint`
  чисто (biome, 69 файлов), `npm test` — 292 passed / 30 файлов (288 прежних + 4 новых S-12).
- Замеченный НЕ-Critical/High край (в отдельную R-запись не выносится, вне явных критериев задачи):
  на fallback-пути body-ссылки reference-стиля сериализуются ProseMirror'ом как inline `[text](url)`, а
  перенесённый в конец `[id]: url` становится «осиротевшим» неиспользуемым определением — при этом URL
  сохранён и в inline-ссылке, и в def-строке, потери данных нет, а критерий «сохранить `[id]: url`
  дословно» выполнен. Это улучшение относительно полной потери до фикса, а не дефект.
- Новых серьёзных проблем за 2 прогона не найдено; открытых R-записей нет. Задача решена полностью —
  можно переходить к следующей задаче.

### [SUMMARY-R-2026-07-05 15:52] Итог ревью задачи — статус: готово к следующей задаче
- Активная задача: [T-012] Исправить S-14: правка текста ячейки таблицы стирает выравнивание колонок
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Область ревью: `.work/Active_Task.md` в рабочем дереве пуст (коммитом @ 2640c30b удалён прежний
  T-011-контент, а T-012 в него не дописан), поэтому требования взяты из `.work/Tasks_Queue.md`
  ([T-012], тот же дословный текст, что копируется в Active_Task.md). Изменение T-012 полностью
  локализовано в `webview/src/pm-markdown.ts` + `webview/tests/pm-markdown.test.ts` + вторая запись в
  `CHANGELOG.md` (`## [Unreleased]` → таблица/align). Прочие изменённые исходники рабочего дерева
  (`webview/src/md-splice.ts`, `webview/tests/md-splice.test.ts`, первая CHANGELOG-запись) — это уже
  принятый T-011 (SUMMARY-R 15:39), сложённый в тот же jj-change; T-012 их не трогал. Открытых
  R-записей на входе не было (R-001…R-004 закрыты ранее), перепроверять на Фазе 1 нечего.
- Вывод: все критерии готовности выполнены.
  1. Выравнивание перенесено через схему как атрибут ячейки: `table_cell` получил `align`
     (`default: null`), `parseDOM` (td/th) читает его из `style="text-align:…"` через `alignFromStyle`,
     `toDOM` пишет `text-align:<align>` только когда align задан. Токенный путь парсера (`th`/`td`
     rules) тоже извлекает align из `tok.attrGet("style")` — согласуется с тем, что markdown-it
     штампует `text-align:left|right|center` на КАЖДУЮ ячейку выровненной колонки (и `th`, и `td`,
     а не только заголовок), поэтому сериализатор корректно читает align из ячеек строки-заголовка.
  2. Сериализатор воспроизводит исходные маркеры: `alignMarker` — точная инверсия `alignFromStyle`
     (`left→:---`, `right→---:`, `center→:---:`, иначе `---`). Для невыровненной таблицы `align=null`
     → `---`, т.е. вывод байт-в-байт совпадает с прежним `cells.map(()=>"---")` — регрессии для
     невыровненных таблиц нет. Число колонок сепаратора = числу ячеек строки-заголовка (тот же
     `row.forEach`), маркеры — валидный GFM.
  3. Round-trip при правке текста ячейки сохраняет выравнивание: инлайн-правка ячейки в ProseMirror
     не трогает node-атрибут `align`, поэтому при последующей сериализации (в т.ч. через block-splice
     или whole-doc fallback того же `serializer`) align сохраняется. Regex `text-align:\s*(left|right|
     center)` покрывает формат markdown-it (без/с пробелом); `attrGet`/`getAttribute` вернувшие null
     обработаны (`style ?? ""`).
  4. Тесты добавлены и осмысленны: round-trip выровненной таблицы (`:--- / ---: / :---: / ---`
     ↔ `md.trimEnd()`) и проверка, что header- и body-ячейки одной колонки получают один align
     (right у первой колонки, null у второй). Прежде тестировались только невыровненные таблицы.
  5. `CHANGELOG.md`: запись добавлена только в `## [Unreleased]`; заголовок релиза
     `## [0.1.0] - 2026-07-04` цел — повторяющийся у прошлых задач дефект «снос заголовка релиза» не
     воспроизведён.
- Безопасность: инъекции нет — `align` принимает только {left,right,center,null} (единственные
  источники — `alignFromStyle`), а ProseMirror DOMSerializer пишет стиль через setAttribute, не
  innerHTML; сериализуемые маркеры — фиксированный набор.
- Замеченный НЕ-Critical/High край (в отдельную R-запись не выносится, вне явных критериев задачи):
  `alignFromStyle` регистрозависим, поэтому HTML-вставка извне с `text-align: Right`/`TEXT-ALIGN`
  дала бы `align=null` — не относится к markdown-round-trip'у (markdown-it всегда отдаёт lowercase),
  порчи данных нет.
- Процессные замечания (НЕ дефекты кода, R-запись не создаётся): (а) `.work/Active_Task.md` пуст —
  требования пришлось восстанавливать из `Tasks_Queue.md`; (б) изменение T-012 сложено в jj-change
  2640c30b, который по-прежнему описан как T-011, — при пуше T-011+T-012 схлопнутся в один коммит с
  описанием только T-011. Обе относятся к ведению рабочих файлов/VCS-гигиене, не к корректности кода.
- Новых серьёзных проблем за 2 прогона не найдено; открытых R-записей нет. Задача решена полностью —
  можно переходить к следующей задаче.

### [SUMMARY-F-2026-07-05 16:10] Итог интеграционного ревью — статус: готово к следующей задаче
- Проверено задач (по Tasks_Done.md): 12 (T-001…T-012 — исправления S-01…S-06, S-08, S-09, S-11…S-14)
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Область: инкремент с прошлого интеграционного ревью (SUMMARY-F 14:41 покрыл T-001…T-009) —
  фокус на трёх новых задачах T-010 (S-11, CRLF на хосте + webview-фикстуры), T-011 (S-12,
  сохранение reference-definitions на fallback-пути md-splice), T-012 (S-14, перенос выравнивания
  колонок таблицы через схему; изменения в рабочем дереве = jj-change @ d38a07c2) и их стыковке
  друг с другом и с ранее исправленным. VCS: git (colocated с jj); T-012 сидит в рабочей копии @
  как отдельный jj-change с собственным описанием — процессное замечание reviewer'а о «схлопывании
  T-011+T-012 в один коммит» на текущем состоянии не воспроизводится (у @ своё описание про align).
- Вывод: интеграционных проблем не найдено; двенадцать исправлений согласованно сочетаются.
  1. Архитектурная целостность / webview-конвейер сериализации. T-009/T-010/T-011/T-012 все трогают
     block-splice, и вместе они когерентны: `md-blocks.ts` держит `blocks.length === childCount`
     (свёртка ведущего контента в первый блок), поэтому потребитель `formatted.ts` индексирует
     `this.blocks[i]` параллельно детям ProseMirror 1:1 без компенсирующего off-by-one
     (`this.blocks[$pos.index(0)]`, `Math.min(doc.childCount, this.blocks.length)`). `md-splice.ts`
     сохраняет non-node-контент (ref-defs) и на 1:1-пути (`headLines`/`tailLines` per-block), и на
     whole-doc fallback (`withPreservedNonNodeContent`/`nonNodeLines`) — обе ветки берут одну и ту же
     непрерывную партицию `[contentStart, contentEnd)` из `splitTopLevelBlocks`, поэтому каждая
     строка рассматривается ровно раз (ни двойного учёта ref-def, ни потери), вырожденный «один
     ref-def без токенов» покрыт. `getText()` диффит текущий PM-док против ФИКСИРОВАННОГО
     `this.original`, поэтому повторный fallback переносит ref-def в конец стабильно и без накопления.
     T-012 (`pm-markdown.ts`) добавил `align` как атрибут `table_cell` — оба пути сериализации
     (`serializeBlock` при 1:1 и `serializer.serialize(edited)` внутри fallback T-011) воспроизводят
     маркеры выравнивания; невыровненная таблица (`align=null` → `---`) байт-идентична прежнему выводу
     (нет регрессии), а `edited.child(i).eq(originalDoc.child(i))` для нетронутой таблицы по-прежнему
     истинно (оба разбора несут одинаковые align-атрибуты). Дублирования логики между задачами нет.
  2. Host-слой: T-010 (EOL) чисто сосуществует с T-005 (токен поколения) и T-006 (атомарный
     discard-гейт). `_lineEnding` — самостоятельное `_sync`-защищённое поле, снимаемое ОДНИМ набором
     с `_text`/`_currentPath`/`_textGeneration` во всех трёх сайтах записи (`OnSave` :468-473,
     `RunDiskAutosave` :732-748, `OnSaveVersion` :812-826), поэтому торн-пары «текст одного документа
     + EOL другого» невозможны; `ApplyLineEnding` нормализует вход к bare-LF безусловно (идемпотентно,
     фикс R-004). В `OnDiscard` три задачи сходятся без конфликта: под `_repoGate` идут
     Discard→бамп `_draftGeneration`→re-read (:632-639), затем ОТДЕЛЬНЫМ `lock(_sync)` — `_state`/
     `_text`/`_lineEnding = DetectLineEnding(reverted)`/`_textGeneration` (:641-656); блоки
     последовательны, не вложены — правило «`_sync` не берётся под `_repoGate`» соблюдено. Атомарный
     T-006-гейт (tryStep + `_publishInFlight` + снимок полей под одним `_sync`, :595-616) не задет
     правками T-010.
  3. Регрессии между задачами / git-слой. T-007…T-012 НЕ трогали `SpecDesk.Git` (подтверждено
     diff-stat 1d349ec^..d38a07c2): три ранее принятых фикса (`DirtyWorkingTreeException` :115,
     `ResolveCredentials`+`OnPushStatusError`+`ThrowIfRejected`, отсутствие живого `DefaultCredentials`)
     на месте и не регрессировали. F#-diff (T-007: расширенный `Ast` DU + исчерпывающие `Projection`/
     `AstDiff`) и `WorkflowConfig` (T-008) с прошлого интеграционного ревью не менялись; сборка при
     warnings-as-errors чиста → все match'и по-прежнему исчерпывающи.
  4. Согласованность контрактов. T-010/T-011/T-012 — целиком внутренние для своих слоёв (host-поле,
     webview-сериализатор, webview-схема); границу C#↔F#↔TS (IPC-envelope, DiffKind-контракт) не
     двигают. `DiffKindContractTests` и весь набор зелёные.
  5. Полнота относительно цели (README: автор правит спеки не касаясь git; без потери данных; простой
     язык) и относительно списка задач. Порядковое ограничение соблюдено: T-009 (S-13) сделан ДО
     T-010/T-011 (S-11/S-12). Из 14 Serious-находок код-ревью закрыты 12; оставшиеся S-15/S-16 и ВСЕ
     Minor/Structure-находки (M-01…M-36, T-01…T-14) заведены в `Tasks_Queue.md` как T-013…T-060 —
     ничего не потеряно молча. Новых пользовательских строк с git-лексикой нет.
  6. Сквозные аспекты / технический долг. CHANGELOG содержит ровно 12 записей под
     `## [Unreleased] → Fixed`, заголовок `## [0.1.0] - 2026-07-04` и его контент целы (повторяющийся
     у прошлых задач дефект «снос заголовка релиза» не воспроизведён). Новых заглушек/TODO в изменённых
     областях нет (единственные TODO — пред-существующие `Pipeline.fs:11` PoC-3+ и `SpecDesk.Ai` PoC-8).
     Известный узкий НЕ-Critical край: `md-blocks.ts` (default preset) и `pm-markdown.ts` (commonmark
     + table + strikethrough, maxNesting 100 vs 20) могут разойтись при вложенности >20 — это
     пред-существующая Structure-находка T-12, заведённая как T-059; T-012 её не расширял (тронул схему/
     парсер/сериализатор, не токенайзер). Процессные заметки (пустой `.work/Active_Task.md`; EOL-only
     изменения `release.cmd`/`.gitignore` в рабочем дереве) уже зафиксированы reviewer'ом как
     VCS-гигиена, не дефекты кода — в масштабе решения не Critical/High.
- Проверка сборки/тестов на текущем состоянии (включая незакоммиченный T-012):
  `dotnet build SpecDesk.slnx` — 0 warnings (warnings-as-errors); `dotnet test SpecDesk.slnx` —
  490 passed / 0 failed / 1 skipped (ожидаемый real device-flow): Contracts 7, Core 83, Markdown 66,
  Diff 27, GitHub 115, Git 32, Host 160. Webview (`webview/`): `npm run typecheck` чисто,
  `npm run lint` чисто (69 файлов), `npm test` — 295 passed / 30 файлов.
- Открытых R-записей нет (все SUMMARY-R закрыты); открытых F-записей нет. Можно продолжать
  (следующие задачи очереди T-013… / чекпоинт с push).

### [SUMMARY-R-2026-07-05 16:25] Итог ревью задачи — статус: готово к следующей задаче
- Активная задача: [T-013] Исправить S-15: команда форматирования блока падает при каретке в позиции 0
  документа, начинающегося с перевода строки
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Область ревью: изменение в рабочей копии (`webview/src/md-format.ts`, `webview/tests/md-format.test.ts`,
  `webview/tests/editor.test.ts`, `CHANGELOG.md`, плюс служебные `.work/`). Открытых R-записей на входе
  не было (R-001…R-004 закрыты в прошлых задачах); исправленных/отклонённых записей для перепроверки на
  Фазе 1 не было.
- Вывод: все критерии готовности выполнены.
  1. Корень дефекта устранён точечно. В `toggleBlock` (`md-format.ts:120`) вычисление начала строки
     заменено на `const blockStart = from === 0 ? 0 : doc.lastIndexOf("\n", from - 1) + 1;`. Прежний
     идиом `lastIndexOf("\n", from-1)` при `from===0` из-за платформенного клэмпа `fromIndex=-1`→`0`
     ошибочно матчил САМ ведущий `\n` (`doc[0]`) и возвращал `0`, давая `blockStart=1` при `blockEnd=0`
     — инвертированный диапазон `{from:1,to:0}`, на котором `view.dispatch` бросал
     `RangeError: Invalid change range 1 to 0`. Теперь при каретке в позиции 0 `blockStart=0` всегда,
     `blockStart <= blockEnd` гарантированно.
  2. Регрессий нет. Для `from===0`, где `doc[0] !== "\n"`, прежний код и так давал `blockStart=0`
     (`lastIndexOf` возвращал `-1`), поэтому новая ветка поведение не меняет; для `from>0`
     `lastIndexOf("\n", from-1) < from`, инверсия невозможна — единственный сломанный случай был
     `from===0 && doc[0]==="\n"`, и он закрыт. Пустой документ (`from===0`, `blockEnd=0`) даёт валидный
     no-op-диапазон, как и раньше. Единственный источник-сайд потребитель `formatMarkdown`/`toggleBlock`
     — `editor.applyFormat` (`editor.ts:499`); одноимённый `toggleBlock` в `pm-commands.ts` — отдельный
     ProseMirror-путь формативного вида, к правке не относится.
  3. Тесты покрывают все критерии для всех 6 блочных команд тулбара (h1, h2, bullet, ordered, quote,
     code). `md-format.test.ts` — прямой pure-вызов `formatMarkdown("\n# Title\n\npara\n", 0, 0, cmd)`:
     «не бросает» + «диапазон не инвертирован» (`edit.from <= edit.to`) — падал бы без фикса
     (`from:1,to:0`). `editor.test.ts` — реальный CodeMirror-путь: `setText` документа с ведущей пустой
     строкой, `applyFormat(cmd)` не бросает `RangeError` из `view.dispatch`. Прогнано локально:
     `npx vitest run tests/md-format.test.ts tests/editor.test.ts` — 40 passed / 0 failed.
  4. `CHANGELOG.md`: запись добавлена только в `## [Unreleased]` → `### Fixed`; заголовок релиза
     `## [0.1.0] - 2026-07-04` цел (ровно два version-заголовка, строки 8 и 115) — повторяющийся у
     прошлых задач дефект «снос заголовка релиза» не воспроизведён.
- Новых серьёзных проблем за 2 прогона не найдено; открытых R-записей нет. Задача решена полностью —
  можно переходить к следующей задаче.

### [SUMMARY-R-2026-07-05 16:38] Итог ревью задачи — статус: готово к следующей задаче
- Активная задача: [T-014] Исправить S-16: одна вставка Ctrl+V может вставить содержимое дважды
  (текст + картинка)
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Область ревью: изменение в рабочей копии (`webview/src/image-capture.ts`,
  `webview/tests/image-capture.test.ts`, `CHANGELOG.md`, плюс служебный `.work/status.md`). Открытых
  R-записей на входе не было (R-001…R-004 закрыты в прошлых задачах); исправленных/отклонённых записей
  для перепроверки на Фазе 1 не было.
- Вывод: все критерии готовности выполнены.
  1. Первопричина устранена по первому из двух разрешённых задачей путей. В paste-обработчике
     `attachImageCapture` (`image-capture.ts:64-67`) ДО чтения `clipboardData.items` добавлен guard:
     `const text = event.clipboardData?.getData("text/plain") ?? ""; if (text.length > 0) return;`.
     Собственный default-paste CodeMirror зарегистрирован на том же `contentDOM` раньше (при
     конструировании EditorView, до `attachImageCapture`), поэтому для буфера с непустым `text/plain`
     он вставляет текст и делает `preventDefault()` первым; порядок регистрации не мешает этому
     листенеру всё равно отработать, поэтому guard необходим — без него был бы вставлен ещё и
     image-link. Теперь `image-capture` детерминированно уступает уже вставленному CodeMirror'ом
     представлению.
  2. «Ровно одно представление» гарантировано во всех комбинациях: text+image → только текст (guard
     отдаёт вставку CodeMirror, `image.paste` не генерируется, собственный `preventDefault` не
     вызывается — драки с CodeMirror нет); только image (скриншот/Print Screen/копия из редактора,
     где `text/plain` пуст) → guard пропускает, картинка захватывается как прежде; только текст →
     guard уступает, картинки и нет. Регрессии основного «вставить скриншот» нет — чистый image-paste
     не несёт `text/plain`. Явный дизайн-трейдофф (image+HTML из браузера с непустым `text/plain`
     вставит текст/URL, а не картинку) — ровно тот путь, который задача разрешает первым критерием;
     не дефект.
  3. Потери данных/крашей нет: `clipboardData` может быть null — обработано (`?.` + `?? ""`, затем
     `!items`-return); guard стоит перед доступом к `items`, поэтому null-случай безопасен. Текст при
     наличии всегда вставляется CodeMirror'ом (никакого «ничего не вставилось»). Новых гонок нет —
     синхронный ранний возврат, асинхронный `emit` не тронут. Drop-путь оставлен без guard осознанно:
     S-16 про Ctrl+V, а не про drag-drop; расширение guard на drop было бы выходом за рамки задачи.
     Буфер с `text/plain` из одних пробелов трактуется как непустой (`length > 0`) — соответствует
     дословному критерию «непустой формат `text/plain`».
  4. Тесты покрывают проводку paste/drop (прежде — только `stripDataUrlPrefix`), окружение
     `// @vitest-environment jsdom` (jsdom ^29 в devDependencies). Проверены: захват image при пустом
     тексте (`onImage` вызван 1 раз, `pos=selectionHead()`, `preventDefault`); пропуск image при
     непустом тексте — S-16-репро (`onImage` НЕ вызван, `defaultPrevented === false`); отсутствие
     файлов без throw; захват dropped-image по координатам drop (`pos=posAtCoords`). Fake-editor
     зеркалит реальные члены `MarkdownEditor` (`contentDOM`/`selectionHead`/`posAtCoords`), fake
     `DataTransferItemList` — array-like/не-iterable, как в Chromium/WebView2 (совпадает с индексным
     обходом в коде). vitest ^4.1.9 поддерживает `vi.fn<T>()` и директиву окружения.
  5. `CHANGELOG.md`: запись добавлена только в `## [Unreleased]` → `### Fixed`; ровно два
     version-заголовка (`## [Unreleased]` строка 8, `## [0.1.0] - 2026-07-04` строка 121) —
     повторяющийся у прошлых задач дефект «снос заголовка релиза» не воспроизведён.
- Процессное замечание (НЕ дефект кода, R-запись не создаётся): изменения `release.cmd` в рабочем
  дереве — симметричная EOL-нормализация (21/21 строк), к T-014 не относятся и безвредны (уже
  отмечались в прошлых ревью).
- Новых серьёзных проблем за 2 прогона не найдено; открытых R-записей нет. Задача решена полностью —
  можно переходить к следующей задаче.

### [SUMMARY-R-2026-07-05 16:58] Итог ревью задачи — статус: готово к следующей задаче
- Активная задача: [T-015] Исправить M-01: нативный парсер без UseEmphasisExtras — зачёркивание
  парсится как литерал
- Прогонов выполнено: 2
- Открытых проблем: 0
- Вывод: все критерии готовности выполнены.
  1. `Pipeline.fs` включает `.UseEmphasisExtras(EmphasisExtraOptions.Strikethrough)` — точечно только
     зачёркивание, без остального extras-набора (sub/superscript/inserted/marked), что согласуется с
     тем, что тулбар вебвью эмитит именно `~~text~~` (`pm-markdown.ts`).
  2. `Projection.fs`: `~~struck~~` — это тоже `EmphasisInline`, отличается только `DelimiterChar = '~'`;
     проверка `DelimiterChar = '~'` стоит ПЕРЕД разбором по `DelimiterCount`, поэтому зачёркивание
     (count=2) не конфлатится с `Strong` (`**bold**`, count=2). Одиночный/двойной `~` оба уходят в
     `Strikethrough` — консистентно.
  3. `Ast.fs`: добавлен отдельный кейс `Strikethrough of Inline list` — переключение зачёркивания
     теперь настоящая семантическая правка, а не мнимый bold-toggle.
  4. `Inlines.fs`: `flatten` рекурсирует в детей `Strikethrough` БЕЗ маркеров (`~~`), поэтому нативный
     flatten («struck») совпадает с ProseMirror `textContent` («struck») → фантомная word-diff-правка
     на `~~`-слове устранена.
  5. Нативное превью рендерит `<del>struck</del>` (Markdig, через `Pipeline.shared` в `Renderer.fs`),
     буквальные тильды больше не показываются.
  6. Тесты добавлены на все три уровня: проекция (`ProjectionTests`: отдельный кейс, не `Strong`),
     flatten без маркеров (`InlinesTests`), нативный рендер `<del>` без `~~` (`RendererTests`).
  7. Запись в `CHANGELOG.md` — только в `## [Unreleased]` → `### Fixed`.
- Проверена исчерпываемость: единственные F#-потребители кейсов `Ast.Inline` — `Inlines.flatten` и
  `Projection.inlineOf`, оба обновлены; иных сопоставлений по `Inline`, которые сломались бы новым
  кейсом при warnings-as-errors, в `src/` и `tests/` нет.
- Новых серьёзных проблем за 2 прогона не найдено; открытых R-записей нет. Можно переходить к
  следующей задаче.

### [SUMMARY-F-2026-07-05 17:08] Итог интеграционного ревью — статус: готово к следующей задаче
- Проверено задач (по Tasks_Done.md): 15 (T-001…T-015 — S-01…S-06, S-08, S-09, S-11…S-16, M-01)
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Область: инкремент с прошлого интеграционного ревью (SUMMARY-F 16:10 покрыл T-001…T-012) —
  фокус на трёх новых задачах T-013 (S-15, каретка в позиции 0 в `md-format.ts`), T-014 (S-16,
  paste-guard в `image-capture.ts`) и T-015 (M-01, нативное зачёркивание в `SpecDesk.Markdown`;
  изменения T-015 не закоммичены — в рабочем дереве @). VCS: git (colocated с jj). T-013/T-014
  закоммичены (fb0672f, d1477bd); T-015 сидит в рабочей копии (правки только в `SpecDesk.Markdown`
  + тесты + CHANGELOG).
- Вывод: интеграционных проблем не найдено; пятнадцать исправлений согласованно сочетаются.
  1. Архитектурная целостность (T-015 / расширение AST DU). `Ast.Inline` получил кейс
     `Strikethrough`; ВСЕ его потребители обработаны без «глотающего» catch-all: `Inlines.flatten`
     (единственная общая точка для Markdown alt-text и Diff similarity/table-cell — рекурсирует в
     детей БЕЗ маркеров `~~`, поэтому нативный flatten «struck» совпадает с ProseMirror `textContent`
     «struck», устраняя фантомную word-diff-правку) и `Projection.inlineOf` (проверка
     `DelimiterChar = '~'` стоит ПЕРЕД разбором по `DelimiterCount`, поэтому `~~struck~~` не
     конфлатится со `Strong` — оба count=2). Grep по `src/` подтвердил: иных сопоставлений по инлайн-
     кейсам нет; `AstDiff.fs`/`DiffWire.fs` работают на `Ast.Block` и берут инлайн-текст через
     `Inlines.flatten`, поэтому новый инлайн-кейс их не задевает. Чистая сборка при warnings-as-errors
     (0/0) — доказательство исчерпываемости всех F#-match'ей.
  2. Кросс-слойная согласованность зачёркивания (ключевая цель M-01). Нативный конвейер
     (`Pipeline.fs` → `.UseEmphasisExtras(EmphasisExtraOptions.Strikethrough)`, узко — без
     sub/superscript/inserted/marked) теперь распознаёт `~~struck~~` согласованно с ОБОИМИ webview-
     слоями, которые это уже делали: редактор кода (CodeMirror lezer GFM `Strikethrough`,
     `webview.js`) и форматированный вид (`pm-markdown.ts`: markdown-it `.enable("strikethrough")`,
     схема-марка `strikethrough`, сериализатор `open/close: "~~"`; тулбар `md-format.ts` `strike:
     "~~"`). Это истинный паритет трёх слоёв, а не новое расхождение: нативное превью рендерит
     `<del>` вместо литеральных тильд, совпадая с рендерингом GFM на GitHub и с редактором.
  3. Отсутствие сквозных регрессий от T-015. Зачёркивание — инлайн-конструкция, поэтому блочная
     структура и `LineMap`↔data-line (Renderer) не меняются: scroll-sync/preview/comment-anchoring
     по счётчикам не задеты (инвариант из фикса 0.1.0 сохранён; известная M-08/T-021-проверка
     касалась блочных списков определений, не инлайнов). `<del>` эмитит сам Markdig, не сырой HTML
     автора (DisableHtml-санитизация не обойдена). Контрактную границу C#↔F#↔TS (IPC-envelope,
     DiffKind-контракт) новый ВНУТРЕННИЙ AST-кейс не двигает.
  4. T-013 и T-014 (webview) локализованы и не пересекаются с остальным. T-013 правит только
     `toggleBlock` (`md-format.ts`, путь редактора КОДА): guard `from === 0 ? 0 : …` устраняет
     инвертированный диапазон `{from:1,to:0}` на документе с ведущим `\n`; для `from>0` и для
     `from===0 && doc[0]!=="\n"` поведение неизменно; ProseMirror-путь (`pm-commands.ts` toggleBlock)
     отдельный, не тронут; пересечения с T-009 (block-splice, отдельная подсистема) нет. T-014
     добавляет ранний guard в paste-обработчик `image-capture.ts` (уступает CodeMirror при непустом
     `text/plain`); drop-путь намеренно без guard (S-16 про Ctrl+V); соседняя очередная M-21/T-034
     (гонка позиции вставки) не done и это не регрессия.
  5. Полнота относительно цели (README: автор правит спеки не касаясь git; без потери данных; простой
     язык) и относительно источника находок (`docs/CODE-REVIEW-PLAN.md`). Все 14 Serious-находок
     (S-01…S-06, S-08, S-09, S-11…S-16 — ровно 14, как заявляет план; S-07 свёрнут в M-16/T-029
     «cross-session variant of S-03», S-10 — пропущенный номер) закрыты в T-001…T-014; секция
     «Серьёзные находки» в `Tasks_Queue.md` корректно пуста. T-015 закрыл первый Minor (M-01);
     остаток очереди T-016…T-060 — Minor/Structure, ничего не потеряно молча. Порядковое ограничение
     соблюдено (T-009 до T-010/T-011).
  6. Сквозные аспекты / технический долг. Новых пользовательских строк с git-лексикой нет
     (зачёркивание — чисто рендер). CHANGELOG содержит ровно 15 записей под `## [Unreleased] →
     Fixed`, заголовок `## [0.1.0] - 2026-07-04` и его контент целы (повторяющийся у прошлых задач
     дефект «снос заголовка релиза» не воспроизведён). Новых заглушек/TODO в изменённых областях
     нет (пред-существующий `Pipeline.fs` `TODO(PoC-3+)` не тронут). Процессные заметки (EOL-only
     изменение `release.cmd` в рабочем дереве, сложённое в тот же @; пустой `.work/Active_Task.md`)
     уже неоднократно зафиксированы reviewer'ом как VCS-гигиена, порчи данных/кода не дают — в
     масштабе решения не Critical/High.
- Проверка сборки/тестов на текущем состоянии (включая незакоммиченный T-015):
  `dotnet build SpecDesk.slnx` — 0 warnings / 0 errors (warnings-as-errors); тест-проекты
  `SpecDesk.Markdown.Tests` — 69 passed (было 66; +3 теста T-015: проекция/flatten/renderer),
  `SpecDesk.Diff.Tests` — 27 passed (ripple DU через Diff зелёный). Webview (`webview/`):
  `npm test` — 317 passed / 30 файлов (включая тесты T-013 md-format/editor и T-014 image-capture).
- Открытых R-записей нет (все SUMMARY-R закрыты); открытых F-записей нет. Можно продолжать
  (следующие задачи очереди T-016… / чекпоинт с push).

### [SUMMARY-R-2026-07-05 17:23] Итог ревью задачи — статус: готово к следующей задаче
- Активная задача: [T-016] Исправить M-02/M-03: неатомарная дедупликация и невалидные ссылки на
  изображения в `ImageEngine.fs`
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Область ревью: изменение в рабочем дереве git (`src/SpecDesk.Core/ImageEngine.fs`,
  `tests/SpecDesk.Core.Tests/ImageEngineTests.fs`, `CHANGELOG.md`, плюс служебные `.work/`). Открытых
  R-записей на входе не было (R-001…R-004 закрыты в прошлых задачах); исправленных/отклонённых записей
  для перепроверки на Фазе 1 не было.
- Вывод: все критерии готовности выполнены.
  1. Атомарная запись (M-02). `File.WriteAllBytes(target, …)` заменён на `writeFileAtomically`: пишет в
     соседний по каталогу temp-файл `target.<guidN>.tmp`, затем `File.Move` в целевое имя. Move в
     пределах одного тома (всегда — temp в том же каталоге) атомарен, поэтому целевое имя появляется
     только полностью записанным. Ключевой инвариант дедупликации сохранён: усечённая при
     крахе/потере питания запись остаётся под `.tmp`-именем, которое glob дедупликации
     `*{hash8}.{ext}` НЕ матчит (заканчивается на `.tmp`, не на `.{ext}`), поэтому «переиспользования»
     обрезка больше не даёт. `finally` удаляет temp при любом провале (`WriteAllBytes` или `Move`),
     не оставляя сироту под целевым именем; при успехе Move temp уже переименован, delete не
     вызывается. Целевое имя дизамбигуируется циклом `while File.Exists` до записи, так что Move не
     натыкается на существующий файл в штатном пути; TOCTOU-гонка неактуальна (IPC серийный,
     подтверждено прошлыми ревью).
  2. Кодирование ссылок (M-03). `buildResult` оборачивает относительный путь в `percentEncodeForLink`
     перед вставкой в `![alt](…)`. Кодируются ровно символы, ломающие «голую» CommonMark-ссылку или
     резолвинг URL: `%` (ПЕРВЫМ, иначе литеральный `%` в пути стал бы двусмысленным с вводимыми
     эскейпами — проверено: путь с `100%` → `100%25`, декод обратно в `100%`, без инъекции `%00`
     и т.п.), `(`/`)`, `#` (фрагмент URL) и любой `Char.IsWhiteSpace` через `Uri.EscapeDataString`.
     Разделитель `/` НЕ кодируется — путь остаётся сегментируемым и в webview (`resolveImageSrc`
     → `normalizeRelative`), и в нативном рендере (`Renderer.rewriteImageUrl`). Контракт чтения
     подтверждён сквозным: `AppAssetResolver.Resolve` (`AppAssetResolver.cs:34`) декодирует URL через
     `Uri.UnescapeDataString(uri.AbsolutePath)` ДО обращения к ФС, поэтому `my%20images/x.png`
     резолвится в он-диск `my images/x.png`. Он-диск путь и `RelativePath` остаются с литеральными
     символами — кодируется только эмитируемая Markdown-ссылка (проверено тестом: файл существует по
     сырому пути с пробелом).
  3. Набор кодируемых символов адекватен для Windows-таргета: `?`/`<`/`>` и прочие «опасные» символы
     недопустимы в именах файлов Windows (запись бы всё равно провалилась), а `#`/`(`/`)`/whitespace —
     ровно перечисленные в задаче случаи. Non-ASCII whitespace (например NBSP) корректно эскейпится
     `Uri.EscapeDataString`.
  4. Тесты (5 новых, `TestImages.tinyPng` существует и компилируется первым в fsproj): путь с пробелом
     кодируется в ссылке, но не на диске; unit-случаи `percentEncodeForLink` (пробел/скобки/hash/
     литеральный `%`; already-safe путь без изменений); `writeFileAtomically` не оставляет temp-артефакт
     и пишет точные байты; `writeFileAtomically` при провале финального Move (целевое имя занято
     каталогом) не оставляет частичный файл под целевым именем и чистит temp. Существующий тест
     `identical content is de-duplicated to a single file` не регрессировал (после атомарной записи в
     каталоге ровно 1 файл — заодно страхует от утечки temp на happy-path).
  5. `CHANGELOG.md`: запись добавлена только в `## [Unreleased]`; заголовок релиза
     `## [0.1.0] - 2026-07-04` цел — повторяющийся у прошлых задач дефект «снос заголовка релиза» не
     воспроизведён.
- Замеченные НЕ-Critical/High края (в отдельную R-запись не выносятся, порчи данных не дают, вне явных
  критериев): осиротевшие `.tmp`-файлы после реального краха процесса накапливаются и слегка
  завышают счётчик `seq` (косметический пропуск в нумерации, не потеря данных); дедупликация по-прежнему
  матчит только по 8-символьному суффиксу хэша без сверки содержимого — но задача требовала лишь
  устранить «переиспользование обрезка» (сделано атомарностью), а коллизия hash8 разных изображений
  астрономически маловероятна и вне рамок задачи.
- Новых серьёзных проблем за 2 прогона не найдено; открытых R-записей нет. Задача решена полностью —
  можно переходить к следующей задаче.

### [SUMMARY-R-2026-07-05 17:42] Итог ревью задачи — статус: готово к следующей задаче
- Активная задача: [T-017] Исправить M-04: SVG с UTF-8 BOM отклоняется как изображение
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Область ревью: изменение в рабочем дереве (последовательный режим, `jj diff`):
  `src/SpecDesk.Core/ImageProcessing.fs`, `tests/SpecDesk.Core.Tests/ImageProcessingTests.fs`,
  `CHANGELOG.md` (плюс служебные `.work/`). Открытых R-записей на входе не было; исправленных/отклонённых
  записей для перепроверки на Фазе 1 не было.
- Вывод: все критерии готовности выполнены.
  1. Срез BOM перед проверкой. В `looksLikeSvg` перед `StartsWith "<svg"`/`"<?xml"` первые ≤256 байт
     декодируются как UTF-8 и очищаются `.TrimStart(char 0xFEFF).TrimStart()`. Разбор верен: BOM
     `EF BB BF` декодируется `Encoding.UTF8.GetString` в один символ U+FEFF (GetString НЕ срезает
     преамбулу), а `Char.IsWhiteSpace` его не считает пробелом, поэтому обычный `TrimStart()` его не
     убирал — теперь убирает явный `TrimStart(char 0xFEFF)`. Порядок (сначала BOM, потом пробелы)
     корректен для реального «BOM в самом начале».
  2. Приём SVG с BOM в paste/clipboard-конвейере. Единственная точка детекции SVG в исходниках —
     `looksLikeSvg`, и она достигается ровно из paste/drop-пути `ImageEngine.fs:128 → processImage →
     looksLikeSvg`; дублирующей детекции в host/webview нет (совпадения `<svg>`/BOM в grep — только
     `node_modules`). `processImage` при `looksLikeSvg = true` и наличии `svg` в `Allowed` отдаёт байты
     verbatim (BOM сохраняется — это корректный pass-through, SVG никогда не перекодируется; BOM в XML
     допустим спецификацией; хэш дедупликации считается по тем же байтам).
  3. Тест на SVG с BOM добавлен — 4 теста: pass-through BOM-SVG через `processImage` (Ext=svg, байты
     verbatim), `looksLikeSvg` на BOM+xml-пролог, на bare `<svg>` с/без BOM, и негатив (мусор с/без BOM,
     пустой массив). Логически все проходят. Видимость `looksLikeSvg` расширена `private → internal`
     ради прямого вызова из тестов — согласуется с уже существующим паттерном (`isInside`,
     `sanitizeExt`) и покрыта `InternalsVisibleTo("SpecDesk.Core.Tests")` в `SpecDesk.Core.fsproj`,
     так что тесты компилируются; `ImagesConfig.defaults.Allowed` содержит `svg`, поэтому pass-through
     тест возвращает `Ok`.
  4. Запись в `CHANGELOG.md` добавлена под `## [Unreleased]` → `### Fixed`, точно описывает симптом и
     причину (U+FEFF не срезается `TrimStart`).
- Регрессий/новых рисков не выявлено: изменение не расширяет детекцию на растровые форматы (их магические
  байты не начинаются с `<svg`/`<?xml`), `GetString` с дефолтным UTF-8 не бросает на невалидных байтах
  (replacement fallback, поведение прежнее), данные не теряются (pass-through verbatim). Новых серьёзных
  проблем не найдено — можно переходить к следующей задаче.

### [SUMMARY-R-2026-07-05 18:00] Итог ревью задачи — статус: готово к следующей задаче
- Активная задача: [T-018] Исправить M-05: экранированные кавычки в `.spectool.toml` сбивают самодельный
  парсер (`Toml.fs`)
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Область ревью: изменение в рабочем дереве (последовательный режим, `jj diff` относительно родителя
  `4c951266`): `src/SpecDesk.Core/Toml.fs`, `tests/SpecDesk.Core.Tests/TomlTests.fs`, `CHANGELOG.md`
  (плюс служебный `.work/status.md`). Открытых R-записей на входе не было; исправленных/отклонённых
  записей для перепроверки на Фазе 1 не было.
- Вывод: оба критерия готовности выполнены по первой ветке (round-trip без экранирования).
  1. Трекер кавычек стал escape-aware. В `stripInlineComment` и `containsUnquoted` добавлен флаг
     `escaped`: внутри строки `\` взводит `escaped`, а следующий символ гасит его, НЕ трогая `inQuote`.
     Порядок ветвей (`inQuote && escaped` → `inQuote && c='\\'` → `c='"'` → искомый символ) даёт
     корректную чётность: `\\` — экранированный бэкслеш (следующий `"` реально закрывает строку),
     `\"` — экранированная кавычка (не закрывает). Прогон `template = "a\"#b"` подтверждает: `#` после
     экранированной кавычки остаётся внутри строки, значение не усекается. `]`-путь (`containsUnquoted`)
     закрывающую скобку массива с экранированной кавычкой в записи находит верно; на входах без бэкслешей
     внутри кавычек (обычные глобы вроде `docs/[a-z].md`) поведение идентично прежнему — регрессий нет.
  2. Разэкранирование. Новая `unescape` переводит `\"`, `\\`, `\n`, `\t`, `\r`; нераспознанная
     последовательность (`\q`) остаётся дословно (бэкслеш + следующий символ), одиночный завершающий `\`
     не даёт выхода за границы (`i + 1 < value.Length`) — деградация мягкая, без исключений. `unquote`
     теперь пропускает содержимое кавычек через `unescape`, поэтому `template = "Say \"hi\""` → `Say
     "hi"` без остаточных бэкслешей в тексте коммита. Границы строки, вычисленные трекером, и единицы
     разэкранирования согласованы (обе трактуют `\X` как пару), поэтому расхождения смещения нет.
  3. Порядок компиляции F# соблюдён (`unescape` объявлена до `unquote`), `Text.StringBuilder`
     разрешается через `open System`.
  4. Тесты (5 новых) логически верны: odd-parity `"a\"#b"`; реальный inline-комментарий после значения
     с экранированными кавычками стрипается; round-trip `"Say \"hi\""`; `\\`/`\n`/`\t`; нераспознанный
     `\q` — дословно. Прослежены вручную — все зелёные.
  5. `CHANGELOG.md`: запись добавлена только под `## [Unreleased]` → `### Fixed`; заголовок релиза
     `## [0.1.0] - 2026-07-04` цел — повторяющийся дефект «снос заголовка релиза» не воспроизведён.
- Замеченные НЕ-Critical/High края (в отдельную R-запись не выносятся, вне критериев задачи и не
  регрессии): `getList` по-прежнему извлекает элементы регуляркой `"([^"]*)"`, которая не разэкранирует
  и споткнётся об экранированную кавычку ВНУТРИ элемента списка (globs/reviewers) — но задача касается
  строкового `template` (путь `getString`/`unquote`), кавычки в глобах/логинах практически не встречаются,
  и код `getList` этой задачей не менялся; на пато­логичном/неоднозначном входе вида `["a\", "b]"]` новый
  трекер находит `]` в другой позиции, чем старый, но это TOML-корректнее прежнего, а не порча.
- Новых серьёзных проблем за 2 прогона не найдено; открытых R-записей нет. Задача решена полностью —
  можно переходить к следующей задаче.

### [SUMMARY-F-2026-07-05 18:14] Итог интеграционного ревью — статус: готово к следующей задаче
- Проверено задач (по Tasks_Done.md): 18 (T-001…T-018 — S-01…S-06, S-08, S-09, S-11…S-16, M-01…M-05)
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Область: инкремент с прошлого интеграционного ревью (SUMMARY-F 17:08 покрыл T-001…T-015) — фокус на
  трёх новых задачах, все в `SpecDesk.Core`: T-016 (M-02/M-03 — атомарная запись + percent-encoding
  ссылки в `ImageEngine.fs`), T-017 (M-04 — распознавание BOM-SVG в `ImageProcessing.fs`), T-018 (M-05 —
  escape-aware трекер кавычек и `unescape` в `Toml.fs`). VCS: jj (colocated с git). T-016/T-017
  закоммичены (git `5eb20da`, `4c95126`); T-018 сидит в рабочей копии (@ `f1bb3bcd`; правки только в
  `Toml.fs` + `TomlTests.fs` + `CHANGELOG.md` + служебные `.work/`).
- Вывод: интеграционных проблем не найдено; три исправления согласованно сочетаются и локализованы, ни
  один новый межмодульный контракт не заведён.
  1. Композиция конвейера изображений (T-016 ∘ T-017 — соседние правки в одном пути). `insertImage`
     (`ImageEngine.fs`) вызывает `ImageProcessing.processImage`; T-017 расширил лишь ветку `looksLikeSvg`
     (BOM срезается перед `<svg`/`<?xml`), возвращающую SVG-байты verbatim, а T-016 переписал только
     ПОСЛЕ-обработочную запись (`writeFileAtomically`) и эмиссию ссылки (`percentEncodeForLink`). Стыки
     согласованы: `hash8` считается по тем же verbatim-байтам, что пишутся, поэтому glob дедупликации
     `*{hash8}.{ext}` (ext=`svg`) находит именно тот файл; temp-имя `target.<guidN>.tmp` НЕ матчит этот
     glob (заканчивается на `.tmp`), поэтому атомарность T-016 не ломает pass-through T-017 и наоборот.
     Функции не пересекаются по состоянию — регрессий между двумя задачами одного подпути нет.
  2. Кросс-слойный контракт ссылки (C#↔F#, ключевой для M-03). `percentEncodeForLink` кодирует ровно
     `%` (первым), `(`, `)`, `#` и любой `Char.IsWhiteSpace`, НЕ трогая `/` — путь остаётся
     сегментируемым. Приёмная сторона проверена по исходнику: `AppAssetResolver.Resolve`
     (`AppAssetResolver.cs:34`) декодирует `Uri.UnescapeDataString(uri.AbsolutePath)` ДО `ResolveRelative`
     и обращения к ФС, поэтому `my%20images/x.png` резолвится в он-диск `my images/x.png`. Он-диск путь и
     `RelativePath` хранят литеральные символы — кодируется только эмитируемая Markdown-ссылка. Граница
     не сдвинута: тот же декод-путь используют и webview (`resolveImageSrc`), и нативный рендер
     (`rewriteImageUrl`), новых расхождений батч не вносит.
  3. Влияние T-018 на потребителей `Toml.fs` (config-слой). `unquote` теперь прогоняет содержимое кавычек
     через `unescape`; это питает `getString` для ВСЕХ таблиц (`[images]`, `[repo]`, `[branch]`,
     `[commit]`), а `ImagesConfig.parse` читает `folder`/`naming`/`preferred`/`case` именно через
     `getString`. Регрессии нет: `\\`→`\`, нераспознанная `\x` остаётся дословно (backslash+символ),
     поэтому типовые значения без escape-последовательностей идентичны прежним; escape-aware трекер на
     входах без `\` внутри кавычек (обычные глобы `docs/[a-z].md`) ведёт себя как раньше. Сборка при
     warnings-as-errors (порядок компиляции F#: `unescape` до `unquote`) зелёная — контракт `Toml`↔
     `ImagesConfig` цел.
  4. Полнота относительно цели и источника находок. Батч закрывает ровно M-02…M-05 из
     `docs/CODE-REVIEW-PLAN.md` (строки 205–208), включая вопрос плана «Is escape support deliberately
     out of scope?» (M-05) — решён по ПЕРВОЙ альтернативе критерия (реализована поддержка escape с
     round-trip-тестом), а не «мягкой» второй; это допустимое закрытие. В `Tasks_Queue.md` секция
     «Серьёзные находки (Serious)» пуста, T-016…T-018 корректно изъяты, Minor-хвост продолжается с T-019
     (M-06) — ничего не потеряно молча и не задублировано. README-цель (автор правит спеки не касаясь
     git; без потери данных; простой язык) не задета: все три правки — бэкенд `Core`, пользовательские
     строки ошибок на простом языке («Could not save the image», «SVG images are not allowed…»), git-
     лексика не протекает.
  5. Сквозные аспекты / техдолг. Webview (`webview/`) этим инкрементом НЕ тронут (правки только в
     `SpecDesk.Core`), поэтому IPC-envelope и TS-слой без изменений — межслойных контрактов не двигали.
     `CHANGELOG.md` содержит три новые записи под `## [Unreleased] → ### Fixed` (атомарная запись/
     percent-encode; BOM-SVG; escape-aware Toml), заголовок релиза `## [0.1.0] - 2026-07-04` цел
     (повторяющийся дефект «снос заголовка релиза» не воспроизведён). Новых заглушек/TODO/git-лексики в
     изменённых областях нет. Ранее зафиксированные reviewer'ом НЕ-Critical края (осиротевшие `.tmp` при
     реальном краше слегка завышают счётчик `seq`; `getList` остаётся не-escape-aware в отличие от
     `getString` — но `Allowed`/`reviewers`/globs практически не содержат экранированных кавычек) в
     масштабе решения не Critical/High и уже описаны в соответствующих SUMMARY-R (17:23, 18:00).
     Процессная заметка: EOL-only правка `release.cmd` видна в git working tree, но НЕ в `jj status`
     (сложена в историю/вне @) — VCS-гигиена, порчи кода/данных не даёт.
- Проверка сборки/тестов на текущем состоянии (включая незакоммиченный T-018):
  `dotnet build SpecDesk.slnx` — 0 warnings / 0 errors (warnings-as-errors). `dotnet test SpecDesk.slnx`
  — все проекты зелёные: `SpecDesk.Core.Tests` 97 passed (было 88 до батча: +5 T-016 ImageEngine
  атомарность/encode, +4 T-017 ImageProcessing BOM, +5 T-018 Toml escape — уже слитые в один проект),
  `SpecDesk.Markdown.Tests` 69, `SpecDesk.Diff.Tests` 27, `SpecDesk.Contracts.Tests` 7,
  `SpecDesk.GitHub.Tests` 115, `SpecDesk.Git.Tests` 32, `SpecDesk.Host.Tests` 160. Webview не менялся —
  npm-набор вне области инкремента.
- Открытых R-записей нет (все SUMMARY-R по T-016/T-017/T-018 закрыты: 17:23, 17:42, 18:00); открытых
  F-записей нет. Можно продолжать (следующая задача очереди — T-019 / M-06, либо чекпоинт с push).

### [SUMMARY-R-2026-07-05 18:33] Итог ревью задачи — статус: готово к следующей задаче
- Задача: T-019 — Исправить M-06: правка только форматирования внутри списка/таблицы эмитит пустую базу diff'а
- Прогонов: 2 (оба пустые — новых серьёзных проблем нет)
- Открытых проблем: 0
- Ревьюил рабочую копию (jj, colocated). Diff задачи: `src/SpecDesk.Diff/DiffWire.fs`,
  `tests/SpecDesk.Diff.Tests/DiffWireTests.fs`, `CHANGELOG.md` (+ служебный `.work/status.md`).
- Суть правки: в `toWire` (ветка `AstDiff.Changed` для контейнера list/table) при пустом результате
  `childDiff` (правка только форматирования — `Inlines.flatten` срезает марки, поэтому плоские тексты
  всех детей совпадают, а контейнер на верхнем уровне классифицирован как `Changed` по mark-aware
  Content) вместо `emit … [||] "" ""` теперь эмитится тот же plain-block fallback, что у не-контейнерного
  `Changed`-блока: `BaseText = AstDiff.blockText before.Content`, `BaseSource = slice baseLines
  before.LineStart before.LineEnd`. Форма записи для webview не меняется.
- Проверка контракта с webview: форма (Kind=changed, Children=[||], непустой BaseText/BaseSource,
  LineStart/LineEnd = head-контейнер) уже обрабатывается существующим whole-block путём —
  `webview/src/diff-marks.ts:56` (`entry.children.length === 0` → whole-block mark с baseText/baseSource),
  Code-пейн `editor.ts:224` (`pushInlineSourceWords` word-diff'ит headSource против реального baseSource),
  Formatted-пейн `formatted.ts:491`. Новый контракт не заведён — правка переиспользует уже
  проверенный/отгруженный путь. До правки `baseSource=""` → весь исходник контейнера подсвечивался как
  «добавленный» (смыв всей таблицы); теперь word-diff подсвечивает только реально изменённый фрагмент
  (напр. `**`), что закрывает M-06.
- Критерии `task.md`: (1) fallback пишет plain-block текст в BaseText/BaseSource — да; (2) word-diff
  подсвечивает только изменённый элемент, а не всю таблицу/список — да (блок-уровневый line-wash всего
  контейнера — неотъемлемое следствие того, что `childDiff` на плоском тексте не может определить строку;
  задача явно выбирает «plain-block fallback», это не Critical/High); (3) тесты на список и таблицу +
  контрольный тест обычной правки текста (per-child путь) — да; (4) `CHANGELOG.md` под `## [Unreleased] →
  ### Fixed`, заголовок релиза `## [0.1.0] - 2026-07-04` цел — да.
- Разбор Critical/High: логических ошибок, потери данных, гонок, необработанных ошибок и сломанных
  контрактов не выявлено. Функция чистая; use `before`/`after` line-диапазонов согласован с соседней
  не-контейнерной веткой; крайние случаи (смена ordered/unordered, пустой контейнер) деградируют
  безопасно к тому же fallback.
- Вывод: новых серьёзных проблем не найдено. Открытых R-записей по задаче нет — готово к следующей задаче.

### [SUMMARY-R-2026-07-05 18:51] Итог ревью задачи — статус: готово к следующей задаче
- Задача: T-020 — Исправить M-07: неограниченная память/CPU diff'а на больших почти-идентичных документах
- Прогонов: 2 (оба пустые — новых серьёзных проблем нет)
- Открытых проблем: 0
- Ревьюил рабочую копию (git/jj colocated). Diff задачи: `src/SpecDesk.Diff/AstDiff.fs`,
  `src/SpecDesk.Diff/SpecDesk.Diff.fsproj`, `tests/SpecDesk.Diff.Tests/AstDiffTests.fs`, `CHANGELOG.md`
  (+ служебный `.work/status.md`).
- Суть правки: `diff` разбит на дешёвый диспетчер и прежнее тело `diffBounded` (O(m·n) LCS +
  попарное сходство, поведение под лимитом не изменилось). Добавлен guard `maxNodePairs = 4_000_000L`:
  при `int64 m * int64 n > maxNodePairs` вместо `diffBounded` вызывается `flatDiff` (все базовые →
  Removed, все головные → Added). `InternalsVisibleTo(SpecDesk.Diff.Tests)` открывает `maxNodePairs`
  тестам.
- Проверка сборки/тестов: `dotnet test SpecDesk.slnx --filter AstDiffTests` — сборка чистая
  (warnings-as-errors проходит), 17/17 AstDiff-тестов зелёные, включая 3 новых M-07-guard-теста.
- Критерии task.md: (1) LCS и попарное сходство ограничены по размеру с откатом на Removed+Added —
  да, единый guard на `m·n` покрывает обе O(m·n)-стоимости (массив LCS `(m+1)(n+1)` и список кандидатов
  ≤ `m·n`); (2) тест на большой почти-идентичный документ, подтверждающий ограниченное время —
  да (`… completes quickly (bounded time)`, порог 5 c), плюс тест на срабатывание отката и контртест
  «крупный, но под лимитом» без ложного отката; (3) запись в `CHANGELOG.md` под `## [Unreleased]`,
  заголовок релиза `## [0.1.0] - 2026-07-04` цел — да.
- Разбор Critical/High: переполнения нет (`int64·int64`, размеры массивов ограничены ~2·10⁹, произведение
  укладывается в int64); краевые случаи `m=0`/`n=0` дают произведение 0 → `diffBounded`, но там вложенный
  цикл вырождается в O(m+n), OOM/зависания нет; односторонний гигант (`m=1, n≫`) при `m·n > лимита` уходит
  в `flatDiff`; `flatDiff` возвращает валидный `DocumentDiff` (только Removed/Added — оба обрабатываются
  потребителем `DiffWire.toWire`), контракт не сломан; функция чистая, общего мутируемого состояния/гонок
  нет; регрессий поведения под лимитом нет (публичная сигнатура `diff` сохранена, прежние тесты зелёные).
  Замечание не-Critical/High: у самой границы (`m·n ≈ 4·10⁶` при почти-идентичном входе) сортировка до
  ~4·10⁶ кандидатов даёт секунды CPU и сотни МБ пиково — это ограниченный, задокументированный худший
  случай (не hang/OOM), критерий «ограничено по размеру» выполнен; doc-комментарий «sub-second» слегка
  оптимистичен, но это стилистика, не дефект.
- Вывод: новых серьёзных проблем не найдено. Открытых R-записей по задаче нет — готово к следующей задаче.

### [SUMMARY-R-2026-07-05 19:15] Итог ревью задачи — статус: готово к следующей задаче
- Задача: T-021 — Проверить M-08: инвариант LineMap↔data-line для списков определений
- Прогонов: 2 (оба пустые — новых серьёзных проблем нет)
- Открытых проблем: 0
- Ревьюил рабочую копию (git/jj colocated, последовательный режим). Diff задачи:
  `src/SpecDesk.Markdown/Renderer.fs`, `tests/SpecDesk.Markdown.Tests/RendererTests.fs`, `CHANGELOG.md`
  (+ служебный `.work/status.md`).
- Суть правки: в `stampLineAnchors.tagTree` добавлен явный кейс `DefinitionItem` (перед общими ветками
  `LeafBlock`/`ContainerBlock`), который тэгирует только дочерние `DefinitionTerm` (`<dt>`), пропуская тело
  определения. До правки общая рекурсия по `LeafBlock` тэгировала и `DefinitionTerm`, и тело-`ParagraphBlock`;
  добавлен `open Markdig.Extensions.DefinitionLists`. CHANGELOG под `## [Unreleased] → ### Fixed`
  (заголовок релиза `## [0.1.0] - 2026-07-04` цел). Новый регрессионный тест на инвариант для списков
  определений.
- Эмпирическая проверка ключевой претензии (Markdig 1.3.2): собрал в scratchpad отдельный пробник,
  который прогоняет тот же список определений, ВРУЧНУЮ навешивает `data-line-start` и на термины, и на
  тела-параграфы (как это делала старая общая рекурсия), затем рендерит через тот же pipeline. Результат:
  из 5 навешенных атрибутов в HTML попадают только 2 — на `<dt>`; `<dd>` рендерятся без атрибута
  (`<dd data-line`: отсутствует). Значит инвариант `occurrences(data-line-start) == LineMap.Length`
  старым кодом РЕАЛЬНО нарушался (5 записей LineMap против 2 атрибутов), баг M-08 подтверждён как настоящий,
  а претензии комментария/CHANGELOG точны.
- Проверка сборки/тестов: `dotnet test SpecDesk.slnx --filter FullyQualifiedName~RendererTests` —
  сборка чистая (warnings-as-errors проходит), 20/20 тестов `SpecDesk.Markdown.Tests` зелёные, включая
  новый тест на списки определений (проверил отдельно по имени — исполняется, не Skipped). Существующий
  общий тест инварианта (`every top-level block emits a line attribute`) по-прежнему зелёный.
- Критерии task.md: (1) добавлен регрессионный тест на инвариант количества LineMap↔data-line для списков
  определений по аналогии с другими семействами блоков — да (термин с одним определением + термин с двумя
  подряд телами); (2) `Renderer.fs` исправлен, т.к. тест выявил реальное расхождение — да, подтверждено
  пробником; (3) запись в `CHANGELOG.md` (правка потребовалась) — да, под `## [Unreleased] → ### Fixed`.
- Разбор Critical/High: инвариант с новым кодом держится (единственный источник `data-line` — наш `tag`;
  тэгируем только `DefinitionTerm`, чей атрибут Markdig всегда пишет → нет пере-/недосчёта); краевые случаи
  (термин с несколькими определениями, вложенный список определений внутри blockquote/list) достигают
  нового кейса и деградируют безопасно; логических ошибок, потери данных, гонок, необработанных исключений,
  сломанных контрактов не выявлено; функция чистая. Порядок веток match корректен (`DefinitionItem`
  перехватывается до общего `ContainerBlock`).
  Замечание не-Critical/High: правка пропускает всю рекурсию в тело определения, поэтому вложенный блочный
  контент внутри `<dd>` (например список определений внутри определения) теряет якоря scroll-sync, которые
  старый код навешивал бы на `<li>` (их Markdig как раз рендерит с атрибутами). Это снижение гранулярности,
  а не нарушение инварианта (счётчики остаются сбалансированными), для редкой конструкции и на фоне и без
  того сломанного старого поведения — не Critical/High.
- Вывод: новых серьёзных проблем не найдено. Открытых R-записей по задаче нет — готово к следующей задаче.

### [SUMMARY-F-2026-07-05 19:22] Итог интеграционного ревью — статус: готово к публикации
- Проверено задач (по Tasks_Done.md): 21 (T-001…T-021 — S-01…S-06, S-08, S-09, S-11…S-16, M-01…M-08);
  инкремент этого батча — 3: T-019/T-020/T-021.
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Область: инкремент с прошлого интеграционного ревью (SUMMARY-F 18:14 покрыл T-001…T-018) — фокус на
  трёх новых задачах diff/render-слоя: T-019 (M-06 — plain-block fallback для formatting-only правки
  контейнера в `DiffWire.fs`), T-020 (M-07 — size-guard O(m·n) LCS/сходства + `flatDiff` в `AstDiff.fs`),
  T-021 (M-08 — инвариант LineMap↔data-line для списков определений в `Renderer.fs`). VCS: jj (colocated
  с git). T-019/T-020 закоммичены (jj `f1b4ea48`, `8c94a2cc`); T-021 сидит в рабочей копии (@ `231635d7`;
  правки в `Renderer.fs` + `RendererTests.fs` + `CHANGELOG.md` + служебные `.work/`).
- Вывод: интеграционных проблем не найдено; три исправления согласованно сочетаются, локализованы в
  diff/render-слое (`SpecDesk.Diff` + `SpecDesk.Markdown`), ни один новый межмодульный/кросс-языковой
  контракт не заведён.
  1. Архитектурная целостность. Все три правки — внутри уже существующих модулей; F#-компиляция при
     warnings-as-errors зелёная, значит все исчерпывающие match'и (включая `AstDiff.kindTag`/`blockText`,
     `DiffWire.childTexts`, `Renderer.tagTree`) по-прежнему покрывают все кейсы DU.
  2. Ключевая точка стыковки T-019 ∘ T-020 (обе в `SpecDesk.Diff` — взаимоусиление, не конфликт).
     `DiffWire.childDiff` (:108) переиспользует `AstDiff.diff` на синтетических пер-строчных/пер-элементных
     документах, поэтому size-guard T-020 (`int64 m * int64 n > maxNodePairs`, откат в `flatDiff`) защищает
     НЕ ТОЛЬКО верхнеуровневый diff, но и дочерний diff внутри контейнера. Именно к child-уровню через
     `childDiff` и относится названный в M-07 патологический случай «таблица на несколько тысяч строк»
     (верхний уровень там 1×1 блок, а взрыв O(m·n) — на строках), так что T-020 закрывает ровно тот путь,
     который расширил T-019. Гигантская таблица теперь и на child-уровне уходит в `flatDiff` (все строки
     Removed+Added) — валидный НЕПУСТОЙ `ChildWireEntry[]`, поэтому formatting-only fallback T-019
     (срабатывает лишь при ПУСТОМ `childDiff`) при этом не задевается; регрессии между двумя задачами
     одного подпути нет.
  3. Кросс-языковой контракт diff цел (ключевой для M-06). Форма wire-записи T-019 (Kind=changed,
     `Children=[||]`, непустые `BaseText`/`BaseSource`, `LineStart`/`LineEnd` = head-контейнер) не заводит
     новый путь — она уже обрабатывается whole-block веткой webview (`diff-marks.ts:56`,
     `entry.children.length === 0` → mark с `baseText`/`baseSource`). Значения `DiffKind` не менялись
     (`added/removed/changed/moved`), поэтому парити-гард `DiffKindContractTests` ↔
     `webview/tests/contract/diff-kinds.json` ↔ TS `DIFF_KINDS` зелёный. Webview этим батчем НЕ тронут
     (правки только в F#), IPC-envelope без изменений — границу C#↔F#↔TS не двигали.
  4. T-021 не пересекается с diff-путём. `Renderer.stampLineAnchors` — ОТДЕЛЬНЫЙ обход дерева Markdig
     (`MarkdownObject`), а не `Projection.toAst`, который потребляет `AstDiff`. Поэтому фикс инварианта
     LineMap↔data-line для списков определений не трогает семантический diff (T-019/T-020) и не
     конфликтует с ним, лишь делит модуль `SpecDesk.Markdown`. Линия-карта (scroll-sync/preview/
     comment-anchoring) живёт в своей системе координат по строкам, независимой от line-диапазонов
     wire-diff'а (те приходят из `Projection`). Убрав лишние LineMap-записи для тел `<dd>` (которые Markdig
     НЕ помечает атрибутом), T-021 УСТРАНЯЕТ потенциальный рассинхрон карты с DOM, а не вносит его; порядок
     веток `tagTree` корректен (`DefinitionItem` перехвачен до общего `LeafBlock`/`ContainerBlock`).
  5. Полнота относительно источника находок и цели. Батч закрывает ровно M-06/M-07/M-08 из
     `docs/CODE-REVIEW-PLAN.md` (строки 209–211); критерии `task.md` каждой задачи выполнены (T-019:
     fallback пишет plain-block в `BaseText`/`BaseSource` + тесты на список и таблицу + контрольная обычная
     правка; T-020: обе O(m·n)-стоимости ограничены одним guard'ом на `m·n` + тест ограниченного времени +
     контртест «крупный, но под лимитом»; T-021: регресс-тест инварианта + реальный фикс `Renderer.fs`,
     подтверждённый эмпирическим пробником reviewer'а). README-цель (автор правит спеки не касаясь git; без
     потери данных; простой язык) не задета — все три правки бэкенд-diff/render, пользовательских строк с
     git-лексикой не добавлено.
  6. Сквозные аспекты / техдолг. `CHANGELOG.md` содержит три новые записи под `## [Unreleased] → ### Fixed`
     (строки 151–169: formatting-only diff base; size-guard diff; definition-list line-map), заголовок
     релиза `## [0.1.0] - 2026-07-04` цел (строка 171) — повторяющийся дефект «снос заголовка релиза» не
     воспроизведён. Новых заглушек/TODO/git-лексики в изменённых областях нет. Ранее зафиксированные
     reviewer'ом НЕ-Critical/High края (SUMMARY-R 18:33/18:51/19:15) в масштабе решения остаются не
     Critical/High: (а) у самой границы M-07 (`m·n ≈ 4·10⁶` почти-идентичных) сортировка кандидатов даёт
     секунды CPU / сотни МБ пиково — ограниченный документированный худший случай, не hang/OOM;
     (б) M-08 снижает гранулярность scroll-sync для вложенного блочного контента внутри `<dd>` (инвариант
     при этом держится, счётчики сбалансированы); (в) T-019 line-wash всего контейнера при formatting-only
     правке неизбежен (плоский `childDiff` не может определить строку) — задача явно выбрала plain-block
     fallback. Пустое jj-изменение `d1acb9a5` трогает только `.work/` — процессный артефакт, не код.
- Проверка сборки/тестов на текущем состоянии (включая незакоммиченный T-021):
  `dotnet build SpecDesk.slnx` — 0 warnings / 0 errors (warnings-as-errors). `dotnet test SpecDesk.slnx`
  — все проекты зелёные: `SpecDesk.Diff.Tests` 33 (было 27 до батча: +3 T-019 formatting-only
  список/таблица/контроль, +3 T-020 M-07-guard), `SpecDesk.Markdown.Tests` 70 (было 69: +1 T-021 инвариант
  для списков определений), `SpecDesk.GitHub.Tests` 115, `SpecDesk.Contracts.Tests` 7,
  `SpecDesk.Core.Tests` 97, `SpecDesk.Git.Tests` 32, `SpecDesk.Host.Tests` 160 = 514 passed / 0 failed /
  0 skipped. Webview (`webview/`) этим инкрементом НЕ тронут (правки только в F#) — npm-набор вне области
  инкремента.
- Открытых R-записей нет (все SUMMARY-R по T-019/T-020/T-021 закрыты: 18:33, 18:51, 19:15); открытых
  F-записей нет; последний прогон пуст. Решение согласовано и готово к публикации (чекпоинт с push).

### [SUMMARY-R-2026-07-05 19:43] Итог ревью задачи — статус: готово к следующей задаче
- Активная задача: [T-022] Исправить M-09: документация IGitHubAuth расходится с реальным поведением при
  ошибке сохранения токена
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Область ревью: изменение в рабочей копии jj (`src/SpecDesk.GitHub/IGitHubAuth.cs` — только XML-doc
  метода `AwaitAuthorizationAsync`, `CHANGELOG.md`, плюс служебный `.work/status.md`). Открытых/исправленных/
  отклонённых R-записей на входе не было — Фаза 1 без работы. Изменение только документационное; поведение
  не менялось.
- Вывод: все критерии готовности выполнены; правка точна и полна относительно реализации.
  1. Doc ↔ реализация. Прежний комментарий (строки 105–107 до правки) утверждал, что сбой сохранения токена
     после успешной авторизации «surfaces as a thrown exception (not a result)». Реально
     `GitHubDeviceFlowAuth.CompleteAuthorizationAsync` (:118–135) оборачивает `_store.Save(...)` в
     `catch (Exception ex) when (ex is IOException or UnauthorizedAccessException or CryptographicException)`
     и возвращает `SignInResult.StorageFailed()` — НЕ бросает. Новый комментарий (:99–102) прямо говорит:
     StorageFailed (disk / permissions / encryption fault) «is returned as a SignInResult, never thrown». Это
     верно и точно ограничено ровно тем набором исключений, что реально ловится: disk→IOException
     (и наследники DirectoryNotFoundException/PathTooLongException), permissions→UnauthorizedAccessException,
     encryption→CryptographicException. `FileTokenStore.Save` (:36–41) бросает именно эти категории; DPAPI
     `Protect` даёт CryptographicException. Формулировка «never thrown» не переширена — она согласована с
     scope «disk/permissions/encryption».
  2. Полнота «never thrown past StartSignInAsync». Проверены все пути `AwaitAuthorizationAsync` (:60–113):
     тело обёрнуто в `catch (OperationCanceledException) when (cancellationToken.IsCancellationRequested)` →
     возврат TimedOut (отмена хостом не бросает). Транзиентные сбои поллинга в `DeviceFlowApi.ExchangeAsync`
     (:193–222) и `GetLoginAsync` (:301–328) отображаются в `Transient`, а per-request-timeout ловится
     `when (!ct.IsCancellationRequested)` — наружу выходит лишь реальная отмена вызывающего, которую и
     перехватывает внешний catch. Так что для целевой Windows-платформы утверждение doc корректно; един­ственный
     теоретический незакрытый тип (PlatformNotSupportedException на не-Windows) вне scope «disk/permissions/
     encryption» и вне v1-таргета — не дефект.
  3. Согласованность с хостом (критерий 2 задачи). Хостовый вызывающий (`HostController.cs:1726–1741`) уже
     опирается на result-контракт: не оборачивает `AwaitAuthorizationAsync` в try/catch, ждущий брошенного
     storage-исключения (широкий `catch` :1749 относится к up-front `StartSignInAsync`), а маршрутизирует
     `result.Outcome` через `SignInMessage` (:1803–1810), где `StorageFailed` даёт «Signed in to GitHub, but
     couldn't save it on this device.». Doc теперь совпадает и с реализацией, и с фактическим поведением
     хоста — единый контракт.
  4. Смежные doc-комментарии. Проверены все упоминания persist/storage/save в `SpecDesk.GitHub`: enum-doc
     `StorageFailed` (`IGitHubAuth.cs:48–52`), doc класса и комментарий catch в `GitHubDeviceFlowAuth.cs`
     (:6–7, :128), `TokenStore.cs` — все описывают storage-fault как ИСХОД (не throw) и уже были согласованы.
     Иных устаревших комментариев, которые следовало бы поправить той же задачей, не осталось.
  5. Тестовое покрытие StorageFailed-контракта уже есть и достаточно для doc-only правки:
     `GitHubDeviceFlowAuthTests.Authorized_but_a_failed_token_save_reports_storage_failed_and_stays_signed_out`
     (:71–88) заставляет `InMemoryTokenStore.Save` бросить `IOException` (один из ловимых типов) и утверждает
     `Outcome == StorageFailed`, `store.Saved == null`, `IsSignedIn() == false` — ровно тот контракт, что
     документирован. Поведение кода не менялось, новых тестов не требуется.
  6. CHANGELOG. Запись добавлена только в `## [Unreleased]` → `### Fixed` (строки 170–173), точно описывает
     суть (doc заявлял throw; реализация всегда возвращала SignInResult(StorageFailed); doc приведён к
     реальному контракту; вызывающим не нужен неработающий try/catch). Заголовок релиза
     `## [0.1.0] - 2026-07-04` цел (ровно два version-заголовка: строки 8 и 175) — повторяющийся у прошлых
     задач дефект «снос заголовка релиза» не воспроизведён.
  7. Сборка. Правка — только XML-doc; все `<see cref>` (`SignInOutcome.Authorized/Failed/StorageFailed/
     TimedOut/Unreachable`, `SignInResult`, `StartSignInAsync`) резолвятся в реальные члены, поэтому CS1574
     при warnings-as-errors не возникает.
- Новых серьёзных проблем за 2 прогона не найдено; открытых R-записей нет. Задача решена полностью —
  можно переходить к следующей задаче.

### [SUMMARY-R-2026-07-05 20:02] Итог ревью задачи — статус: готово к следующей задаче
- Активная задача: [T-023] Проверить M-10: двойная отмена по таймауту в GitHub-запросах
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Область ревью: изменение в рабочей копии jj (`jj diff -r kttmlwuk`) — 4 файла, ВСЕ вне продакшена:
  тесты `tests/SpecDesk.Host.Tests/HostControllerReviewTests.cs` (+2 теста), фейки
  `FakeGitHubReview.cs` (тоггл `ThrowTimeoutOnOpen`) и `FakeVersioning.cs` (тоггл `ThrowTimeoutOnPush`),
  плюс служебный `.work/status.md`. Продакшен-код и `CHANGELOG.md` не тронуты. Открытых/исправленных/
  отклонённых R-записей на входе не было — Фаза 1 без работы. Наивысший ранее использованный номер — R-004
  (все закрыты/удалены).
- Вывод: вывод исследования подтверждён независимой трассировкой — реального бага нет; все критерии
  готовности выполнены.
  1. Классификация ошибки — нет ветвления по типу исключения (критерий 1). Проверены все точки пути
     send/update/refresh:
     • `RunReviewPublish` (HostController.cs:1332-1370) — единственный `catch (Exception ex)` логирует и
       зовёт `SendError(errorMessage)`, где `errorMessage` — ФИКСИРОВАННАЯ строка параметра callsite'а.
       Двум вызовам переданы ровно те строки, что проверяют тесты: "Couldn't send this for review…"
       (:951-953) и "Couldn't update the review…" (:1076-1078). Никакого различения таймеров.
     • `OnRefreshReviewStatus` (:1211-1216) — `catch (Exception ex)` делает только `LogWarning`, НИКАКОГО
       user-facing сообщения; SendLifecycleStatus зовётся лишь на успехе (`changed`). Классификация к
       пользователю утечь не может по построению.
     • `OpenPullRequestAsync`/`PostGraphQlAsync`/`RequestReviewersAsync` (GitHubReview.cs:136-138,451-452,
       203-205) — каждый строит linked CTS с `CancelAfter(RequestTimeout=30s)`; отменённый
       `_http.SendAsync(…, timeout.Token)` бросает OCE/TCE и пробрасывается. Единственный catch —
       `catch (JsonException)` (:176), ограниченный парсингом; ветка не-2xx бросает HttpRequestException,
       но отменённый send до статус-кода не доходит. Реклассификации нет.
     • `WithAccessTokenAsync` (GitHubDeviceFlowAuth.cs:167-174) — просто зовёт `use(token, ct)`, без catch.
     • Глобальный поиск cancellation-специфичных catch по `src/`: единственные
       `catch (OperationCanceledException) when (…)` — в device-flow SIGN-IN (GitHubDeviceFlowAuth.cs:107,
       DeviceFlowApi.cs:215/322, HostController.cs:1743) — вне scope M-10.
     Итог: какой бы из вложенных таймеров ни сработал (внутренний 30s на запрос ИЛИ внешний 120s
     SendForReviewTimeout, прокинутый в тот же токен), результат — OCE/TCE → тот же широкий catch → та же
     фиксированная строка. Запутанной/неверной классификации не возникает. `_publishInFlight` всегда
     снимается в `finally` (`ClearPublishInFlight`), поэтому таймаут не заклинивает фичу.
  2. Тесты реально воспроизводят сценарий и поймали бы регрессию.
     • Send-тест: `ThrowTimeoutOnOpen` бросает OCE из `OpenPullRequestAsync` ПОСЛЕ успешного push (порядок
       throw в фейке — после HTTP-422-тоггла, до `ReleaseGate`), утверждает точную generic-строку и что
       `LatestStatus().State` остаётся "draft" (транзиентный фрейм "Sending for review…" несёт state=_state
       ="draft", т.к. state продвигается лишь в `TryAdvanceReview` на успехе), затем ретрай с тогглом=false
       доходит до "inReview" — доказывает, что не заклинено.
     • Update-тест: через `BuildInReview`→`SaveAVersion` (seq>shared, гард "nothing new" пройден)
       `ThrowTimeoutOnPush` бросает OCE из `PushBranch` (throw до инкремента `PushBranchCalls`), утверждает
       точную "Couldn't update the review…" и state "inReview", затем ретрай пушит (`PushBranchCalls==2`).
     • Оба теста повторяют УЖЕ ЗЕЛЁНЫЙ паттерн `SendForReview_recovers_after_a_repo_read_fault_and_is_not_
       wedged` (:236-254; тот фолт тоже идёт через background-lambda→catch→SendError→finally), поэтому
       новой флейковости "ретрай vs finally-снятие флага" не вносят. Заявленная авторами
       fail-then-pass-проверка (временно классифицировать cancellation в отдельное сообщение → оба теста
       падают → откат) — корректный мутационный тест: ассерты `Is.EqualTo(точная строка)` сломались бы.
  3. Покрытие достаточно для verify-задачи. Критерий "подтверждено тестом ИЛИ трассировкой" выполнен
     (2 теста + трассировка); неверной классификации не найдено → правки/фикса не требуется; запись в
     CHANGELOG не нужна, т.к. поведение не менялось (в диффе CHANGELOG отсутствует — согласуется с
     критерием). Замеченный НЕ-Critical/High момент (в отдельную R-запись не выносится): третий
     таймаут-сценарий — фоновый refresh (`OnRefreshReviewStatus`, ReviewStatusTimeout 35s + внутренний 30s)
     — новым тестом не покрыт, НО этот путь структурно молчалив (любой фолт → только LogWarning, ни одного
     user-facing фрейма), поэтому мисклассификации К ПОЛЬЗОВАТЕЛЮ там не бывает по построению; молчаливость
     подтверждена инспекцией/трассировкой (это и есть допустимый критерием способ). Отсутствие такого теста
     — не серьёзный зазор.
- Новых серьёзных проблем за 2 прогона не найдено; открытых R-записей нет. Задача решена полностью —
  можно переходить к следующей задаче.

### [SUMMARY-R-2026-07-05 20:15] Итог ревью задачи — статус: готово к следующей задаче
- Активная задача: [T-024] Усилить M-11: хранение токена сверх DPAPI CurrentUser
- Область ревью: изменение в рабочей копии jj `qtpmurlx` (`src/SpecDesk.GitHub/TokenProtector.cs`,
  `tests/SpecDesk.GitHub.Tests/DpapiTokenProtectorTests.cs`, `CHANGELOG.md`, плюс служебный
  `.work/status.md`). Диффы уже закоммиченных, но не запушенных T-022/T-023 в область не входят —
  ревьюился только собственный дифф задачи. Открытых R-записей на входе не было (R-001…R-004 закрыты
  ранее), исправленных/отклонённых записей для перепроверки на Фазе 1 не было.
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Вывод: все критерии готовности выполнены; проверены все пять запрошенных пунктов.
  1. Константа энтропии и вызовы DPAPI корректны. `AppEntropy` — единый `static readonly byte[]`
     (`Encoding.UTF8.GetBytes("SpecDesk.GitHub.TokenStore.v1")`), используемый И в `Protect`, И в основном
     пути `Unprotect`, поэтому пара protect/unprotect гарантированно согласована (round-trip не может
     разъехаться из-за опечатки). Порядок аргументов верен для обеих сигнатур
     (`Protect(userData, optionalEntropy, scope)` / `Unprotect(encryptedData, optionalEntropy, scope)`):
     `Protect(plaintext, AppEntropy, DataProtectionScope.CurrentUser)`,
     `Unprotect(ciphertext, AppEntropy, DataProtectionScope.CurrentUser)`. Scope — `CurrentUser` в обоих
     местах (не изменён). Энтропия не мутируется (только читается), гонок при конкурентных вызовах нет
     (статические методы `ProtectedData` потокобезопасны, массив только читается).
  2. Миграционный фолбэк надёжен и не переоткрывает уязвимость. `catch` перехватывает ИСКЛЮЧИТЕЛЬНО
     `CryptographicException` (узко, не общий `Exception`), только в `Unprotect`; `Protect` не тронут
     try/catch и ВСЕГДА пишет с энтропией — новые токены не сохраняются в слабом формате. Фолбэк на
     no-entropy достижим лишь когда энтропийный unprotect бросил `CryptographicException`, т.е. для
     легаси-токена (сохранён с `optionalEntropy: null` до апгрейда) — тогда фолбэк успешно читает и юзер
     не разлогинивается молча. Для нового токена энтропийный unprotect успешен, фолбэк не выполняется;
     для реально повреждённого/чужого файла фолбэк тоже падает `CryptographicException`, которое
     пробрасывается и ловится узким catch'ем `TokenStore.Load` (`JsonException or CryptographicException
     or IOException or UnauthorizedAccessException`) → «signed out». Фолбэк НЕ ослабляет защиту новых
     токенов: чужой процесс, пробующий no-entropy unprotect против нового шифротекста, всё равно падает —
     это подтверждает тест. Прочие типы исключений (`ArgumentNullException` и т.п.) фолбэком не
     перехватываются и корректно пробрасываются (ciphertext приходит из `File.ReadAllBytes`, не null).
  3. Новые тесты гоняют настоящий Windows DPAPI, не фейк, и поймали бы регрессию. Оба используют
     ПРОДАКШН-класс `DpapiTokenProtector` и прямые статические `ProtectedData`-вызовы; гейт `[Platform("Win")]`
     ограничивает выполнение Windows-legом CI, `[SupportedOSPlatform("windows")]` глушит CA1416 для прямых
     вызовов в тесте. `Protect_uses_app_specific_entropy...` шифрует продакшн-`Protect`'ом и утверждает, что
     no-entropy unprotect бросает `CryptographicException` — если убрать энтропию из `Protect`, no-entropy
     unprotect стал бы успешен и ассерт `Throws` упал бы (авторами проверено fail-then-pass).
     `Unprotect_still_reads_a_token_saved_before_entropy_was_added` строит легаси-шифротекст
     (`optionalEntropy: null`) и проверяет, что продакшн-`Unprotect` его читает через фолбэк — удаление
     фолбэка роняет тест реальным `CryptographicException` (авторами проверено). Доступ к internal-классу
     уже был настроен (прежний round-trip-тест им пользуется).
  4. Других мест с прямым использованием `ProtectedData` в продакшене нет. Глобальный grep по репозиторию:
     единственные продакшн-вызовы — в `TokenProtector.cs`; остальные совпадения — тестовый файл и
     package-reference (`Directory.Packages.props`, `.csproj`). Дополнительной обработки нигде не требуется.
  5. Запись в `CHANGELOG.md` уместна. Новый подзаголовок `### Security` добавлен под `## [Unreleased]`
     (строка 175), после существующего `### Fixed`, перед `## [0.1.0] - 2026-07-04` (строка 181) —
     заголовок релиза 0.1.0 и его контент не тронуты (повторявшийся у прошлых задач дефект «снос заголовка
     релиза» не воспроизведён). `### Security` — стандартная категория Keep a Changelog и правильный выбор
     для hardening-изменения (не «Fixed»); текст точен (энтропия + сохранение существующих сессий через
     фолбэк).
- Замеченный НЕ-Critical/High момент (в отдельную R-запись не выносится, порчи данных не даёт, вне
  критериев задачи): легаси-токен остаётся в формате no-entropy на диске до следующего `Save()` — `Load`
  читает его через фолбэк, но не перешифровывает. Это неотъемлемое свойство мягкой миграции (нельзя
  ретроактивно усилить уже записанные данные без перезаписи), не регрессия (токен и так был no-entropy до
  апгрейда) и задачей перешифровка при загрузке не требовалась. Отдельного упоминания также заслуживает
  комментарий-предупреждение «never change this value once shipped»: смена `AppEntropy` разлогинила бы
  пользователей с токеном под прежней энтропией (фолбэк на null им не поможет) — тестом стабильность
  константы не запинена, но это латентный риск сопровождения, а не дефект текущего диффа; он явно
  задокументирован в коде.
- Новых серьёзных проблем за 2 прогона не найдено; открытых R-записей нет. Задача решена полностью —
  можно переходить к следующей задаче.

### [SUMMARY-F-2026-07-05 20:22] Итог интеграционного ревью — статус: готово к публикации
- Проверено задач (по Tasks_Done.md): 24 (T-001…T-024 — S-01…S-06, S-08, S-09, S-11…S-16, M-01…M-11);
  инкремент этого батча — 3: T-022/T-023/T-024.
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Область: инкремент с прошлого интеграционного ревью (SUMMARY-F 19:22 покрыл T-001…T-021) — фокус на
  трёх новых задачах, все в домене GitHub-auth/token и его тестах: T-022 (M-09 — XML-doc
  `IGitHubAuth.AwaitAuthorizationAsync` приведён к реальному result-контракту StorageFailed, doc-only),
  T-023 (M-10 — подтверждение отсутствия «двойной отмены по таймауту», ТОЛЬКО тесты/фейки, продакшена
  не тронул), T-024 (M-11 — DPAPI `TokenProtector` привязан к app-specific `AppEntropy` с миграционным
  фолбэком). VCS: jj (colocated с git). Диапазон `jj diff --from 5b409cae --to @`. T-022/T-023
  закоммичены (jj `4723295d`, `95d55b4d`), T-024 сидит в рабочей копии (@ `055d5f2c`); служебный
  `0ad8c360` («Reset the checkpoint counter…») трогает только `.work/` — процессный артефакт, не код.
- Вывод: интеграционных проблем не найдено; три изменения согласованно сочетаются, локализованы в
  `SpecDesk.GitHub` (+ его тестах) и не заводят ни одного нового межмодульного/кросс-языкового контракта.
  1. Архитектурная целостность. T-022 — чистая правка XML-doc (0 изменений поведения); T-023 — ноль
     продакшн-строк (2 теста + два opt-in-тоггла в фейках); T-024 — единственная содержательная правка
     кода, полностью внутри `DpapiTokenProtector`. Ни одна не расширяет публичную сигнатуру и не двигает
     границу C#↔F#↔TS (IPC-envelope/DiffKind-контракт не затронуты). Сборка при warnings-as-errors
     чистая → добавленный `using System.Text` и `AppEntropy` резолвятся, `<see cref>` в новом doc-тексте
     (`SignInOutcome.StorageFailed`/`Authorized`/`Failed`/`TimedOut`/`Unreachable`, `StartSignInAsync`)
     валидны (нет CS1574).
  2. Ключевая точка стыковки T-022 ∘ T-024 (обе о пути персистенции токена — взаимная согласованность,
     не конфликт). T-022-doc теперь утверждает, что сбой сохранения токена после успешной авторизации —
     включая «encryption fault» — возвращается как `SignInResult.StorageFailed()`, никогда не бросается.
     T-024 меняет слой шифрования (`Protect` теперь пишет с `AppEntropy`), НО намеренно НЕ оборачивает
     `Protect` в try/catch — поэтому `CryptographicException` из `Protect` по-прежнему пробрасывается в
     `CompleteAuthorizationAsync`, где его ловит `catch (Exception ex) when (ex is IOException or
     UnauthorizedAccessException or CryptographicException)` (:125-126) → `StorageFailed()`. Инвариант
     T-022 (encryption-сбой при Save → StorageFailed, не throw) остаётся точным ПОСЛЕ правки T-024 —
     контракт не разъехался. Миграционный фолбэк T-024 (`catch (CryptographicException)` → повтор с
     `optionalEntropy: null`) живёт исключительно в `Unprotect`, т.е. на пути ЗАГРУЗКИ (`Load`/
     `IsSignedIn`/`GetToken`), а не Save — с StorageFailed-путём T-022 не пересекается. Реально
     повреждённый/чужой файл на Load даёт двойной `CryptographicException` → ловит узкий catch
     `TokenStore.Load` → честный «signed out»; потери данных/маскировки порчи нет.
  3. Регрессии между задачами отсутствуют. (а) Round-trip токена не разъезжается: `Protect` и основной
     путь `Unprotect` используют один `AppEntropy`, поэтому токен, сохранённый в текущей сессии, читается;
     подтверждено — `SpecDesk.GitHub.Tests` 117 passed (было 115: +2 Windows-DPAPI-теста T-024, включая
     проверку, что no-entropy unprotect против нового шифротекста бросает — фолбэк не ослабляет защиту
     новых токенов). (б) T-023-тогглы `ThrowTimeoutOnOpen`/`ThrowTimeoutOnPush` — `bool` по умолчанию
     `false`, поэтому существующие Host-тесты идут прежним путём; `SpecDesk.Host.Tests` 162 passed (было
     160: +2 теста T-023). (в) T-022/T-024 не касаются review-путей T-023, а T-023 не касается токен-
     защиты — три задачи не делят изменяемого состояния, лишь домен.
  4. Согласованность контрактов / полнота относительно критериев `task.md`. T-022: doc↔реализация
     выровнены (StorageFailed возвращается, не бросается), хост опирается на единый result-контракт
     (`HostController` маршрутизирует `result.Outcome`, не ждёт брошенного storage-исключения),
     CHANGELOG-запись есть — все 3 критерия. T-023: «оба таймера → одна фиксированная строка» подтверждено
     трассировкой (единственный `catch (Exception ex)` в `RunReviewPublish` рапортует callsite-строку
     независимо от того, какой таймер сработал; `_publishInFlight` всегда снимается в `finally`) И двумя
     детерминированными тестами, пиннящими точные строки «Couldn't send this for review…»/«Couldn't
     update the review…» + state + не-заклинивание (ретрай доходит до inReview); реальной мисклассификации
     не найдено → фикса не требуется → CHANGELOG корректно отсутствует (поведение не менялось) — все 3
     критерия. T-024: app-specific `optionalEntropy` добавлена в оба вызова DPAPI, существующие round-trip-
     тесты зелёные, CHANGELOG-запись (`### Security`) есть — все 3 критерия.
  5. Сквозные аспекты / техдолг. Обработка исключений соответствует стилю AGENTS.md: новый
     `catch (CryptographicException)` в `Unprotect` узкий (не общий `Exception`) и несёт rationale-
     комментарий (не пустой). Git-лексика в пользовательские строки не протекает (изменения — doc/бэкенд/
     тесты; сообщения ошибок review остаются простоязычными). `CHANGELOG.md`: T-022 добавлен в
     существующий `## [Unreleased] → ### Fixed`, T-024 — в новый `## [Unreleased] → ### Security` (валидная
     Keep-a-Changelog-категория для hardening, после `### Fixed`), заголовок релиза `## [0.1.0] - 2026-07-04`
     и его контент целы — повторяющийся у прошлых задач дефект «снос заголовка релиза» не воспроизведён.
     Новых заглушек/TODO/git-лексики в изменённых областях нет. Ранее зафиксированные reviewer'ом
     НЕ-Critical/High края (SUMMARY-R 19:43/20:02/20:15) в масштабе решения остаются не Critical/High:
     (а) легаси-токен остаётся no-entropy на диске до следующего `Save()` — Load читает через фолбэк, но не
     перешифровывает (неотъемлемое свойство мягкой миграции, не регрессия); (б) `AppEntropy` — «never change
     once shipped»: смена константы разлогинила бы пользователей — латентный риск сопровождения, явно
     задокументирован в коде; (в) фоновый refresh-таймаут (`OnRefreshReviewStatus`, ReviewStatusTimeout)
     новым тестом T-023 не покрыт, но путь структурно молчалив (любой фолт → только LogWarning, ни одного
     user-facing фрейма), поэтому мисклассификации к пользователю там не бывает по построению.
- Проверка сборки/тестов на текущем состоянии (включая незакоммиченный T-024, `@` `055d5f2c`):
  `dotnet build SpecDesk.slnx` — 0 warnings / 0 errors (warnings-as-errors). `dotnet test SpecDesk.slnx`
  — все проекты зелёные: `SpecDesk.Markdown.Tests` 70, `SpecDesk.Contracts.Tests` 7,
  `SpecDesk.Diff.Tests` 33, `SpecDesk.Core.Tests` 97, `SpecDesk.GitHub.Tests` 117 (+2 T-024),
  `SpecDesk.Git.Tests` 32, `SpecDesk.Host.Tests` 162 (+2 T-023) = 518 passed / 0 failed / 0 skipped.
  Webview (`webview/`) этим инкрементом НЕ тронут (правки только в `SpecDesk.GitHub` + Host-тестах) —
  npm-набор вне области инкремента.
- Открытых R-записей нет (все SUMMARY-R по T-022/T-023/T-024 закрыты: 19:43, 20:02, 20:15); открытых
  F-записей нет; последний прогон пуст. Решение согласовано и готово к публикации (чекпоинт с push).

### [SUMMARY-R-2026-07-05 20:37] Итог ревью задачи — статус: готово к следующей задаче
- Активная задача: [T-025] Проверить M-12: Commands.Stage("*") стейджит всё без гарантии .gitignore
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Область ревью: изменение в рабочей копии jj (`jj diff -r nptuzull`): комментарии у двух вызовов
  `Commands.Stage(repo, "*")` в `src/SpecDesk.Git/LibGit2DocumentVersioning.cs` (`Initialize`,
  `SaveVersion`), новый регресс-тест в `tests/SpecDesk.Git.Tests/LibGit2DocumentVersioningTests.cs`,
  запись в `CHANGELOG.md`, плюс служебный `.work/status.md` (артефакт процесса). Продакшн-поведение
  НЕ менялось — задача проверочная («verify»). Открытых/исправленных/отклонённых R-записей на входе
  не было (историческая нумерация закрыта на R-004; следующий свободный — R-005, но серьёзных находок
  нет, поэтому R-записи не заводились).
- Вывод: все критерии готовности выполнены; заявленный вывод «бага нет» подтверждён независимо.
  1. Стейджинг по умолчанию уважает `.gitignore` — ПОДТВЕРЖДЕНО на уровне API и эмпирически.
     Рефлексией по фактически используемой сборке (LibGit2Sharp 0.31.0, `net472`/`net8.0` из NuGet-кэша):
     `StageOptions.IncludeIgnored` в дефолтном экземпляре = `False`; используемый перегруз
     `Commands.Stage(IRepository, string)` (без `StageOptions`) не может включить игнор. Механизм —
     диффовый: `Commands.Stage` строит diff рабочего дерева против индекса с `DiffModifiers.IncludeUntracked`
     (значение 2) и добавляет `DiffModifiers.IncludeIgnored` (значение 16) ТОЛЬКО когда
     `StageOptions.IncludeIgnored == true`; при дефолте игнорируемые пути в diff не попадают → не
     стейджатся. Это ровно git-дефолт (`git add -A`/`git add .` уважают `.gitignore`, нужен `-f` для
     форса), а НЕ LibGit2Sharp-специфичный оверрайд, расходящийся с raw libgit2. Эмпирически: реальный
     регресс-тест прогнан на настоящем нативном libgit2 — `SaveVersion_DoesNotCommitAGitignoredBuildArtifactDirectory`
     PASSED (1/1, 293 ms).
  2. Тест реально задействует git/libgit2 и ловит регрессию, без критичных зазоров.
     Тест пишет НАСТОЯЩИЙ `.gitignore` (`build/\n`) в реальный temp-репо, реально сидит (`Initialize`,
     где `.gitignore` заодно коммитится в seed), заводит рабочую ветку (`BeginEdit`), создаёт реальный
     игнорируемый `build/artifact.bin` ПЛЮС настоящую отслеживаемую правку (`spec.md` → «# Version two»,
     чтобы сейв не был no-op), зовёт реальный `SaveVersion` и проверяет: `result.Committed == true`
     (правка закоммичена) И `ReadHeadContent(repo, "build/artifact.bin") == null` (игнорируемый файл в
     HEAD-дереве отсутствует). Регрессию ловит: при `IncludeIgnored = true` diff получил бы флаг 16,
     игнорируемый файл застейджился бы, `ReadHeadContent` вернул бы непустой контент — ассерт `Is.Null`
     упал бы (совпадает с заявленной fail-then-pass проверкой). Исключение не даёт файлу выпасть по иной
     причине: `build/` создаётся ПОСЛЕ `BeginEdit` (форс-checkout его не трогает), `"*"` матчит всё —
     единственная причина невключения именно `.gitignore`. Изоляция: свежий GUID-темп на `SetUp`,
     очистка на `TearDown` — не флаки. Разобранные задачей потенциальные зазоры (проверить, что сам
     `.gitignore` уцелел/отслеживается; что у УЖЕ отслеживаемого-затем-игнорируемого файла не сбивается
     прежний tracked-контент) КРИТЕРИЯМИ T-025 не требуются: критерии просят лишь «репозиторий с
     игнорируемой build-директорией — SaveVersion её не коммитит», что и покрыто; `.gitignore` де-факто
     закоммичен в seed. Не Critical/High.
  3. Оба код-комментария (и текст CHANGELOG) точны по операционным утверждениям — но с одной мелкой
     неточностью формулировки. Верно и проверено: «`IncludeIgnored` defaults to false»,
     «doesn't force-add ignored paths», «the same default `git add -A` uses». Неточность (мелкая,
     НЕ Critical/High, в отдельную R-запись не выносится): формулировка «libgit2's underlying
     index-add-all» неверно называет механизм — `Commands.Stage` в LibGit2Sharp реализован через
     дифф рабочего дерева↔индекс (`DiffModifiers.IncludeUntracked`), а не через `git_index_add_all`.
     На поведение не влияет, ключевой вывод (уважение `.gitignore`) остаётся верным при любой
     формулировке; это чисто documentation-precision-нюанс.
  4. CHANGELOG корректен, релиз-заголовок цел. Запись добавлена только в `## [Unreleased]` → `### Fixed`
     (строки 174-177); содержание точно по исходу (та же мелкая «index-add-all»-формулировка). Заголовок
     `## [0.1.0] - 2026-07-04` (строка 185) и весь его контент нетронуты — повторяющийся у прошлых задач
     дефект «снос заголовка релиза» НЕ воспроизведён. Мелкий стилевой нюанс (запись под `### Fixed`, хотя
     поведение не менялось) — вне Critical/High; критерии T-025 требовали CHANGELOG-запись безусловно, а
     формулировка «Confirmed … Pinned with a regression test» это отражает.
- Новых серьёзных проблем за 2 прогона не найдено; открытых R-записей нет. Задача решена полностью —
  можно переходить к следующей задаче.

### [SUMMARY-R-2026-07-05 20:41] Итог целевого повторного ревью формулировки — статус: готово к следующей задаче
- Активная задача: [T-025] Проверить M-12: Commands.Stage("*") стейджит всё без гарантии .gitignore
- Прогонов выполнено: 2 (узкое подтверждающее ревью; оба прогона без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Область ревью: ЦЕЛЕВОЕ повторное ревью одной точечной правки формулировки поверх уже чистого ревью
  (SUMMARY-R 20:37), а НЕ полное первичное ревью — поэтому контекст/дифф намеренно у́же первого прохода.
  Инкремент (по `jj evolog -p -r nptuzull`, верхний снапшот) правит РОВНО: одну строку `CHANGELOG.md` и
  комментарий у `Commands.Stage(repo, "*")` в методе `SaveVersion`
  (`src/SpecDesk.Git/LibGit2DocumentVersioning.cs`); плюс служебные `.work/Review.md`/`.work/status.md`.
  Продакшн-логика и тест-код в инкременте НЕ трогались — подтверждает заявление исполнителя «No other
  change was made». Открытых/исправленных/отклонённых R-записей на входе не было.
- Повод: прошлое ревью (20:37, пункт 3) отметило НЕ-Critical documentation-precision-неточность —
  формулировка «index-add-all» неверно называла механизм (`Commands.Stage` в LibGit2Sharp реализован
  через дифф рабочего дерева↔индекс с `DiffModifiers.IncludeUntracked`, а не `git_index_add_all`).
- Вывод: правка формулировки корректна и НЕ вносит новой неточности.
  1. `CHANGELOG.md` (строка 175): было «(libgit2's index-add-all doesn't force-add ignored paths unless
     explicitly told to)» → стало «(`StageOptions.IncludeIgnored` defaults to false, the same default
     `git add -A` uses)». «index-add-all» убран; новая формулировка точна и проверена (дефолт
     `IncludeIgnored=false`; `git add -A` уважает `.gitignore`).
  2. Комментарий `SaveVersion` (строки 142-145): исходно «index-add-all» НЕ содержал; реворд ДОБАВИЛ
     явное и точное описание механизма — «LibGit2Sharp's Commands.Stage only includes ignored paths in
     the diff it stages from when that's set». Совпадает с ранее (20:37) верифицированным рефлексией
     поведением: `DiffModifiers.IncludeIgnored` добавляется в дифф ТОЛЬКО при `StageOptions.IncludeIgnored
     == true`; при дефолте игнорируемые пути в дифф не попадают → не стейджатся. Ключевой вывод (уважение
     `.gitignore` по умолчанию) остаётся верным. Новой неточности нет.
  3. Комментарий `Initialize` (строки 55-56) не трогался — он «index-add-all» и так не содержал, точен.
- Оставшийся минорный нюанс (НЕ Critical/High, в отдельную R-запись НЕ выносится): комментарий над тестом
  `tests/SpecDesk.Git.Tests/LibGit2DocumentVersioningTests.cs:166-169` ВСЁ ЕЩЁ содержит исходную
  формулировку «libgit2's underlying index-add-all» — реворд её не затронул (исполнитель уточнял только
  комментарий `SaveVersion` и `CHANGELOG`). Это ровно та же documentation-precision-неточность из пункта 3
  ревью 20:37: на поведение/сборку не влияет (комментарий; markdown/comment не входят в компиляцию),
  ключевой вывод корректен; отмечено здесь, чтобы будущий читатель знал, что фраза сохранилась именно в
  тест-комментарии.
- Новых серьёзных проблем не найдено; открытых R-записей нет. Целевая правка формулировки принята —
  можно переходить к следующей задаче.

### [SUMMARY-R-2026-07-05 20:45] Итог второго целевого повторного ревью — статус: готово к следующей задаче
- Активная задача: [T-025] Проверить M-12: Commands.Stage("*") стейджит всё без гарантии .gitignore
- Прогонов выполнено: 1 подтверждающий проход (ВТОРОЕ, ещё более узкое целевое повторное ревью:
  закрытие единственного оставшегося минорного нюанса из SUMMARY-R 20:41 — краткий подтверждающий
  проход уместен, а не полное перечитывание).
- Открытых проблем: 0
- Область ревью: РОВНО одна правка формулировки в комментарии над тестом
  `tests/SpecDesk.Git.Tests/LibGit2DocumentVersioningTests.cs:166-169`. Инкремент подтверждён по
  `jj evolog -p -r nptuzull` (верхний снапшот 20:45 vs предыдущий 20:39): среди код/тест/CHANGELOG-файлов
  изменён ТОЛЬКО этот тест-комментарий; `src/SpecDesk.Git/LibGit2DocumentVersioning.cs` и `CHANGELOG.md`
  в инкременте НЕ трогались (остальное — служебные `.work/Review.md`/`.work/status.md`). Это подтверждает
  заявление исполнителя «No other change was made anywhere».
- Повод: прошлое ревью (20:41) отметило единственный оставшийся минорный нюанс — тест-комментарий всё ещё
  содержал неточную формулировку «libgit2's underlying index-add-all» (реальный механизм `Commands.Stage`
  диффовый, а не `git_index_add_all`). В отдельную R-запись он не выносился (НЕ Critical/High).
- Вывод: правка корректна, новой неточности не вносит, нюанс закрыт.
  1. Диф правки: было «…stages everything matching the pathspec, but libgit2's underlying index-add-all
     does NOT force-add ignored paths…» → стало «…stages everything matching the pathspec, but it does
     NOT force-add ignored paths…». Единственное отличие — неточная фраза «libgit2's underlying
     index-add-all» заменена на нейтральное «it» (референт — `Commands.Stage(repo, "*")`). Остальной текст
     комментария (StageOptions.IncludeIgnored=false, «same default behaviour as plain `git add -A`»,
     утверждение про build-artifact-директорию) идентичен и остаётся точным.
  2. Новая формулировка точна: механизм намеренно не называется вовсе (ни «index-add-all», ни «diff-based»),
     поэтому источник прежней неточности устранён без введения нового спорного имени. «it does NOT
     force-add ignored paths unless explicitly told to (StageOptions.IncludeIgnored…)» верно: дефолт
     `IncludeIgnored=false`, `git add -A` уважает `.gitignore` — совпадает с ранее верифицированным
     рефлексией поведением (SUMMARY-R 20:37/20:41). Ключевой вывод (стейджинг по умолчанию уважает
     `.gitignore`) остаётся корректным.
  3. Фраза «index-add-all» полностью выведена из кодовой базы: grep по `CHANGELOG.md`,
     `src/SpecDesk.Git/LibGit2DocumentVersioning.cs` и `tests/.../LibGit2DocumentVersioningTests.cs` — 0
     совпадений. Комментарий `SaveVersion` (142-145) и `Initialize` (55-56), а также CHANGELOG (строка 175)
     её не содержат и остаются точными.
  4. Комментарий не входит в компиляцию — сборочный статус не затрагивается; заявление исполнителя о
     чистой сборке (0 warnings) правдоподобно и с этой правкой не могло измениться.
- Новых серьёзных проблем не найдено; открытых R-записей нет. Последний оставшийся минорный нюанс закрыт —
  можно переходить к следующей задаче.

### [SUMMARY-R-2026-07-05 21:07] Итог ревью задачи — статус: готово к следующей задаче
- Активная задача: [T-026] Исправить M-13: TryAdvanceReview ключуется по (state, имя ветки), неверно
  штампует пересозданный одноимённый черновик
- Прогонов выполнено: 2 (оба подряд без новых серьёзных находок → остановка)
- Открытых проблем: 0
- Область ревью: T-026-часть изменения в рабочей копии jj (`jj diff -r nptuzull`) —
  `src/SpecDesk.Host/HostController.cs` (проброс `generation` через
  `OnSendForReview`/`OnUpdateReview`/`RunReviewPublish`/`TryAdvanceReview`), новый регресс-тест
  `SendForReview_does_not_stamp_a_same_named_draft_recreated_while_the_old_push_is_still_resolving`
  (`tests/SpecDesk.Host.Tests/HostControllerReviewTests.cs`) и запись в `CHANGELOG.md` (строки 178-186).
  T-025-часть того же коммита (`LibGit2DocumentVersioning.cs`, его тест, M-12 CHANGELOG) уже была
  отревьюирована и закрыта ранее (SUMMARY-R 20:37/20:41/20:45) — в область данного ревью не входит.
  Открытых/исправленных/отклонённых R-записей на входе не было (историческая нумерация закрыта на R-004).
- Вывод: все критерии готовности выполнены; независимо подтверждены все пять запрошенных пунктов.
  1. Репродукция реальна и достижима. `LoadFile` (:1832) безусловно сбрасывает
     `_state`→Published/`_branch`→null/`_versionsSaved`/`_versionsShared`→0 БЕЗ проверки
     `_publishInFlight` — в отличие от `OnDiscard` (:603), который явно отказывается, пока публикация в
     полёте. `LoadFile` достижима во время публикации через `OnReady` (:349, повторный `Ready`) и
     `OnOpen` (:449). Критично: `LoadFile` НЕ бампает `_draftGeneration` (:1868-1870), а последующий
     `OnEdit`→`BeginEdit` бампает его (:541). По `Lifecycle.fs` переход `Draft→Edit` ОТСУТСТВУЕТ (есть
     только `Published→Edit`), поэтому пересоздать одноимённый draft со `_state=draft`/`_branch=X` во
     время in-flight send можно ТОЛЬКО через сброс (LoadFile/Discard→Published) + BeginEdit — а он
     всегда сдвигает generation. Баг подлинный, не предотвращён иным образом.
  2. Проброс `generation` корректен, новой гонки нет. Захват `generation = Interlocked.Read(ref
     _draftGeneration)` идёт в ТОЙ ЖЕ `lock(_sync)`-секции, что и `fromState`/`branch`/`seq`
     (OnSendForReview :930, OnUpdateReview :1055); `TryAdvanceReview` читает `_state`/`_branch`/живой
     `_draftGeneration` вместе под одним `lock(_sync)` (:1407). Торн-пары `_textGeneration`/
     `_draftGeneration` (из поля-комментария :100-137) здесь НЕ возникает: она мотивирована тем, что
     `RunDiskAutosave` снимает снимок на ФОНОВОМ таймер-потоке в окне «бамп под _repoGate ↔ _text
     догоняет под поздним _sync»; `OnSendForReview`/`OnUpdateReview` же исполняются на однопоточном
     серийном насосе IPC (как и `OnEdit`/`OnDiscard`/`LoadFile` — единственные писатели
     `_state`/`_branch`/`_draftGeneration`), поэтому наблюдают всегда согласованный кортеж, а
     межлоковое окно OnEdit/OnDiscard для них недостижимо. `_draftGeneration` монотонен (только
     Interlocked.Increment, без сброса), поэтому сравнение снимок↔живое значение точно: ложное
     совпадение после реального чекаута невозможно (значение не возвращается), ложное расхождение без
     чекаута невозможно. Любое расхождение смещает строго к «spurious not-advancing» (безопасная
     сторона, согласно документированной философии метода — недосчёт, не пересчёт); пути к
     ложному ПРОДВИЖЕНИЮ чужого черновика нет.
  3. Регресс-тест реально воспроизводит баг, без мок-обхода. Использует настоящий `FakeGitHubReview.
     ReleaseGate` (блокировка на `OpenPullRequestAsync` после `PushBranch`, `review.Calls==1`
     подтверждает вход в PR-фазу с уже освобождённым `_repoGate`), настоящий повторный `Ready`→`LoadFile`
     (Build задаёт `_initialDocPath=_docPath`, файл существует), `FakeVersioning.BeginEdit` эхует
     переданное имя `spec/reused` (:112), поэтому `_branch` совпадает оба раза. Проверка fail-then-pass
     обоснована трассировкой: без generation-проверки стейл-push видит `_state==draft &&
     _branch==spec/reused` → продвигает в `inReview` и штампует `_versionsShared=1` → assert на
     `LatestStatus().State == "draft"` (:341) падает «Expected draft, But was inReview» (ровно как
     заявлено); с фиксом `generation` G1≠G2 → не продвигает. Дополнительно вторая половина (собственный
     send пересозданного draft доходит до `review.Calls==2` и `inReview`) отсекает false negative.
     Прогнан локально: `dotnet test --filter …` → Passed 1/0/0; сборка чистая при warnings-as-errors.
  4. Иных вызывающих не пропущено. `TryAdvanceReview` — единственный вызов из `RunReviewPublish`
     (:1350); `RunReviewPublish` — ровно два вызова (`OnSendForReview` :957, `OnUpdateReview` :1086),
     оба теперь передают `generation`. Проверено grep'ом по `src`.
  5. CHANGELOG точен, релиз-заголовок цел. Запись (:178-186) под `## [Unreleased]` → `### Fixed`, верно
     описывает и путь через `LoadFile` (переоткрытие без проверки publish-in-flight, в отличие от
     Discard), и суть фикса (сравнение per-checkout generation counter). Заголовок `## [0.1.0] -
     2026-07-04` (:194) и его контент нетронуты — повторяющийся у прошлых задач дефект «снос заголовка
     релиза» НЕ воспроизведён.
- Замеченный НЕ-Critical/High нюанс (в отдельную R-запись не выносится): комментарий
  `TryAdvanceReview` (:1400-1401) и CHANGELOG формулируют бамп как «discard+recreate всегда бампает
  минимум дважды», тогда как фактический путь репродукции (LoadFile+recreate) бампает ОДИН раз (через
  BeginEdit пересоздания). Фикс корректен и при одном бампе (монотонный счётчик), а оба текста тут же
  явно описывают именно LoadFile-путь, поэтому это documentation-precision-нюанс без влияния на
  поведение.
- Новых серьёзных проблем за 2 прогона не найдено; открытых R-записей нет. Задача решена полностью —
  можно переходить к следующей задаче.

### [SUMMARY-R-2026-07-05 21:10] Итог узкого целевого повторного ревью формулировки — статус: готово к следующей задаче
- Активная задача: [T-026] Исправить M-13: TryAdvanceReview ключуется по (state, имя ветки), неверно
  штампует пересозданный одноимённый черновик
- Прогонов выполнено: 1 подтверждающий проход (УЗКОЕ целевое повторное ревью поверх уже чистого
  ревью SUMMARY-R 21:07 — краткий подтверждающий проход уместен, а не полное перечитывание).
- Открытых проблем: 0
- Область ревью: РОВНО одна правка формулировки комментария `TryAdvanceReview`
  (`src/SpecDesk.Host/HostController.cs`), закрывающая единственный НЕ-Critical нюанс
  documentation-precision из SUMMARY-R 21:07. Инкремент подтверждён по `jj evolog -p -r nptuzull`
  (верхний снапшот 21:10:09, операция 9e2a886e): среди код/тест/CHANGELOG-файлов изменён ТОЛЬКО
  комментарий-блок `HostController.cs` (:1395-1403); продакшн-логика, регресс-тест и `CHANGELOG.md`
  в инкременте НЕ трогались — подтверждает заявление исполнителя «No other change was made anywhere».
  Остальное в снапшоте — служебные `.work/Review.md`/`.work/status.md`. Открытых/исправленных/
  отклонённых R-записей на входе не было (историческая нумерация закрыта на R-004).
- Повод: прошлое ревью (21:07) отметило НЕ-Critical documentation-precision-нюанс — комментарий
  `TryAdvanceReview` формулировал бамп как «a discard+recreate always bumps it at least twice»,
  тогда как фактический путь репродукции (переоткрытие через дубль-Ready → `LoadFile`, затем свежий
  `BeginEdit`) бампает `_draftGeneration` РОВНО ОДИН раз (сам `LoadFile` не бампает; бампает только
  последующий `BeginEdit`).
- Вывод: правка корректна, точна и НЕ вносит новой неточности; нюанс закрыт.
  1. Диф правки (две фразы одного комментария):
     а) мотивирующий пример «discarded and recreated while this push was in flight» →
        «recreated while this push was in flight (e.g. via a reload — LoadFile resets the draft
        fields without checking _publishInFlight, unlike Discard — followed by a fresh BeginEdit)»;
     б) гарантия «a discard+recreate always bumps it at least twice» →
        «any recreation bumps it at least once more».
     Строка-зачин («(_state, _branch) alone can't tell "the same draft, untouched" from "a
     same-named draft that was …») и остальной текст комментария не менялись.
  2. Новая формулировка (б) точна и совпадает с гарантией, на которую реально опирается фикс.
     Пересоздать одноимённый draft со `_state=draft` можно ТОЛЬКО через сброс к Published
     (`LoadFile`/`Discard`) + `BeginEdit` (в `Lifecycle.fs` нет перехода `Draft→Edit`, только
     `Published→Edit`), а `BeginEdit` всегда бампает `_draftGeneration` (после успешного
     чекаута, под `_repoGate`). Значит любое пересоздание сдвигает счётчик минимум на 1 (путь
     LoadFile — ровно 1, путь Discard — 2). Счётчик монотонен (только `Interlocked.Increment`),
     поэтому снимок, снятый старым push ДО пересоздания, не может совпасть с живым значением —
     ровно проверка `Interlocked.Read(ref _draftGeneration) != generation` в `TryAdvanceReview`.
     «At least once more» — точная нижняя граница; прежнее «at least twice» её завышало для
     LoadFile-пути. Точность гарантии, на которую опирается фикс, восстановлена.
  3. Новая формулировка (а) точна: `LoadFile` действительно безусловно сбрасывает draft-поля
     без проверки `_publishInFlight` (в отличие от `Discard`, который отказывается), а
     последующий `BeginEdit` пересоздаёт draft — это и есть путь, воспроизводимый регресс-тестом.
     Параграф теперь описывает именно reload+BeginEdit как мотивирующий пример, а не только
     «discarded and recreated». Новой неточности не введено.
  4. Парентеза «which bumps on every BeginEdit/Discard checkout change» не менялась и остаётся
     верной (bump в `OnEdit` после `BeginEdit` и в `OnDiscard` после `Discard`, оба под `_repoGate`).
  5. Правка — только комментарий, в компиляцию не входит: сборочный статус не затрагивается,
     заявление исполнителя о чистой сборке (0 warnings) с этой правкой измениться не могло.
  6. `CHANGELOG.md` (:185-186) не трогался и остаётся точным: формулировка «which a discard/recreate
     always advances» не заявляет счётчик «дважды», а лишь монотонный сдвиг («always advances» ≥ 1),
     что корректно и для LoadFile-пути; правки не требовала.
- Новых серьёзных проблем не найдено; открытых R-записей нет. Единственный оставшийся нюанс
  documentation-precision из SUMMARY-R 21:07 закрыт — можно переходить к следующей задаче.
