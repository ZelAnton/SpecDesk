# Завершённые задачи

# Активная задача

Статус: выполнена
Исходная задача: [T-001] Исправить S-01: PushBranch сообщает об успехе при отклонённом push

Оценка сложности: высокая
Рекомендуемый effort: high

`src/SpecDesk.Git/LibGit2DocumentVersioning.cs:229-248`. `PushOptions` задаёт `CredentialsProvider` и
`OnPushTransferProgress`, но не `OnPushStatusError` — LibGit2Sharp сообщает об отклонении конкретного
ref (non-fast-forward, защищённая ветка, отклонивший pre-receive hook) только через этот колбэк, а
`Network.Push` при этом возвращается как обычно. В результате при коллизии одноимённых веток черновика
(имена по умолчанию детерминированы по дате) push на сервере отклоняется, но `PushBranch` возвращает
успех, и `HostController` (:840, :954) сообщает "Отправлено/обновлено на проверку" и продвигает
жизненный цикл, хотя ревьюер так и не получил коммиты. Интерфейсная документация (`GitPublishing.cs:30-31`)
прямо утверждает, что метод бросает исключение при неудаче.

Критерии готовности:
- В `PushOptions`, передаваемых в `Network.Push`, задан `OnPushStatusError` (или иной способ проверить
  результат push) — отклонение любого ref обнаруживается.
- При отклонённом push `PushBranch` выбрасывает исключение, а не возвращается штатно.
- Комментарий в `GitPublishing.cs:30-31` ("throws on failure") соответствует реальному поведению.
- Добавлен тест, воспроизводящий отклонённый push (текущий тест пушит только в свежий локальный bare-репозиторий).
- Запись в `CHANGELOG.md` под `## [Unreleased]`.

# Активная задача

Статус: выполнена
Исходная задача: [T-002] Исправить S-02: не-GitHub push-эндпоинт получает Windows SSO-креды

Оценка сложности: средняя
Рекомендуемый effort: high

`src/SpecDesk.Git/LibGit2DocumentVersioning.cs:238-241`. Колбэк credentials возвращает
`new DefaultCredentials()` для любого URL, кроме `github.com` — это `GIT_CREDENTIAL_DEFAULT` (Windows
Negotiate/NTLM текущего пользователя), что прямо противоречит соседнему комментарию ("gets no
credential"). Если `.git/config` `pushurl` репозитория (полученного, например, как zip/расшаренная
папка) указывает на хост атакующего, "Отправить на проверку" инициирует NTLM-хэндшейк от имени автора —
NetNTLMv2 challenge/response может быть перехвачен или релеен.

Критерии готовности:
- Колбэк credentials возвращает `null` (или бросает исключение) вместо `DefaultCredentials()` для
  не-GitHub URL.
- Push на не-GitHub URL корректно и безопасно завершается неудачей (NTLM-хэндшейк не инициируется).
- Поведение push на GitHub не изменилось.
- Добавлен тест: push на не-GitHub URL безопасно проваливается.
- Комментарий у соответствующего кода приведён в соответствие с реальным поведением.
- Запись в `CHANGELOG.md`.

# Активная задача

Статус: выполнена
Исходная задача: [T-003] Исправить S-03: принудительный checkout в BeginEdit уничтожает автосохранённую работу другого документа

Оценка сложности: высокая
Рекомендуемый effort: high

`src/SpecDesk.Git/LibGit2DocumentVersioning.cs:101-108` совместно с
`src/SpecDesk.Host/HostController.cs:1674-1685`. Принудительный checkout сбрасывает всё рабочее дерево
целиком, а не только редактируемый документ. Сценарий: черновик A, автосохранение пишет
несохранённые-в-версию правки на диск ("Сохранено" в UI, но без "Сохранить версию"); открывается
документ B (хост сбрасывает состояние на "Опубликовано", оставляя ветку A checked out и грязной); клик
"Редактировать" на B → принудительный checkout сбрасывает файл A к его закоммиченному состоянию, а
буфер редактора с текстом A не сохраняется нигде — работа теряется безвозвратно. Это противоречит
обещанию продукта "Сохранено (непрерывно, автоматически)".

Критерии готовности:
- Checkout, инициированный "Редактировать", отказывает или безопасно обрабатывает случай, когда рабочее
  дерево содержит незакоммиченные изменения, принадлежащие другому документу/ветке, чем редактируемый.
- Автосохранённый буфер черновика A не теряется молча при открытии "Редактировать" на документе B.
- Добавлен тест, воспроизводящий сценарий: автосохранение A грязное → открыть B → Редактировать B →
  содержимое A либо сохраняется, либо редактирование безопасно блокируется с понятным сообщением.
- Запись в `CHANGELOG.md`.

# Активная задача

Статус: выполнена
Исходная задача: [T-004] Исправить S-04: запрос app:// с закодированным NUL валит процесс

Оценка сложности: средняя
Рекомендуемый effort: high

`src/SpecDesk.Host/AppAssetResolver.cs:34` декодирует `uri.AbsolutePath` (`%00` → `\0`), а `:51`
передаёт результат в `Path.GetFullPath`, который бросает `ArgumentException` на встроенном null.
`Program.cs:100-123` (`ServeAsset`) перехватывает только `IOException`/`UnauthorizedAccessException`,
несмотря на собственный комментарий "must never let an exception escape into the message pump".
Спецификация с `![x](a%00.png)` при рендере вызывает запрос `app://repo/a%00.png` — исключение проходит
через обратный P/Invoke-колбэк WebView2 и валит процесс. Это DoS из содержимого документа, приходящего
извне (например, из шаренного репозитория).

Критерии готовности:
- `ServeAsset` (или `AppAssetResolver`) перехватывает/предотвращает некорректные символы в пути
  (встроенный NUL и т.п.), а не только `IOException`/`UnauthorizedAccessException`.
- Запрос `app://repo/a%00.png` возвращает безопасный ответ об ошибке, а не валит процесс.
- Добавлен тест на некорректные символы пути и тест "ServeAsset никогда не бросает исключение наружу".
- Запись в `CHANGELOG.md`.

# Активная задача

Статус: выполнена
Исходная задача: [T-005] Исправить S-05: автосохранение не перепроверяется под _repoGate — может воскресить отменённый черновик

Оценка сложности: высокая
Рекомендуемый effort: xhigh

## Критерии выполнения
- Запись автосохранения (`RunDiskAutosave`, `HostController.cs`) повторно проверяет идентичность
  черновика уже внутри `_repoGate`, непосредственно перед `File.WriteAllText`: если черновик успел
  смениться/отмениться (`OnDiscard`, `LoadFile`, начало новой правки, смена документа), запись не
  выполняется.
- Проверка не нарушает установленный порядок блокировок: `_sync` нельзя брать, удерживая `_repoGate`
  (комментарий у `_repoGate`, ~строка 86-93). Значит токен поколения читается без захвата `_sync` —
  через lock-free чтение (`volatile`/`Interlocked`), а не под `_sync` внутри `_repoGate`.
- Воспроизведённый сценарий (колбэк автосохранения «в полёте» захватывает `_repoGate` после того,
  как «Отменить» уже откатил базу и удалил ветку) больше не оставляет отменённое содержимое как
  грязную рабочую копию: файл на опубликованной ветке соответствует откаченному состоянию, а не
  тексту черновика.
- Добавлен регрессионный тест, детерминированно воспроизводящий гонку (в стиле уже имеющихся гейтов
  `FakeVersioning.SaveGate`/`PushGate` — держим `_repoGate` во время `Discard`, освобождаем колбэк
  автосохранения) и падающий без исправления.
- Запись в `CHANGELOG.md` под `## [Unreleased]`.
- `dotnet build` и `dotnet test` проходят (warnings-as-errors).

## План выполнения
- [x] Этап 1: Ввести монотонный токен поколения черновика (например `_draftGeneration`) с lock-free
      доступом (`Interlocked`/`volatile`), инкрементируемый при каждой смене идентичности черновика:
      начало правки, открытие/загрузка документа (`LoadFile`), `OnDiscard`, сброс в Published. Убедиться,
      что все точки смены состояния черновика бампают токен (свериться с местами, где сейчас
      `_autosaveTimer` сбрасывается и где меняется `_state`/`_branch`).
- [x] Этап 2: В `RunDiskAutosave` захватывать значение токена в снимок под `_sync` (вместе с `text`/`path`),
      а внутри `_repoGate`, непосредственно перед `File.WriteAllText`, сравнивать захваченный токен с
      текущим значением (lock-free чтение, без `_sync`); при расхождении — пропустить запись и залогировать.
      Проверить, что `CancelAutosave`/`OnDiscard` бампают токен ДО отката, чтобы «в полёте» колбэк увидел
      расхождение.
- [x] Этап 3: Добавить регрессионный тест в `HostControllerLifecycleTests.cs`: удержать `_repoGate` во
      время `Discard` (через новый/существующий гейт в `FakeVersioning`), дать колбэку автосохранения
      пройти проверку состояния и встать в очередь на gate, затем отпустить Discard первым и убедиться,
      что после завершения колбэка на диск НЕ записан текст отменённого черновика. Тест должен падать до
      исправления и проходить после.
- [x] Этап 4: Добавить запись в `CHANGELOG.md` под `## [Unreleased]` и прогнать `dotnet build`/`dotnet test`.

## Описание
`src/SpecDesk.Host/HostController.cs:603-646` (`RunDiskAutosave`) снимает снимок `(path,text)` под
`_sync`, затем пишет под `_repoGate` без повторной проверки; `OnDiscard` отменяет таймер, но
`Timer.Dispose()` не дожидается уже выполняющегося колбэка, а мониторы .NET не гарантируют порядок
FIFO. Сценарий: вставка изображения держит `_repoGate`; сработавший колбэк автосохранения проходит
проверку состояния и встаёт в очередь на gate; "Отменить" тоже встаёт в очередь и захватывает gate
первым (принудительный checkout базы + удаление ветки), после чего колбэк дописывает текст черновика
поверх откаченного файла — "отменённое" содержимое остаётся грязной рабочей копией на *опубликованной*
ветке, хотя UI показывает "Опубликовано".

Критерии готовности:
- Запись автосохранения повторно проверяет идентичность черновика (токен поколения или связка
  состояние+путь) уже внутри `_repoGate`, непосредственно перед записью — либо `CancelAutosave`
  синхронно дожидается завершения уже выполняющегося колбэка через `Timer.Dispose(WaitHandle)` до того,
  как продолжится "Отменить".
- Воспроизведённый сценарий (автосохранение "в полёте" во время "Отменить") больше не оставляет
  отменённое содержимое как грязную рабочую копию на опубликованной ветке.
- Добавлен регрессионный тест.
- Запись в `CHANGELOG.md`.

# Активная задача

Статус: выполнена
Исходная задача: [T-006] Исправить S-06: гейт жизненного цикла в OnDiscard не атомарен с проверкой publish-in-flight

Оценка сложности: высокая
Рекомендуемый effort: high

`src/SpecDesk.Host/HostController.cs:509` вычисляет `Lifecycle.tryStep(_state,"discard")` из
незаблокированного чтения; `lock(_sync)` на `:517-530` проверяет только `_publishInFlight`. В
`RunReviewPublish`, `TryAdvanceReview`, `SendLifecycleStatus` и `ClearPublishInFlight` (:1191-1222) —
три отдельных захвата `_sync`, поэтому есть окно, когда состояние уже `InReview`, а флаг уже снят;
"Отменить" с устаревшим чтением, увидевшим "Черновик", удаляет локальную ветку, на которой только что
открылся PR. `OnSendForReview`/`OnUpdateReview` уже атомарно проверяют переход состояния и захватывают
флаг под одной блокировкой — `OnDiscard` не следует этой дисциплине.

Критерии готовности:
- `tryStep` для "discard" вычисляется в том же блоке `_sync`, что и проверка `_publishInFlight` (по
  образцу `OnSendForReview`/`OnUpdateReview`).
- "Отменить", гонящееся с только что открывшимся PR (в момент завершения "Отправить на проверку"),
  больше не может удалить локальную ветку, за которой стоит этот PR.
- Добавлен регрессионный/гоночный тест (либо целевой unit-тест на атомарность проверки).
- Запись в `CHANGELOG.md`.

# Активная задача

Статус: выполнена
Исходная задача: [T-007] Исправить S-08: семантический diff не видит правки чекбоксов, сносок и списков определений

Оценка сложности: высокая
Рекомендуемый effort: high

`src/SpecDesk.Markdown/Projection.fs:14-30, 41-66`. `Pipeline.fs` включает `UseTaskLists`,
`UseFootnotes`, `UseDefinitionLists`, но инлайны `TaskList`, `FootnoteLink`/`FootnoteGroup` и блоки
`DefinitionList` попадают в ветку `| _ -> None` — для них нет отдельных случаев проекции. Из-за этого
переключение `- [ ] x` → `- [x] x` проецируется в одинаковый `ListBlock` → оверлей "Показать изменения"
(единственный потребитель этих данных) сообщает "изменений нет" для реальной, отрендеренной правки;
правка тела сноски или термина/определения также невидима для diff.

Критерии готовности:
- `Projection.fs` получает случаи проекции для инлайнов `TaskList`, `FootnoteLink`/`FootnoteGroup` и
  блоков `DefinitionList` (расширение DU `Ast`, либо отображение состояния задачи на элемент списка) —
  вместо падения в `| _ -> None`.
- Переключение чекбокса задачи детектируется diff'ом как `Changed`, а не как отсутствие изменений.
- Правка тела сноски и термина/определения списка определений детектируется diff'ом.
- Добавлены тесты diff на task-list, сноски и списки определений.
- Запись в `CHANGELOG.md`.

# Активная задача

Статус: выполнена
Исходная задача: [T-008] Исправить S-09: некорректный {date:FMT} в .spectool.toml молча отключает "Редактировать"

Оценка сложности: средняя
Рекомендуемый effort: high

`src/SpecDesk.Core/Tokens.fs:32` (`ctx.Date.ToString(fmt, …)`) вызывается из
`WorkflowConfig.expandOrDefault` (`:63-68`), которая не обёрнута в `try/with` (только чтение TOML
обёрнуто). Например, `[branch] pattern = "spec/{docSlug}-{date:q}"` даёт `FormatException`, которое
проходит сквозь `branchNameForHost`/`commitMessageForHost`; catch в `OnEdit` фильтрует только
`LibGit2SharpException|InvalidOperationException`, поэтому "Редактировать" молча ничего не делает, а
`OnSuggestVersionNote`/`OnSuggestBranchName` вообще не отвечают (webview выжидает свои 30 секунд
таймаута). Подслучай: `{date:}` (пустой формат) разворачивается через "G" в
"07/04/2026 09:30:00 +00:00" — пробелы/двоеточия недопустимы в git-ref, но `expandOrDefault` это
пропускает (проверяет только остаточные `{` / пустую строку) → каждое "Редактировать" падает с общей
ошибкой. Это противоречит собственной документации модуля "invalid config must never break the
workflow".

Критерии готовности:
- Разворачивание `{date:FMT}` в `expandOrDefault` обёрнуто в такой же guard, какой уже есть в
  `ImageEngine.insertForHost`.
- Развёрнутый ref валидируется на допустимость символов git-ref (отклоняет пробелы/двоеточия и т.п.,
  включая случай пустого `{date:}`) до принятия; при ошибке — откат на паттерн по умолчанию.
- `OnEdit`/`OnSuggestVersionNote`/`OnSuggestBranchName` больше не молча зависают/no-op'ают на
  некорректном паттерне.
- Добавлены тесты на некорректный `{date:FMT}` и на `{date:}` (пустой формат).
- Запись в `CHANGELOG.md`.

# Активная задача

Статус: выполнена
Исходная задача: [T-009] Исправить S-13: ведущая пустая строка ломает выравнивание блок↔нода 1:1 (делать перед S-11/S-12)

Оценка сложности: высокая
Рекомендуемый effort: xhigh

## Критерии выполнения
- В `md-blocks.ts` ведущий контент (пустые строки и/или ведущие reference-definitions
  до первого top-level токена) больше не образует отдельный синтетический блок:
  для документа хотя бы с одним блоком `splitTopLevelBlocks(md).length` равно числу
  top-level токенов, то есть `blocks.length === originalDoc.childCount`. Инвариант
  `joinBlocks(splitTopLevelBlocks(md)) === md` сохранён для всех существующих и новых
  фикстур (включая пустой/whitespace-only документ).
- `serializeWithSplice(original, edited)` для документа с ведущей пустой строкой или
  ведущим reference-definition при отсутствии реальных правок возвращает `original`
  байт-в-байт (больше не срабатывает whole-document fallback по несовпадению
  `childCount !== blocks.length`).
- При правке первого блока такого документа его ведущий контент (пустые строки /
  reference-definitions перед нодой) сохраняется дословно — симметрично уже
  существующему сохранению хвоста (`contentLineEnd`/`tailLines`).
- Переключение "Форматированный" → "Код" (`index.ts` applyMode) для неизменённого
  документа с ведущей пустой строкой не переразбирает и не меняет текст: `formatted.getText()`
  на незатронутом документе байт-идентичен исходнику.
- В `formatted.ts` маппинг `blocks[i]` ↔ `doc.child(i)` для документов с ведущим
  контентом совпадает 1:1: подсветка активного блока, hover и scroll-sync указывают на
  правильную ноду (нет смещения на один блок).
- Добавлены фикстуры и тесты: документ с ведущей пустой строкой и документ с ведущим
  reference-definition, прогнанные через md-blocks (round-trip + число блоков),
  md-splice (no-op байт-идентичность + правка первого блока сохраняет ведущий контент)
  и formatted (маппинг подсветки/строк + getText неизменённого документа).
- Запись в `CHANGELOG.md` под `## [Unreleased]`.
- `dotnet build`/тесты .NET не затрагиваются; в `webview/` проходят
  `npm run typecheck`, `npm run lint`, `npm test`.

## План выполнения
- [x] Этап 1: Устранить синтетический ведущий блок в `md-blocks.ts`. Вместо
  `boundaries.unshift(0)` (создающего лишний блок `[0, start[0]-1]`) сдвинуть начало
  ПЕРВОГО реального блока на строку 0, чтобы ведущие пустые строки/ref-defs ехали
  вместе с первым блоком, а `blocks.length` равнялся числу top-level токенов. Сохранить
  whole-doc-поведение для документов без единого токена (пустой/whitespace-only —
  один блок на весь текст). Добавить в `MdBlock` поле для начала node-контента первого
  блока (симметрично `contentLineEnd`), напр. `contentLineStart`, выставляемое как
  `token.map[0]` первого блока, чтобы splice знал границу ведущего head-контента.
- [x] Этап 2: Добавить сохранение "головы" блока в `md-splice.ts`. Ввести
  `headLines(lines, block)` симметрично `tailLines`: строки `[block.lineStart, contentLineStart)`
  — ведущий head-контент (пустые строки + ref-defs до ноды). В `serializeWithSplice`
  при правке блока с непустой головой пере-присоединять её дословно перед пере-
  сериализованной нодой. Убедиться, что no-op путь (нода не изменена → `lines.slice`)
  остаётся байт-идентичным. Обновить/удалить теперь неактуальные комментарии про
  «synthetic leading-blank block» в обоих файлах.
- [x] Этап 3: Расширить unit-тесты. В `md-blocks.test.ts` добавить фикстуры «ведущая
  пустая строка» и «ведущий reference-definition»: round-trip байт-в-байт и проверка,
  что `blocks.length` совпадает с `parser.parse(...).childCount`. В `md-splice.test.ts`
  добавить: no-op round-trip этих двух фикстур байт-идентичен (без fallback) и правка
  первого блока сохраняет ведущий контент дословно.
- [x] Этап 4: Проверить и покрыть тестами выравнивание в `formatted.ts`/`index.ts`.
  В `formatted.test.ts` добавить фикстуру с ведущей пустой строкой и проверить: (а)
  `getText()` неизменённого документа байт-идентичен исходнику (нет переразбивки при
  выходе из "Форматированного"); (б) маппинг подсветки/`sourceLineForPos`/scroll-sync
  указывает на правильную ноду (нет смещения на один блок). При необходимости — минимальная
  правка `formatted.ts`/`index.ts`, если 1:1 маппинг не восстанавливается автоматически.
- [x] Этап 5: Записать пользовательски-видимое изменение в `CHANGELOG.md` под
  `## [Unreleased]`; прогнать `npm run typecheck`, `npm run lint`, `npm test` в `webview/`
  и убедиться, что вся существующая и новая проверка зелёная.

## Описание
`webview/src/md-blocks.ts:91-94` добавляет синтетический блок в начало (unshift), когда первый токен
начинается не с 0-й строки, поэтому `blocks.length = childCount + 1` навсегда, и `md-splice.ts:52`
берёт полный откат на весь документ при каждом `getText()`. Последствия: (a) одно нажатие клавиши
переписывает весь файл (переразбивка hard-wrap, перенумерация списков, setext→ATX, выравнивание
таблиц, плюс удаление reference-definitions из S-12); (b) `index.ts:399-403` при выходе из режима
"Форматированный" читает `formatted.getText()` и перезаписывает исходный редактор переразобранным
текстом — автор видит изменённый документ без единой правки; (c) `formatted.ts:395-404` индексирует
`blocks[i]` параллельно детям ProseMirror, поэтому подсветки/hover/scroll-sync смещены на один блок.
Каждый тестовый фикстур в проекте удобно начинается с заголовка, поэтому баг ничем не покрыт.

Критерии готовности:
- Синтетический ведущий блок становится реальной, адресуемой нодой (либо смещение блок↔дитя-маппинга
  сохраняет соответствие 1:1) — `blocks.length` остаётся синхронным с `childCount`.
- Документ с ведущей пустой строкой (или ведущим reference-definition) больше не вызывает полный откат
  на каждый `getText()` при отсутствии реальных изменений.
- Переключение "Форматированный" → "Код" больше не переразбирает/не меняет документ, который не
  редактировался.
- Подсветки/hover/scroll-sync корректно совпадают для документов с ведущей пустой строкой.
- Добавлены фикстуры: документ с ведущей пустой строкой и документ с ведущим reference-definition,
  прогнанные через тесты md-blocks/md-splice/formatted.
- Запись в `CHANGELOG.md`.

# Активная задача

Статус: выполнена
Исходная задача: [T-010] Исправить S-11: документы с CRLF переписываются в LF при каждом автосохранении

Оценка сложности: высокая
Рекомендуемый effort: xhigh

## Критерии выполнения
- Доминирующий стиль перевода строки документа определяется при загрузке из сырого содержимого
  файла (в `LoadFile`, до всякой нормализации) и сохраняется как отдельное состояние документа,
  не зависящее от `_text` (который приходит из webview уже в LF).
- Определённый стиль повторно применяется ко ВСЕМ трём точкам записи на диск — `OnSave`
  (`HostController.cs:470`), `RunDiskAutosave` (`:762`), `OnSaveVersion` (`:826`) — так что LF-текст
  из webview записывается на диск в исходном стиле (LF→CRLF при необходимости).
- Стиль перевода строки при записи снимается как согласованный набор вместе с `text`/`path` под
  `lock (_sync)` (как остальные поля документа), чтобы автосейв-таймер не мог записать «рваное»
  сочетание (текст одного документа + EOL другого). После `Discard` (повторное чтение файла,
  `:627`) стиль обновляется из перечитанного содержимого.
- Для нового/неверсионированного файла без загруженного стиля используется разумное значение по
  умолчанию (задокументированное в коде).
- CRLF-документ (репозиторий с `core.autocrlf=true`) после «Редактировать» + одна правка +
  автосохранение проходит round-trip без изменения окончаний строк на неизменённых строках.
- Добавлен host-тест (проект `SpecDesk.Host.Tests`), проверяющий CRLF round-trip через путь
  сохранения/автосейва (сейчас EOL-покрытия нет).
- Добавлены CRLF round-trip фикстуры в тесты `md-blocks`/`md-splice` (`webview/tests/`, сейчас их
  нет вовсе): `joinBlocks(splitTopLevelBlocks(md)) === md` побайтово для CRLF-входа; неизменённые
  блоки в `serializeWithSplice` сохраняются дословно вместе с `\r`.
- Запись в `CHANGELOG.md` под `## [Unreleased]`.
- `dotnet build`/`dotnet test SpecDesk.slnx` и `npm run typecheck && npm run lint && npm test`
  (в `webview/`) проходят; предупреждения = ошибки.

## План выполнения
- [x] Этап 1: Хост — определение и хранение стиля EOL. Добавить хелпер определения доминирующего
      перевода строки по сырому тексту (подсчёт `\r\n` против одиночных `\n`) и `_sync`-защищённое
      поле стиля документа. Заполнять его в `LoadFile` (из `File.ReadAllText`, до отправки в webview)
      и в `Discard` (из перечитанного содержимого); для нового файла — значение по умолчанию. Пока
      без изменения записи на диск.
- [x] Этап 2: Хост — повторное применение стиля при записи. Во всех трёх точках записи
      (`OnSave`, `RunDiskAutosave`, `OnSaveVersion`) конвертировать LF-текст в сохранённый стиль
      перед `File.WriteAllText`, снимая стиль под `lock (_sync)` в том же наборе, что `text`/`path`
      (для автосейва — вместе со снимком, синхронно с проверкой поколения черновика).
- [x] Этап 3: Хост-тест round-trip. В `SpecDesk.Host.Tests` добавить тест: CRLF-документ →
      «Редактировать» → правка → автосейв/сохранение → файл на диске остаётся CRLF на неизменённых
      строках; плюс проверка значения по умолчанию для нового файла. Опереться на существующие
      фейки (`FakeVersioning` и паттерн `HostControllerAutosaveRaceTests`/`...LifecycleTests`).
- [x] Этап 4: Webview — CRLF-фикстуры для `md-blocks`. В `webview/tests/md-blocks.test.ts` добавить
      кейсы, подтверждающие, что `splitTopLevelBlocks`/`joinBlocks` побайтово восстанавливают
      CRLF-вход (границы блоков и source-map корректны при `\r\n`).
- [x] Этап 5: Webview — CRLF-фикстуры для `md-splice`. В `webview/tests/md-splice.test.ts` добавить
      кейсы: no-op edit CRLF-документа байт-идентичен; неизменённые блоки сохраняются дословно с
      `\r`; зафиксировать (тестом или комментарием) поведение перевода строки в переэмитированном
      изменённом блоке.
- [x] Этап 6: `CHANGELOG.md` — запись под `## [Unreleased]` о сохранении окончаний строк документа.

## Описание
`webview/src/editor.ts:528-530` (`getText` = `doc.toString()`, соединяет строки через LF) — CodeMirror 6
нормализует переносы строк на входе, поэтому полный LF-текст уходит хосту ~120 мс после загрузки и при
каждой правке; хост пишет его дословно (`HostController.cs:603-631`, `File.WriteAllText`), и ничто не
восстанавливает CRLF. Репозиторий, склонированный на Windows с `core.autocrlf=true` (значение по
умолчанию установщика), хранит `.md` в CRLF → "Редактировать" → одна напечатанная буква →
автосохранение переписывает окончания строк во всём файле → diff "Сохранить версию"/PR затрагивает
каждую строку — именно то, для предотвращения чего существует поблочная сериализация (block-splice). Ни
одной фикстуры с `\r`/CRLF нет ни в одном тесте `webview/tests/`.

Критерии готовности:
- Доминирующий стиль перевода строки документа определяется при загрузке (на стороне хоста или
  webview) и повторно применяется при записи текста на диск.
- CRLF-документ (`core.autocrlf=true`) после "Редактировать" + одна правка + автосохранение проходит
  round-trip без изменения окончаний строк на неизменённых строках.
- Добавлены CRLF round-trip фикстуры в тесты `md-blocks`/`md-splice` (сейчас их нет вовсе).
- Запись в `CHANGELOG.md`.

Примечание: полезно делать после [T-009] (S-13), так как обе задачи затрагивают md-blocks/md-splice.

# Активная задача

Статус: выполнена
Исходная задача: [T-011] Исправить S-12: откатной полный ресериализатор удаляет link-reference-definitions

Оценка сложности: средняя
Рекомендуемый effort: high

`webview/src/md-splice.ts:44-54`. При несовпадении числа блоков откат — это
`serializer.serialize(edited)`; у reference-definitions нет узла ProseMirror (markdown-it резолвит их
на этапе парсинга), поэтому вывод теряет каждую строку `[id]: url`, использованные reference-ссылки
превращаются в inline, а неиспользуемые определения удаляются. §5 документирует, что добавление/удаление
блока приводит к полному ре-сериализации — но не разрешает потерю содержимого. Триггер тривиален:
нажать Enter в WYSIWYG, чтобы начать новый абзац в любом документе с reference-definition → определения
исчезают из сохранённого Markdown.

Критерии готовности:
- Откатной путь при несовпадении числа блоков сохраняет reference-definitions (`[id]: url`) дословно —
  либо переносит их через откат, либо отказывается от отката при их наличии в исходнике (используя
  более безопасный путь).
- Нажатие Enter в WYSIWYG для нового абзаца в документе с reference-definitions больше не удаляет их из
  сохранённого Markdown.
- Добавлен тест "откат сохраняет содержимое" с явной проверкой на reference-definitions (текущий тест
  отката ничего не проверяет по сохранности).
- Запись в `CHANGELOG.md`.

# Активная задача

Статус: выполнена
Исходная задача: [T-012] Исправить S-14: правка текста ячейки таблицы стирает выравнивание колонок

Оценка сложности: средняя
Рекомендуемый effort: high

`webview/src/pm-markdown.ts:63-72` (парсер игнорирует атрибуты выравнивания markdown-it — в схеме нет
`align`) и `:104-107` (сериализатор всегда выводит `| --- |`). Правка *текста* ячейки — часть v1
(только *структурные* правки таблиц отложены согласно §5), поэтому: исправление опечатки в одной ячейке
столбца с правым выравниванием переписывает всю таблицу без какого-либо выравнивания.

Критерии готовности:
- Выравнивание таблицы (атрибуты выравнивания markdown-it) переносится через схему как атрибут
  ячейки/колонки.
- Сериализатор воспроизводит исходные маркеры выравнивания вместо всегда `| --- |`.
- Правка текста одной ячейки в таблице с выравниванием сохраняет выравнивание этой таблицы
  сквозь весь round-trip.
- Добавлен round-trip тест для таблицы с выравниванием (сейчас протестированы только невыровненные
  таблицы).
- Запись в `CHANGELOG.md`.

# Активная задача

Статус: выполнена
Исходная задача: [T-013] Исправить S-15: команда форматирования блока падает при каретке в позиции 0 документа, начинающегося с перевода строки

Оценка сложности: средняя
Рекомендуемый effort: medium

`webview/src/md-format.ts:114-118` — для `from=0` с `doc[0]==="\n"` вычисленная правка —
`{from:1, to:0}`; `editor.ts:498-505` диспатчит её без проверки → CodeMirror бросает
`RangeError: Invalid change range 1 to 0`, необработанное исключение в обработчике клика тулбара.
Триггер: документ с ведущей пустой строкой, Ctrl+Home, клик по H1/H2/списку/нумерованному
списку/цитате/коду.

Критерии готовности:
- Вычисляемый диапазон правки больше не даёт некорректный `{from:1,to:0}` для каретки в позиции 0, если
  `doc[0]` — перевод строки.
- Клик по H1/H2/списку/нумерованному списку/цитате/коду с кареткой в позиции 0 документа с ведущей
  пустой строкой больше не бросает исключение.
- Добавлен тест: каретка в позиции 0 документа с ведущей пустой строкой, для каждой команды тулбара.
- Запись в `CHANGELOG.md`.

# Активная задача

Статус: выполнена
Исходная задача: [T-014] Исправить S-16: одна вставка Ctrl+V может вставить содержимое дважды (текст + картинка)

Оценка сложности: средняя
Рекомендуемый effort: medium

`webview/src/image-capture.ts:55-76`. Собственный обработчик вставки CodeMirror регистрируется на том
же `contentDOM` раньше `attachImageCapture`, поэтому он вставляет `text/plain` и делает
`preventDefault()` первым; `image-capture` затем независимо находит элемент `image/*` и генерирует
`image.paste`, ответ хоста на который вставляет ссылку на изображение в Markdown. Для буферов обмена с
обоими форматами (ячейки Excel, фрагмент Word, изображение+HTML из браузера) автор получает и текст, и
ссылку на изображение. Собственный `preventDefault()` `image-capture` (`:74`) срабатывает слишком
поздно.

Критерии готовности:
- `image-capture` пропускает вставку картинки, если в буфере обмена уже присутствует непустой формат
  `text/plain` (либо перехват переносится в capture-фазу с детерминированным выбором одного формата).
- Вставка буфера обмена, содержащего одновременно текст и изображение, вставляет ровно одно
  представление, а не оба.
- Добавлен тест на проводку paste/drop (сейчас протестирован только `stripDataUrlPrefix`).
- Запись в `CHANGELOG.md`.

# Активная задача

Статус: выполнена
Исходная задача: [T-015] Исправить M-01: нативный парсер без UseEmphasisExtras — зачёркивание парсится как литерал

Оценка сложности: средняя
Рекомендуемый effort: medium

`src/SpecDesk.Markdown/Pipeline.fs:13-22` — нативный парсер не включает `UseEmphasisExtras` (или
эквивалентное расширение Markdig), поэтому `~~зачёркнутый~~` (который эмитит тулбар редактирования)
парсится как обычный текст в нативном пайплайне → фантомные правки `~~`-слова в word-diff
"Форматированного" режима и буквальные тильды в нативном превью.

Критерии готовности:
- `Pipeline.fs` включает `UseEmphasisExtras` (или эквивалент), чтобы `~~зачёркнутый~~` нативно
  парсился как зачёркивание — согласуясь с тем, что уже эмитит тулбар.
- Нативное превью больше не показывает буквальные тильды для зачёркнутого текста.
- word-diff между нативным и форматированным больше не сообщает фантомную правку `~~`-слова.
- Добавлен тест на round-trip/нативный рендер зачёркивания.
- Запись в `CHANGELOG.md`.

# Активная задача

Статус: выполнена
Исходная задача: [T-016] Исправить M-02/M-03: неатомарная дедупликация и невалидные ссылки на изображения в ImageEngine.fs

Оценка сложности: средняя
Рекомендуемый effort: high

`src/SpecDesk.Core/ImageEngine.fs:80-84, 135-142, 164`. Два смежных пробела в устойчивости пути
вставки изображений:
1. Дедупликация сверяет только по суффиксу имени файла (содержимое никогда не проверяется; `hash8` —
   32-битный), а `File.WriteAllBytes` неатомарен, поэтому усечённый `…{hash8}.png` от прерванной
   на середине записи "переиспользуется" навсегда.
2. Эмитируемая ссылка на изображение невалидна как CommonMark, если относительный путь (из конфига)
   содержит пробел/`(`/`#`: `![image](../my images/x.png)` рендерится буквально.

Критерии готовности:
- Запись изображения выполняется через временный файл + переименование (либо повторным хэшированием
  существующего кандидата), чтобы усечённая прежняя запись не переиспользовалась молча.
- Эмитируемые ссылки на изображения оборачиваются в `<…>` или percent-encode'ятся, если путь содержит
  символы, недопустимые в "голой" CommonMark-ссылке.
- Добавлены тесты: сценарий прерванной записи при дедупликации; путь изображения с пробелом рендерится
  корректно.
- Запись в `CHANGELOG.md`.

# Активная задача

Статус: выполнена
Исходная задача: [T-017] Исправить M-04: SVG с UTF-8 BOM отклоняется как изображение

Оценка сложности: низкая
Рекомендуемый effort: medium

`src/SpecDesk.Core/ImageProcessing.fs:62-70` — BOM (`﻿`) не срезается `TrimStart()`, поэтому проверка
`StartsWith "<?xml"` не проходит → файл уходит по растровому пути → ошибка "Could not read the image".

Критерии готовности:
- BOM срезается перед проверкой `StartsWith "<?xml"` (либо проверка терпима к ведущему BOM).
- Вставка/вставка из буфера обмена SVG с UTF-8 BOM принимается как SVG, а не отклоняется.
- Добавлен тест на SVG с BOM.
- Запись в `CHANGELOG.md`.

# Активная задача

Статус: выполнена
Исходная задача: [T-018] Исправить M-05: экранированные кавычки в .spectool.toml сбивают самодельный парсер

Оценка сложности: средняя
Рекомендуемый effort: medium

`src/SpecDesk.Core/Toml.fs:13-44, 121-125` — экранированные кавычки `\"` сбивают трекер кавычек, а
`unquote` их никогда не разэкранирует, поэтому `template = "Say \"hi\""` неверно отслеживает
последующий `#`/`]` и возвращает буквальные обратные слеши в текст коммита. Нужно сначала определить,
входит ли поддержка экранирования в текущий объём модуля.

Критерии готовности:
- Либо: `template = "Say \"hi\""` проходит round-trip корректно (без экранирования, без сбитых
  разделителей), с тестом на это.
- Либо: отсутствие поддержки экранирования явно задокументировано комментарием в `Toml.fs`, а значение
  конфига с `\"` даёт понятную ошибку валидации вместо молчаливо неверного текста коммита.
- Запись в `CHANGELOG.md`.

# Активная задача

Статус: выполнена
Исходная задача: [T-019] Исправить M-06: правка только форматирования внутри списка/таблицы эмитит пустую базу diff'а

Оценка сложности: средняя
Рекомендуемый effort: high

`src/SpecDesk.Diff/DiffWire.fs:155-164` — правка только форматирования внутри элемента
списка/таблицы эмитит `Changed` с пустыми `Children`/`BaseText`, из-за чего webview делает word-diff
против пустой базы и "смывает" всю таблицу вместо изменённой строки.

Критерии готовности:
- Правка только форматирования внутри элемента списка/таблицы эмитит текст запасного варианта
  (plain-block fallback) контейнера в `BaseText`/`Children` вместо пустого значения.
- word-diff в webview подсвечивает только реально изменённую строку/элемент, а не всю таблицу/список.
- Добавлены тесты на правку только форматирования внутри списка и внутри таблицы.
- Запись в `CHANGELOG.md`.

# Активная задача

Статус: выполнена
Исходная задача: [T-020] Исправить M-07: неограниченная память/CPU diff'а на больших почти-идентичных документах

Оценка сложности: средняя
Рекомендуемый effort: high

`src/SpecDesk.Diff/AstDiff.fs:87, 146-158` — неограниченная по памяти LCS O(m·n) и список
попарного сходства "все со всеми"; патологически большой документ (глоссарий/changelog с множеством
почти-идентичных блоков, или таблица на несколько тысяч строк) может подвесить хост или вызвать OOM.

Критерии готовности:
- Вычисление LCS и попарного сходства ограничено по размеру (с откатом на Removed+Added при
  превышении лимита) для патологически больших входов.
- Добавлен тест на большой документ, подтверждающий ограниченное время/память вместо зависания/OOM.
- Запись в `CHANGELOG.md`.

# Активная задача

Статус: выполнена
Исходная задача: [T-021] Проверить M-08: инвариант LineMap↔data-line для списков определений

Оценка сложности: низкая
Рекомендуемый effort: medium

`src/SpecDesk.Markdown/Renderer.fs:115-136` — неясно, сохраняется ли инвариант количества
LineMap↔data-line для эмиссии атрибутов `<dt>`/неявного `<dd>`-параграфа Markdig; это единственное
включённое семейство блоков без регрессионного теста.

Критерии готовности:
- Добавлен регрессионный тест, прогоняющий списки определений и проверяющий инвариант количества
  LineMap↔data-line (по аналогии с покрытием других включённых семейств блоков).
- Если тест выявляет реальное расхождение — `Renderer.fs` исправлен так, чтобы инвариант выполнялся.
- Запись в `CHANGELOG.md` — только если потребовалось исправление.

# Активная задача

Статус: выполнена
Исходная задача: [T-022] Исправить M-09: документация IGitHubAuth расходится с реальным поведением при ошибке сохранения токена

Оценка сложности: низкая
Рекомендуемый effort: medium

`src/SpecDesk.GitHub/IGitHubAuth.cs:106-107` — документация утверждает, что ошибка сохранения токена
проявляется как брошенное исключение, но `CompleteAuthorizationAsync` в реальности возвращает
`SignInResult.StorageFailed()` (не бросает). Устаревшая документация может спровоцировать избыточный
try/catch на стороне хоста.

Критерии готовности:
- Комментарий интерфейса соответствует реальному поведению (возврат `SignInResult.StorageFailed()`)
  — либо, если бросок исключения был изначально задуманным контрактом, реализация изменена так, чтобы
  бросать исключение; выбрать один вариант и привести код и документацию в соответствие.
- Обработка этого пути на стороне хоста (если есть) опирается на единый согласованный контракт.
- Запись в `CHANGELOG.md`.

# Активная задача

Статус: выполнена
Исходная задача: [T-023] Проверить M-10: двойная отмена по таймауту в GitHub-запросах

Оценка сложности: средняя
Рекомендуемый effort: high

`src/SpecDesk.GitHub/GitHubReview.cs:447-465` и `src/SpecDesk.Host/HostController.cs:1002-1003` —
30-секундный CTS на запрос плюс второй таймаут в пути обновления (refresh) — нужно подтвердить, что
двойная отмена не даёт запутанную классификацию ошибки. Зависание на этапе connect/handshake
ограничено только таймаутом сокета ОС (то же ограничение, что и у PushBranch, S-01).

Критерии готовности:
- Подтверждено (тестом или трассировкой), что срабатывание обоих таймаутов не даёт запутанную/неверно
  классифицированную ошибку пользователю.
- Если найдена реальная неверная классификация — исправлена так, чтобы сообщение об ошибке точно
  отражало, какой таймаут сработал.
- Запись в `CHANGELOG.md` — только если поведение изменилось.

# Активная задача

Статус: выполнена
Исходная задача: [T-024] Усилить M-11: хранение токена сверх DPAPI CurrentUser

Оценка сложности: низкая
Рекомендуемый effort: medium

`src/SpecDesk.GitHub/TokenStore.cs:36-41`, `TokenProtector.cs:22-40` — файл токена полагается только
на DPAPI `CurrentUser` (`optionalEntropy = null`), без ужесточения ACL файловой системы; читаем любым
процессом той же сессии. Приемлемо по §7, но специфичная для приложения энтропия усилила бы защиту
от другого приложения того же пользователя, знающего путь.

Критерии готовности:
- В вызовы DPAPI protect/unprotect добавлена специфичная для приложения `optionalEntropy` (сейчас
  `null`).
- Существующие тесты round-trip авторизации/токена проходят с добавленной энтропией.
- Запись в `CHANGELOG.md`.

# Активная задача

Статус: выполнена
Исходная задача: [T-025] Проверить M-12: Commands.Stage("*") стейджит всё без гарантии .gitignore

Оценка сложности: низкая
Рекомендуемый effort: medium

`src/SpecDesk.Git/LibGit2DocumentVersioning.cs:55, 122` (`Initialize`, `SaveVersion`) —
`Commands.Stage(repo, "*")` стейджит всё без гарантии соблюдения `.gitignore` сверх поведения libgit2
по умолчанию; репозиторий с директорией build-артефактов может закоммитить её в версию.

Критерии готовности:
- Подтверждено/задокументировано, что стейджинг по умолчанию в libgit2 уважает `.gitignore`; если нет —
  добавлена явная защита (например, проверка наличия `.gitignore` / стейджинг только отслеживаемых и
  намеренных путей).
- Добавлен тест: репозиторий с игнорируемой директорией build-артефактов — `SaveVersion` не
  коммитит её.
- Запись в `CHANGELOG.md`.

# Активная задача

Статус: выполнена
Исходная задача: [T-026] Исправить M-13: TryAdvanceReview ключуется по (state, имя ветки), неверно штампует пересозданный одноимённый черновик

Оценка сложности: средняя
Рекомендуемый effort: high

`src/SpecDesk.Host/HostController.cs:1235-1248` — `TryAdvanceReview` ключуется по
`(state, branch-name)`; одноимённый черновик, пересозданный в процессе публикации, получает неверный
штамп, из-за чего следующее "Обновить проверку" ложно сообщает "Нет новых версий".

Критерии готовности:
- Пересозданный одноимённый черновик в процессе публикации больше не штампуется как уже увиденный —
  следующее "Обновить проверку" корректно детектирует новые версии вместо ложного "Нет новых версий".
- Добавлен регрессионный тест на сценарий "пересоздание одноимённого черновика во время публикации".
- Запись в `CHANGELOG.md`.

Примечание: монотонный токен поколения черновика (см. [T-048], консолидация состояния сессии) —
более фундаментальное решение; для этой задачи достаточно точечного исправления, устраняющего
ложноотрицательный результат.

# Активная задача

Статус: выполнена
Исходная задача: [T-027] Исправить M-14: отменённый вход может закрыть подсказку с кодом устройства более нового входа

Оценка сложности: средняя
Оценка ответственности: высокая
Риск: high (auth/concurrency)

`src/SpecDesk.Host/HostController.cs:1578, 1593` (номера устарели — логика с тех пор переехала в
`HostController.SignIn.cs`) — отменённый flow входа эмитит неассоциированный кадр "вышел из системы",
который закрывает подсказку с кодом устройства *более нового* flow входа.

Критерии готовности:
- Запасной эмит "вышел из системы" защищён проверкой `ReferenceEquals(_signInCts, cts)` (или
  эквивалентом), так что отменённый flow не может закрыть UI другого, более нового flow входа.
- Добавлен регрессионный тест: начать вход, отменить, начать новый вход — подсказка с кодом устройства
  нового flow не закрывается очисткой первого.
- Запись в `CHANGELOG.md`.

Итог: основные публикующие пути (`PublishPromptIfCurrent`/`PublishTerminalIfCurrent`) уже были
гейтированы; найден и исправлен реальный пробел — незагейченный fallback-emit в
`OnGitHubSignInCancel` при сбое персистентности sign-out. Добавлен регрессионный тест, гоняющий два
flow конкурентно. Запись в CHANGELOG добавлена (поведение изменилось).

# Активная задача

Статус: выполнена
Исходная задача: [T-028] Исправить M-15: повторное событие ready перезагружает _initialDocPath и переключает автора с открытого документа

Оценка сложности: низкая
Оценка ответственности: средняя
Риск: medium

`src/SpecDesk.Host/HostController.cs:299-308` (номера устарели — `OnReady()` теперь в
`HostController.Session.cs`) — повторно сработавшее событие `ready` (восстановление WebView2/
перезагрузка страницы) перезагружает `_initialDocPath`, переключая автора с открытого документа и
заново штампуя "Опубликовано".

Критерии готовности:
- Повторное событие `ready` не перезагружает `_initialDocPath` и не перештамповывает "Опубликовано",
  если документ уже открыт.
- Добавлен регрессионный тест, симулирующий дублирующееся событие `ready` при открытом документе.
- Запись в `CHANGELOG.md` — только если поведение изменилось.

Итог: верификация подтвердила, что защёлка `_initialDocLoadAttempted` уже полностью закрывает баг по
всем путям (null-путь, отсутствующий файл, исключение в `LoadFile`) и уже покрыта существующим
регрессионным тестом — правок кода не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-030] Исправить M-17: недоверенные поля лога из webview допускают подделку записей через встроенные переводы строк

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: medium (security-adjacent, радиус мал)

`src/SpecDesk.Host/LogBridge.cs:35-54` — недоверенные поля `Message`/`Data` из webview пишутся
дословно в общий rolling-лог; встроенные переводы строк подделывают дополнительные записи лога.

Критерии готовности:
- Поля `Message`/`Data`, приходящие из webview, санитизируются перед записью в лог.
- Добавлен тест: полезная нагрузка с встроенными переводами строк даёт одну корректно оформленную
  запись лога.
- Запись в `CHANGELOG.md` — только если поведение изменилось.

Итог: верификация подтвердила, что `SanitizeForLog` уже применяется к обоим полям на обоих путях
(`Receive`, `TraceBridge`) и уже покрыта существующим тестом — правок кода не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-032] Исправить M-19: риск частичного посева SampleRepo из-за зависящего от файловой системы порядка копирования

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low

`src/SpecDesk.Host/SampleRepo.cs:28-40` — признак завершённости посева — существование `welcome.md`,
но порядок копирования зависит от файловой системы, поэтому сбой посреди копирования после
`welcome.md` пропускает повторный посев навсегда.

Критерии готовности:
- Проверка завершённости посева больше не полагается только на существование `welcome.md`.
- Добавлен тест, симулирующий сбой посреди посева: следующий `Initialize` дозавершает посев.
- Запись в `CHANGELOG.md` — только если поведение изменилось.

Итог: верификация подтвердила, что атомарный маркер `.specdesk-seed-complete` (temp-файл + атомарный
`File.Move` последним шагом) уже реализован корректно; добавлена одна недостающая ассерция в
существующий регрессионный тест (маркер действительно появляется после ретрая).

# Активная задача

Статус: выполнена
Исходная задача: [T-033] Проверить M-20: PhotinoFileDialogs.OnUiThread может подвесить поток сообщений без таймаута

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low

`src/SpecDesk.Host/Program.cs:157-176` — `OnUiThread` блокирует поток сообщений без таймаута; если
окно закрывается до выполнения `Invoke`, насос сообщений может зависнуть.

Критерии готовности:
- Проверено, гарантирует ли Photino опустошение очереди invoke при закрытии окна; если нет — добавлен
  ограниченный таймаут (или отмена).
- Тест или задокументированное обоснование, подтверждающее исправление.
- Запись в `CHANGELOG.md` — только если поведение изменилось.

Итог: независимое ревью (codex) нашло реальный пробел, не покрытый исходной верификацией —
`w.Invoke(...)` вызывался на потоке абонента и мог заблокировать его навсегда, если нативный
callback никогда не доставлялся при закрытии окна; отменяемым был только `WaitOrAbandon` после
Invoke, не сам блокирующий вызов. Исправлено: нативный `Invoke` теперь диспетчеризуется в фоновую
задачу, вызывающий поток ждёт только через уже отменяемый `WaitOrAbandon`; добавлен регрессионный
тест, симулирующий никогда не возвращающийся `Invoke`. Запись в CHANGELOG добавлена.

# Активная задача

Статус: выполнена
Исходная задача: [T-029] Исправить M-16: состояние жизненного цикла только в памяти — рестарт посреди черновика показывает "Опубликовано" (кросс-сессионный вариант S-03)

Оценка сложности: низкая
Оценка ответственности: средняя
Риск: medium

Состояние жизненного цикла документа хранилось только в памяти; рестарт посреди черновика
показывает "Опубликовано", хотя HEAD стоит на ветке черновика, а следующий принудительный
checkout при "Редактировать" рисковал потерять автосохранённый ввод.

Итог: верификация подтвердила, что `ResolveInitialLifecycle`/`_lifecycleResolvedOnce` уже
корректно резолвят состояние по фактически checked-out ветке при первой загрузке процесса, а
`Lifecycle.tryStep` не имеет перехода `(Draft, Edit)`, из-за чего принудительный checkout при
уже верно резолвленном Draft не запускается — оба пункта находки закрыты существующим кодом и
существующим регрессионным тестом. Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-031] Исправить M-18: сырое сообщение исключения попадает в канал, видимый автору

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low

`LogBridge.Export()` — сырое сообщение исключения при сбое экспорта лога могло попасть в канал,
видимый автору, вместо простоязычного сообщения.

Итог: верификация подтвердила, что `Export()` уже перехватывает `IOException`/
`UnauthorizedAccessException` и уведомляет автора фиксированным "Could not export the log."
(без `ex.Message`), уже покрыто регрессионным тестом. Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-034] Исправить M-21: гонка позиции вставки картинки с асинхронным round-trip к хосту

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low

Позиция вставки вставленного изображения могла разъехаться с фактической позицией курсора из-за
гонки с асинхронным round-trip к хосту (конкурентный ввод текста или несколько изображений с
ответами не по порядку).

Итог: верификация подтвердила, что вставка уже фиксирует позицию через `editor.trackPosition`/
`insertAtMarker` (переиспользуемые CodeMirror-маркеры, ремаппящиеся при каждом изменении
документа) вместо сырого числового смещения; оба сценария находки уже покрыты тестами. Правок
не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-035] Проверить M-22: второй open() prompt-bar в окне "закрытие в полёте" молча проглатывается

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low

Второй `open()` prompt-bar, вызванный в окне "закрытие в полёте" первого запроса, мог молча
проглатываться защёлкой состояния.

Итог: верификация подтвердила, что `close()` уже немедленно сбрасывает защёлку `opening` и
инкрементирует токен, так что второй `open()` сразу после `close()` стартует независимый запрос;
точный сценарий уже покрыт регрессионным тестом. Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-036] Исправить M-23: reviews-panel зависает на "Загрузка ваших проверок…" при отклонении requestReviews

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low

Панель ревью могла зависнуть на состоянии загрузки, если `requestReviews()` отклонял промис.

Итог: верификация подтвердила, что `open()` уже оборачивает `requestReviews()` в try/catch с
запасным состоянием ошибки, уже покрыта тестом на сценарий reject+открытая панель. Добавлен
недостающий регрессионный тест на смежный edge-case (панель закрыта до отклонения промиса) —
код уже корректно проверял это условие, тест лишь закрыл пробел в покрытии.

# Активная задача

Статус: выполнена
Исходная задача: [T-037] Исправить M-24: Escape закрывает диалоги только при фокусе в текстовом поле

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low

Escape мог закрывать диалоговые бары только при фокусе именно в текстовом поле, а не при фокусе
на других элементах бара (например, на кнопке).

Итог: верификация подтвердила, что обработчики Escape уже навешаны на контейнер бара (не только
на поле ввода) во всех трёх диалогах (branch-name, version-note, pr-text), симметрично и уже
покрыто тестами. Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-038] Исправить M-25/M-26: наихудший случай стоимости LCS в word-diff и мёртвый второй flushDel()

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low

Наихудший случай стоимости LCS в word-level diff мог считаться по сырой длине символов вместо
числа токенов; подозревался недостижимый второй вызов `flushDel()`.

Итог: верификация подтвердила, что гвард уже считается по числу токенов (не символов) и что оба
(на деле три) вызова `flushDel()` достижимы и функционально различны — недостижимого кода нет.
Уже покрыто тестом на патологический вход. Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-039] Исправить M-27: в scroll-geometry нет защиты от span==0, может давать NaN

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low

Функции интерполяции скролла могли делить на ноль (span==0 / height==0), давая NaN.

Итог: верификация подтвердила, что обе функции (`scrollTopForLine`, `lineAtScrollTop`) уже
защищены от деления на ноль и уже покрыты тестами. Добавлен комментарий, документирующий, что
markdown-it на практике никогда не порождает блочный токен с нулевым span — защита является
defense-in-depth, а не фиксом наблюдаемого реального входа.

# Активная задача

Статус: выполнена
Исходная задача: [T-040] Исправить M-28: log.ts может упасть на JSON.stringify циклических/BigInt данных; отсутствуют тесты

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low

`log.ts` мог упасть на `JSON.stringify` циклических/`BigInt` данных; отсутствовали тесты.

Итог: верификация подтвердила, что `safeStringify` уже оборачивает `JSON.stringify` в try/catch с
replacer'ом для `BigInt` и плейсхолдером для циклических данных, уже покрыто тестами; `raf.ts`
уже имеет содержательное тестовое покрытие. Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-041] Исправить M-29/M-30: styles.css — обрезка плашки изменения у верха панели; переполнение review-meta/review-state без переноса

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low

Плашка аннотации изменения могла обрезаться у верха панели Formatted; `.review-meta`/
`.review-state` могли переполняться без переноса; `#reviews-panel-head` не переносился по
строкам.

Итог: верификация построчно подтвердила, что все три под-находки уже устранены в
`webview/styles.css` (`flex-wrap`, `ellipsis`+`max-width`, отдельный `padding-top`); дополнительно
проведена обязательная визуальная проверка (Playwright-харнесс на реальной разметке/стилях) в
светлой и тёмной темах в нескольких ширинах — подтвердила корректный рендер. Правок не
потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-043] Исправить M-33: переключение префикса строки в md-format дублирует префикс на смешанном выделении

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low

Переключение префикса списка/заголовка на смешанном по префиксу выделении могло удваивать
префикс вместо нормализации.

Итог: верификация подтвердила исходные три сценария (heading/list/quote) уже нормализуются
корректно и покрыты тестами. При углублённой проверке (пробные падающие тесты) найден и исправлен
реальный пробел вне исходного списка: список внутри blockquote (`"> - a"`) дублировал маркер
из-за `^`-заякоренных регэкспов, не видящих сквозь `> `-префикс. Исправлено по образцу уже
существующей обработки заголовков (`splitContainers`); добавлены регрессионные тесты и запись в
CHANGELOG.md.

# Активная задача

Статус: выполнена
Исходная задача: [T-045] Проверить M-35: ipc.ts on() перезаписывает последнего подписавшегося, молча отключая предыдущего

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: medium (центральный модуль message-routing, хотя сама находка узкая)

Повторная регистрация обработчика IPC того же вида могла молча заменить предыдущий подписки.

Итог: верификация подтвердила, что `IpcClient.on()` уже бросает исключение при повторной
регистрации того же вида сообщения вместо тихой замены, с задокументированным обоснованием;
легитимного сценария законной перерегистрации не найдено. Уже покрыто тестом. Правок не
потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-052] Сделать таргет BundleWebview инкрементальным и пропускать design-time сборки (T-05)

Оценка сложности: низкая
Оценка ответственности: средняя
Риск: medium (build-инфраструктура, влияет на каждую сборку)

MSBuild-таргет `BundleWebview` мог выполняться при каждой сборке (без инкрементальности) и
срабатывать при design-time сборках IDE.

Итог: верификация (включая реальные прогоны сборки) подтвердила, что таргет уже условен на
`'$(DesignTimeBuild)' != 'true'` и уже инкрементален через контентный SHA-256-фингерпринт
(`scripts/verify-bundle.mjs`) — более надёжный эквивалент буквальных MSBuild `Inputs`/`Outputs`,
устойчивый к переключению рабочей копии. Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-053] Удалить мёртвые рёбра ProjectReference (T-06)

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low

`SpecDesk.GitHub.csproj`/`SpecDesk.Ai.csproj` содержали неиспользуемые рёбра `ProjectReference`
(`SpecDesk.Contracts`, `SpecDesk.Core`).

Итог: верификация подтвердила, что оба мёртвых ребра уже удалены, `docs/CODE-REVIEW-GUIDE.md`
уже отражает актуальные зависимости, а `SpecDesk.Ai` больше не проект-заглушка (несёт реальную
реализацию чат-агента). Других мёртвых `ProjectReference` в решении не найдено. Правок не
потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-042] Исправить M-31/M-32: index.ts — некорректный diff.result молча становится пустым diff'ом; гонка debounce между панелями может затереть неотправленную правку

Оценка сложности: средняя
Оценка ответственности: средняя
Риск: medium — центральный конвейер синхронизации редактирования (webview/src/index.ts), затрагивает
обе панели редактора и корреляцию debounce.

`webview/src/index.ts` — некорректная (декодированная в `null`) полезная нагрузка `diff.result`
рисковала молча превращаться в пустой diff, а 120-мс debounce между панелями — незаметно затирать
ещё не отправленную правку через зеркальное `setText`.

Итог: верификация подтвердила, что обе проблемы уже устранены более ранним изменением —
`ipc.on(Kinds.diffResult, ...)` явно отбрасывает payload, декодированный в `null` (комментарий в
коде прямо описывает этот сценарий), а `shouldMirrorInto`/`flushPendingChangesInOrder` несут
развёрнутый комментарий, объясняющий, почему 120-мс гонка уже безопасна. Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-044] Исправить M-34: расхождение clamp'а активной строки между editor.ts и formatted.ts

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low — узкая, локализуемая правка поведения подсветки внутри двух уже известных редакторных
компонентов.

Панели "Код" (CodeMirror) и "Форматированный" (ProseMirror) расходились в том, как ограничивать
подсветку активной строки после сокращения документа.

Итог: верификация подтвердила, что `editors/editor.ts` и `editors/formatted.ts` уже согласованно
клампят активную строку (задокументированная политика силентного клампа на последнюю строку в
`editor.ts` совпадает с фактическим поведением `formatted.ts`). Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-046] Исправить M-36: заменить приведения as в тестах на паттерн instanceof-хелпера

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low — точечная мех. замена паттерна внутри тестового кода, без влияния на поведение продукта.

`webview/tests/review/reviews-panel.test.ts`/`preview.test.ts` использовали приведение `as` там,
где уже есть паттерн instanceof-хелпера (см. `dialogs.test.ts`).

Итог: верификация подтвердила, что оба файла уже используют исключительно паттерн `instanceof`-
хелпера (с явным комментарием "mirrors the pattern in dialogs.test.ts") — приведений `as` в
узнаваемом виде не найдено. Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-047] Разбить HostController.cs на partial-классы по областям ответственности (T-01)

Оценка сложности: средняя
Оценка ответственности: средняя
Риск: medium — структурная правка центрального оркестрирующего класса Host (широкий радиус:
IPC-роутинг, сессия документа, блокировки).

`src/SpecDesk.Host/HostController.cs` был на грани god-object (1937 строк в исходной находке),
владея IPC-роутером, сессией документа, оркестрацией GitHub-ревью, входом, вставкой изображений,
сравнением и внешними ссылками.

Итог: верификация подтвердила, что `HostController.cs` (сегодня 1032 строки) уже разбит на
partial-классы, причём точно на требуемые `HostController.Review.cs`/`.SignIn.cs`/`.Session.cs`,
плюс дополнительно ещё на `.Activity.cs`/`.Cancellation.cs`/`.Chat.cs`/`.PullRequest.cs`/
`.RepositoryBrowse.cs`/`.Workspace.cs` (более полное разбиение, чем требовала находка). Полная
сборка и тесты решения проходят. Запись в CHANGELOG.md уже присутствует. Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-049] Определить и реализовать модель корреляции запросов ipc.ts для будущего стриминга (T-02)

Оценка сложности: средняя
Оценка ответственности: средняя
Риск: medium — общая точка транспорта IPC, разделяемая всеми запрос/ответ вызовами webview.

`webview/src/wire/ipc.ts` — корреляция запросов допускала только один ответ на id, что не позволяло
выразить запланированный стриминг `chat.delta`/`chat.done` для PoC-9.

Итог: верификация подтвердила, что модель уже реализована — `ipc.ts` несёт метод
`subscribe(id, handler): () => void`, удерживающий подписку живой до явного `unsubscribe()`,
задокументированный комментарием, противопоставляющим его одно-разовой корреляции `request()`.
Одно-ответное использование `request()` не затронуто. Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-054] Консолидировать разрозненные строки идентичности продукта в один ProductInfo/AppPaths (T-07)

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low — консолидация уже существующей идентичности продукта в call-site файлах, без изменения
поведения.

Разрозненные строки идентичности продукта (имя, версия, пути) были разбросаны по нескольким файлам
вместо единого источника.

Итог: верификация подтвердила, что `SpecDesk.AppInfo`'s `ProductInfo`/`AppPaths` — уже единый
источник идентичности; все call-site файлы (`Program.cs`, `Logging.cs`, `LibGit2DocumentVersioning.cs`,
`GitHubHttp.cs`, `TokenStore.cs`, `GitHubDeviceFlowAuth.cs`) используют их, не хардкодя заново;
пиннинг-тест `AppPathsTests.Auth_IsByteIdenticalToTheOriginalHandRolledPath` подтверждает байт-в-байт
совместимость с исходным путём. Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-055] Удалить мёртвый шаблонный/релизный балласт (T-08)

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low — удаление неиспользуемого шаблонного/релизного балласта, без влияния на поведение.

`.gitignore`/`cliff.toml`/`Directory.Build.props` предположительно несли мёртвые ссылки на
`release.yml` и шаблонный `__ProjectName__`-балласт.

Итог: верификация подтвердила, что балласт уже вычищен более ранним коммитом
(`a3e919b34203`, "Remove dead release.yml references and unused project scaffolding"),
включая соответствующую запись `### Removed` в CHANGELOG.md. Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-056] Починить битые ссылки на AGENTS.md/CLAUDE.md из отслеживаемых документов (T-09)

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low — правка markdown-ссылок в документации; не затрагивает код/поведение.

Около 10 отслеживаемых документов ссылались на `../AGENTS.md`, притом что `AGENTS.md`/`CLAUDE.md`
предположительно были в `.gitignore`, из-за чего каждая такая ссылка отдавала бы 404 на GitHub.

Итог: верификация подтвердила, что `AGENTS.md`/`CLAUDE.md` уже отслеживаются git (не в
`.gitignore`), все перечисленные ссылки резолвятся к реально существующему файлу, а CHANGELOG.md
уже документирует это изменение. Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-062] Починить macOS-специфичное падение MainWorktreeGuardTests в CI

Оценка сложности: низкая
Оценка ответственности: средняя
Риск: low — верификация уже существующего symlink-aware фикса гейта актуальности рабочей копии.

CI-джоба `dotnet (macos-latest)` предположительно стабильно падала на `MainWorktreeGuardTests`
из-за различия резолва путей/git-ссылок на macOS-раннере.

Итог: верификация подтвердила, что symlink-aware фикс (`CanonicalPath`/`ResolveLinks`/
`StripExtendedLengthPrefix` в `MainWorktreeGuard.cs`, `SamePath` канонизирующий оба операнда) уже
на месте и работает: в последнем прогоне CI (run 29541738641) `MainWorktreeGuardTests` не
фигурируют среди упавших ни на одной из трёх ОС. Джоба `dotnet (macos-latest)` в целом остаётся
красной из-за несвязанных пре-существующих падений (`HostControllerFilesystemTests` и смежные,
одинаково красные на всех трёх ОС) — вне scope этой находки. Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-065] Учитывать экранированные кавычки при разборе TOML-массивов строк в Toml.getList

Оценка сложности: средняя
Оценка ответственности: низкая
Риск: low — внутренний парсер `.spectool.toml`, узкий радиус — только списковые ключи TOML-конфига.

`src/SpecDesk.Core/Toml.fs` — `getList` извлекал элементы массива наивным regex `"([^"]*)"`,
не знающим про экранированные кавычки, из-за чего элемент вида `reviewers = ["Say \"hi\""]`
разрезался на неправильные элементы, а экранирование не разворачивалось.

Итог: реальный фикс. Добавлен приватный escape-aware сканер `splitQuotedElements` (те же приёмы
`inQuote`/`escaped`, что уже использует `stripInlineComment`/`containsUnquoted`), каждый найденный
элемент теперь прогоняется через тот же `unescape`, что и `getString`. Наивный regex и импорт
`System.Text.RegularExpressions` удалены. Добавлен тест на элемент с `\"` внутри; существующие
`getList`-тесты не изменили поведения. Запись в CHANGELOG.md.

# Активная задача

Статус: выполнена
Исходная задача: [T-063] Защитить Cancel() переиспользуемых CancellationTokenSource от гонки с Dispose() фонового владельца

Оценка сложности: средняя
Оценка ответственности: высокая
Риск: high (auth/secrets — sign-in/sign-out и удаление персистентного токена)

Фоновые задачи владеют своими `CancellationTokenSource` и диспозят их в `finally`, пока
обработчики на message-потоке отменяют снапшот того же поля вне лока, не защищаясь от
`ObjectDisposedException` — гонка могла сорвать `_auth.SignOut()` и оставить персистентный
токен неудалённым при выходе пользователя.

Итог: реальный фикс. Добавлен общий хелпер `GuardedCancel` (делегирует в уже существующий
`CancelForDispose`) в новом `HostController.Cancellation.cs`; через него проведены все
незащищённые вызовы `Cancel()` в `HostController.SignIn.cs`/`.Workspace.cs`/`.RepositoryBrowse.cs`,
включая 6 дополнительных мест с тем же паттерном гонки сверх исходного списка находки (в тех же
трёх файлах). Добавлен регрессионный тест, детерминированно воспроизводящий гонку через
уже-диспознутый CTS и подтверждающий, что `_auth.SignOut()` всё равно выполняется; подтверждено,
что тест падает без фикса. Запись в CHANGELOG.md.

# Активная задача

Статус: выполнена
Исходная задача: [T-048] Заменить шесть разрозненных полей сессии черновика на один неизменяемый снимок с токеном поколения (T-13)

Оценка сложности: высокая
Оценка ответственности: высокая
Риск: high — консолидация центрального изменяемого состояния сессии черновика, используемого во
множестве IPC-обработчиков внутри `HostController.cs`; широкий радиус внутри одного центрального
файла, высокая цена ошибки для корректности активного документа.

Шесть разрозненных полей сессии черновика (`_state`/`_branch`/`_baseBranch`/`_dirty`/
`_versionsSaved`/`_versionsShared`) в `HostController.cs` рисковали рассинхронизацией
(class дефектов S-05/S-06/M-13).

Итог: верификация подтвердила, что консолидация уже реализована более ранним изменением
(коммит 80734f2, 2026-07-06) — единственный неизменяемый record `DraftSession` (с токеном
поколения `Generation`/`_draftGeneration`) заменяет все шесть полей, атомарно присваиваемый
целиком под `_sync`. Шести отдельных изменяемых полей в файле не осталось. Запись в
CHANGELOG.md уже присутствует. Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-051] Реорганизовать webview/src по подпапкам согласно реальным границам областей (T-04)

Оценка сложности: высокая
Оценка ответственности: средняя
Риск: medium — механическое перемещение затрагивает практически весь каталог `webview/src`; широкий
диапазон diff означает большой радиус при низкой сложности отдельной правки.

`webview/src`/`webview/tests` (плоская структура, 32 файла) станут тяжелее навигировать при
появлении файлов комментариев/сравнения из PoC-6/8.

Итог: верификация подтвердила, что реорганизация уже выполнена: `webview/src` разбит по
подпапкам `wire/editors/review/chrome/sync/util/workspace`, `webview/tests/` зеркалирует
раскладку 1:1, entry-point esbuild не менялся, `wire()` в `index.ts` уже разбит на
`wireEditors`/`wireLifecycle`/`wireGitHub`/`wireViewMode`. Запись в CHANGELOG.md уже
присутствует. Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-057] Выделить общую HTTP-обвязку GitHub в один хелпер (T-10)

Оценка сложности: низкая
Оценка ответственности: высокая
Риск: high — домен включает `DeviceFlowApi.cs` (OAuth device-flow вход) и `GitHubReview.cs`
(GitHub REST/GraphQL транспорт) — категория «безопасность, auth, permissions, секреты, PII»,
активная категория обязательного human review; фактический diff оказался additive test-only.

`DeviceFlowApi.cs` и `GitHubReview.cs` предположительно заново определяли 30-секундный таймаут,
User-Agent, паттерн связанного CTS и почти идентичные JSON-парсеры вместо общего хелпера.

Итог: верификация подтвердила, что консолидация в `GitHubHttp.cs` уже полностью выполнена
(вероятно вместе с T-054) — оба транспорта используют `RequestTimeout`/`UserAgent`/`NewTimeout`/
`StringOf`/`NumberOf`, остаточных дублей не найдено. Добавлено регрессионное покрытие
(`GitHubHttpTests.cs`) — 5 тестов, пинингующих общий таймаут/User-Agent и доказывающих, что оба
транспорта (`DeviceFlowApi`, `GitHubReview`) метят реальный исходящий запрос точным общим
`GitHubHttp.UserAgent`. Запись в CHANGELOG.md добавлена (test-only регрессионное покрытие, без
изменения поведения). Approval-request `apr-26c3cc2792b0f6850da44764a2f0e174` подан на аудит
(human-review категория) — не блокирующий по текущей политике `.work/constraints.md` этого проекта
(сигнал повышенного внимания, не жёсткий гейт), остаётся открытым для оператора.

# Активная задача

Статус: выполнена
Исходная задача: [T-058] Добавить скрипт регенерации контрактных фикстур по всему решению (T-11)

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low — новый dev-tooling скрипт и точечные ссылки в документации; не затрагивает
production-код, контракты или пути публикации.

Регенерация контрактных фикстур фильтрованным прогоном тестов рисковала оставлять устаревшие
фикстуры не по всему решению.

Итог: верификация подтвердила, что скрипт `scripts/update-contract-fixtures.cmd` уже существует
и запускает `UPDATE_CONTRACT_FIXTURE=1 dotnet test SpecDesk.slnx` по всему решению без фильтра;
`AGENTS.md`/`docs/CODE-REVIEW-GUIDE.md` уже ссылаются на него; запись в CHANGELOG.md уже
присутствует (добавлено коммитом 9791b428, 2026-07-06). Локальный прогон скрипта подтвердил
идемпотентность регенерации (0 изменений). Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-064] Разбирать payload pr.list.request через SafeGetPayload (OnListReviews может бросить JsonException и оставить запрос без ответа)

Оценка сложности: низкая
Оценка ответственности: средняя
Риск: low — узкий, локализуемый в одном обработчике одного файла bug-fix по уже принятому в
контроллере паттерну (`SafeGetPayload<T>`).

`OnListReviews` (`HostController.Review.cs`) читал недоверенный payload `pr.list.request`
напрямую через `message.GetPayload<PrListRequestPayload>()` дважды подряд, вместо принятого во
всём контроллере `SafeGetPayload<T>`; малформный payload (например, `scope` числом) бросал
необработанный `JsonException`, оставляя коррелированный запрос без ответа (30-секундный таймаут
на стороне webview-панели вместо мгновенного фолбэка).

Итог: реальный фикс. `OnListReviews` теперь разбирает payload один раз через
`SafeGetPayload<PrListRequestPayload>` и ветвится по полученному `scope`; малформный payload
корректно проваливается в легаси-ветку вместо необработанного исключения. Добавлен тест
`ListReviews_WithAMalformedScopePayload_RepliesOnTheLegacyBranchInsteadOfHanging`. Запись в
CHANGELOG.md.

# Активная задача

Статус: выполнена
Исходная задача: [T-066] Синхронизировать чтения _currentPath/_repoRoot в OnSuggestVersionNote, OnSuggestBranchName и DocRelativeDir

Оценка сложности: средняя
Оценка ответственности: средняя
Риск: low — узкая, поимённо ограниченная (3 места) правка конкурентного чтения по уже
установленному в том же файле паттерну.

`OnSuggestVersionNote`, `OnSuggestBranchName` и `DocRelativeDir` (`HostController.Session.cs`)
читали `_currentPath`/`_repoRoot` голыми полями без лока `_sync`, нарушая задокументированный
инвариант («публикуются и снапшотятся только под `_sync`») — фоновые терминальные публикации
могли отдать рассинхронизированную пару path/root из разных документов.

Итог: реальный фикс. Все три места теперь снапшотят пару `_repoRoot`/`_currentPath` под `_sync`
по уже установленному в файле паттерну (`OnCompare`/`SendWorkspaceContext`). Поведение и
существующие тесты не изменились (правка чисто синхронизационная).

# Активная задача

Статус: выполнена
Исходная задача: [T-067] Перевести GitHubRepositoryCatalog на общие конвенции GitHubHttp (per-request таймаут и User-Agent)

Оценка сложности: средняя
Оценка ответственности: средняя
Риск: medium — HTTP-транспорт GitHub API (не сам auth-flow, но данные организаций/репозиториев/
деревьев/файлов, используемые сразу после входа).

`GitHubRepositoryCatalog.cs` — третий GitHub-транспорт решения, но единственный, обходивший общий
хелпер `GitHubHttp`: хардкодил `UserAgent.ParseAdd("SpecDesk/1.0")` (второй захардкоженный "1.0"
после консолидации ProductInfo) и не ограничивал ни один из семи HTTP-запросов per-request
таймаутом, полагаясь на дефолтный 100-секундный таймаут `HttpClient`.

Итог: реальный фикс. Все семь запросов каталога (включая постраничные `GetOrganizationsAsync`/
`GetRepositoriesAsync`) переведены на `GitHubHttp.NewTimeout` (свежий CTS на каждую страницу);
`CreateRequest` использует общий `GitHubHttp.UserAgent`, хардкод `"SpecDesk/1.0"` удалён.
Публичные сигнатуры каталога не изменены, регрессии в `RefreshAccountOrganizations`/
`TryRequestRemoteLevel` нет. Добавлены два теста (User-Agent; таймаут применяется). Запись в
CHANGELOG.md.

# Активная задача

Статус: выполнена
Исходная задача: [T-069] Исправить XML-док RepositoryStatusPayload, описывающий RegisteredRepo, в Payloads.cs

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low — правка только doc-комментариев в контрактном файле; сигнатуры, поля и поведение типов
не меняются.

XML-doc-комментарий, описывающий `RegisteredRepo` (Id/Name/Url), был ошибочно прикреплён к
`RepositoryStatusPayload` (Ahead/Behind/HasUncommitted/StashCount/HasConflicts) — у которого нет
ни одного из упомянутых в комментарии параметров; `RegisteredRepo`/`RegisteredBranch`/
`RegisteredClone` остались вовсе без документации.

Итог: реальный фикс. `RepositoryStatusPayload` получил собственный корректный XML-комментарий;
комментарий про registered-repository перенесён и переработан на `RegisteredRepo` со всеми его
реальными параметрами; добавлены недостающие XML-доки для `RegisteredBranch`/`RegisteredClone`.
Все `paramref` ссылаются только на существующие параметры. Публичные сигнатуры/поведение не
менялись.

# Активная задача

Статус: выполнена
Исходная задача: [T-082] Написать авторское руководство пользователя (docs/user-guide.md)

Оценка сложности: средняя
Оценка ответственности: низкая
Риск: low — только документация, не затрагивает код/контракты/безопасность.

`docs/` описывал дизайн-замысел, роадмап, ревью и тестирование, но не было ни одного документа
для конечного автора (Start-страница, копии/рабочие линии, черновики/версии, инлайн-комментарии,
документ проверки PR, ассистент, Disk/избранное).

Итог: реальный контент. Добавлен `docs/user-guide.md` (на английском, в словаре жизненного цикла
§8, без git-лексики) — сквозной сценарий от открытия репозитория до одобренной публикации, раздел
«если что-то пошло не так» (экспорт лога). Все конкретные утверждения (метки кнопок, текст
подсказок, семантика переходов ревью) сверены с реальным кодом webview/HostController, а не только
с дизайн-документом. Добавлена ссылка в `docs/README.md`. Запись в CHANGELOG.md.

# Активная задача

Статус: выполнена
Исходная задача: [T-083] Завести property-based тесты FsCheck для F#-домена (Toml, Slug, Lifecycle, AstDiff)

Оценка сложности: высокая
Оценка ответственности: низкая
Риск: low — тестовый код (только `tests/`), не затрагивает поведение продакшн-кода; добавляет
новую dev-зависимость (FsCheck) точечно в два существующих тестовых проекта.

F#-домен (`Toml.fs`, `Slug.fs`, `Lifecycle.fs`, `AstDiff.fs`/`DiffWire.fs`) был покрыт только
example-based NUnit-тестами, пропуская краевые случаи, которые property-based тесты дёшево ловят.

Итог: реальный фикс. FsCheck 3.3.3 подключён через Central Package Management в
`SpecDesk.Core.Tests`/`SpecDesk.Diff.Tests`; добавлены property-тесты на round-trip TOML
(quote/unquote/escape через `escapeBasic`/`unescape`), идемпотентность/инварианты Slug,
тотальность переходов Lifecycle (все пары состояние×команда), инварианты AST-диффа
(`diff(x,x)` пуст, известные `DiffKind`). Найденная в ревью находка (сырой NUL-байт в
fallback-сентинеле, ломавший diff-читаемость .fs-файла как бинарного) исправлена заменой на
текстобезопасный сентинел вне алфавита генератора. CHANGELOG не тронут (test-only, по конвенции
проекта).

# Активная задача

Статус: выполнена
Исходная задача: [T-050] Дать diff.request параметр базы сравнения вместо жёсткого "база = локальный HEAD" (T-03)

Оценка сложности: средняя
Оценка ответственности: средняя
Риск: medium — трогает внутренний native↔webview wire-контракт, формально зафиксированный
кросс-языковыми контрактными фикстурами; радиус ограничен Host-диспетчером сравнения и
оверлеем webview.

`diff.request` не нёс полезную нагрузку, а `OnCompare` всегда читал HEAD; `ReviewController`
жёстко вызывал `requestCompare("lastVersion")` без механизма выбора базы, что размножило бы
виды сообщений с приходом PoC-6/PoC-7 (сравнение с PR/другой базой).

Итог: реальный фикс. Аудит подтвердил, что нативная сторона (`DiffRequestPayload(Base, Pr)`/
`DiffBaseKinds` в `Payloads.cs`, `OnCompare` в `HostController.Session.cs`) уже несёт и
валидирует `base` — работа нужна была только со стороны webview. `ReviewController.toggle(base)`
теперь протягивает вызывающий `base` через `requestCompareOnceSettled` до `requestCompare(base)`
вместо хардкода `"lastVersion"`; дефолт сохраняет текущее поведение «Показать изменения» без
изменений. Форма сообщения по проводу не менялась — регенерация контрактной фикстуры не
потребовалась. Запись в CHANGELOG.md.

# Активная задача

Статус: выполнена
Исходная задача: [T-060] Свести стиль получения DOM-элементов в chrome-слое к единому паттерну внедрения элементов (T-14)

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low — механический рефакторинг стиля получения DOM-элементов без изменения поведения.

`Dialogs`/`SignInController`/`ReviewsPanel` предположительно самостоятельно запрашивали `document`
вместо получения элементов через внедрение зависимостей (паттерн `lifecycle-chrome.ts` и др.).

Итог: верификация подтвердила, что рефакторинг уже выполнен более ранним коммитом (`b400776`,
«Convert Dialogs, SignInController, and ReviewsPanel to element injection») — все три класса уже
получают элементы через `*Deps`-интерфейс конструктора; оставшиеся обращения к `document` в
`signin.ts`/`reviews-panel.ts` легитимны (roving-tabindex навигация, `document.createElement` при
построении строк списка), не самостоятельный запрос собственных элементов. Тесты трёх модулей уже
используют паттерн внедрения. Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-061] Исправить рассинхрон выравнивания в начале документа: lead-спейсер дублирует padding formatted-панели (~24px)

Оценка сложности: средняя
Оценка ответственности: средняя
Риск: low — точечная коррекция вычисления в height-sync, контур ограничен геометрией рендера
Split-режима; покрыт существующим E2E-гейтом реальной геометрии (Layer 1).

Lead-спейсер в начале документа предположительно дублировал padding-top formatted-панели
(~24px), рассинхронизируя выравнивание первого блока между панелями Split-режима.

Итог: верификация подтвердила, что первопричина уже устранена более ранним коммитом (`9821e35`,
«Fix top-of-document ~24px lead misalignment in Split view»): `computeGapAdjustments` вычитает
`referenceInset` из каждого `previewTop`, так что inset панели больше не учитывается дважды.
E2E-пин обновлён (полоса 20-28px удалена, `h1` — обычный член `SCROLLABLE`), регрессионный
юнит-тест `describe("first-block lead (T-061)")` активен, запись в CHANGELOG.md присутствует.
Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-072] Синхронизировать инлайн-комментарии документа с PR review comments GitHub (PoC-8)

Оценка сложности: высокая
Оценка ответственности: высокая
Риск: high — двусторонняя запись/чтение реального контента в открытый GitHub PR (публикация review
comment от имени автора, подтягивание чужих комментариев, переякорение при смене head-коммита);
использует существующие auth-токены для мутирующих вызовов GitHub API.

Инлайн-комментарии к выделению уже жили локально, а разговор PR отображался в документе проверки,
но не хватало моста с GitHub: публикации локального комментария как PR review comment, подтяжки
комментариев с GitHub к инлайн-якорям, обработки вне-hunk случая и переякорения при смене
head-коммита.

Итог: реальный, крупный фикс (20 файлов, ~1930 строк — крупнейшая задача сессии). Добавлены
`CreateReviewCommentAsync`/`GetReviewSyncAsync` в `GitHubReview.cs` (через общий `GitHubHttp`,
K-006) и новый `HostController.ReviewComments.cs` с IPC-обработчиками `OnReviewCommentSync`/
`OnReviewCommentPublish` (парсинг через `SafeGetPayload<T>`, K-004; owner/repo/path резолвятся из
собственного состояния хоста, не из webview-payload). Webview: локальная модель остаётся
источником истины, GitHub-синхронизация — непостоянная проекция (`githubSync`: synced/publishable/
local-only); вне-hunk треды получают метку «ещё не на GitHub» вместо ошибки; переякорение — через
существующий offset-mapper без потери комментариев, де-дупликация через `ownedGithubIds`. Токены
не логируются и не персистятся (персистится только числовой `githubId`). Тесты на всех уровнях
(hunk-парсер, публикация, синхронизация, переякорение) + CHANGELOG-запись. Approval-request
`apr-f4382d27fa390343f5adf887c1712505` подан на аудит (auth/security human-review категория) —
не блокировал публикацию по документированной политике `.work/constraints.md` этого проекта,
остаётся открытым для оператора.

# Активная задача

Статус: выполнена
Исходная задача: [T-059] Унифицировать конфигурацию токенайзера markdown-it и кэшировать результат парсинга при block-splice (T-12)

Оценка сложности: средняя
Оценка ответственности: средняя
Риск: medium — затрагивает общий путь round-trip block-splice (парсер + сериализация), которым
пользуется каждый открытый документ; ограничено webview-модулем рендеринга markdown.

Две конфигурации токенайзера markdown-it (`md-blocks.ts` — пресет по умолчанию, maxNesting 100;
`pm-markdown.ts` — commonmark+таблица+зачёркивание, maxNesting 20) расходились на вложенности
глубже 20, а совпадение границ верхнего уровня block-splice ничем не фиксировалось; также
`serializeWithSplice` дважды парсил `original` за вызов `getText()`, а debounce-тик в `index.ts`
пере-парсил формат на каждом тике только ради проверки равенства.

Итог: верификация подтвердила, что всё уже реализовано более ранним коммитом (`3774dba`, «Unify
markdown-it tokenizer config and cache block-splice parse results»): общий `md-config.ts::
createTokenizer` используется в обеих конфигурациях; тест на согласование границ при вложенности
25 присутствует; `serializeWithSplice` кэширует пару `(original → {doc, blocks})`;
`formatted.ts` кэширует `getText()` на паре `(original, doc)`. Запись в CHANGELOG.md уже
присутствует. Правок не потребовалось.

# Активная задача

Статус: выполнена
Исходная задача: [T-068] Унифицировать сравнение идентичности путей в Host (Ordinal vs OrdinalIgnoreCase vs канонический handle-путь)

Оценка сложности: высокая
Оценка ответственности: высокая
Риск: high — единая политика идентичности пути затрагивает одновременно claim-проверки сессии
документа, актуальность workspace-дерева и деструктивный путь удаления файлов; класс проблем,
близкий к ранее исправленному S-03 (потеря/порча работы через неверное определение "того же
документа").

В Host сосуществовали три расходящиеся трактовки «это один и тот же путь»: `SameFullPath`
(`Path.GetFullPath`+`OrdinalIgnoreCase`), claim/актуальность-проверки мутаций
(`StringComparison.Ordinal`) и канонические handle-пути удаления (кейс-чувствительный хвост +
кейс-нечувствительный volume authority). Путь, отличающийся только регистром, мог быть «тем же
документом» для одной проверки и «другим» для другой — источник трудноотлаживаемых отказов вида
«The open document changed» или ложных совпадений активного документа. Отдельно,
`IsWorkspaceTreeRequestCurrentLocked` расходился с соседними Ordinal-проверками того же поля без
документированной причины.

Итог: реальный фикс. Добавлен `PathIdentity.cs` с двумя явно задокументированными хелперами:
`SameFilesystemPath` (OrdinalIgnoreCase, «то же место на диске») и `SameSessionPath` (Ordinal,
fail-closed, «тот же документ сессии»). Все 20+ claim/актуальность-сравнений в
`HostController.Session.cs` переведены на `SameSessionPath` без ослабления (остались Ordinal).
`IsWorkspaceTreeRequestCurrentLocked` переведён с `OrdinalIgnoreCase` на `SameSessionPath` —
намеренное ужесточение (fail-closed: рассинхронизация теперь отбрасывает стейл-дерево, а не
отдаёт его). Канонические handle-сравнения в `WindowsHandleFileDeletion.cs` поведенчески не
изменены (только задокументированы перекрёстной ссылкой на `PathIdentity`, третья осознанная
политика — «идентичность записи каталога»). Добавлен регресс-тест на путь, отличающийся только
регистром, покрывающий цепочку открытие→claim мутации→удаление. Полный набор тестов Host
проходит без регрессий (667 passed). Запись в CHANGELOG.md.

# Активная задача

Статус: выполнена
Исходная задача: [T-070] Ввести реестр IPC-обработчиков вместо центрального switch в HostController.OnMessage

Оценка сложности: высокая
Оценка ответственности: высокая
Риск: high — широкий радиус (правит `HostController.cs` и все ~11 partial-слайсов,
маршрутизирующих ~60 видов сообщений); задевает категорию «изменение архитектурных границ»
(внутренняя граница диспетчеризации, протокол/контракт сообщений не меняется).

`HostController.OnMessage` маршрутизировал уже ~60 видов сообщений одним центральным switch;
каждый следующий срез функциональности по роадмапу добавлял новые case в этот же файл, хотя
сами обработчики живут в partial-слайсах.

Итог: реальный, крупный фикс. Центральный switch заменён на реестр «kind → обработчик»
(`IpcHandlerRegistry.cs`, `Dictionary<string, Action<IpcMessage>>`, `StringComparer.Ordinal`,
дубликат вида бросает исключение); каждый из ~11 partial-слайсов регистрирует свои виды в
своём же файле. Все 59 бывших case сверены 1:1 с новыми регистрациями — ни один вид не
потерян/задвоен/переставлен. Особые случаи вне switch (сброс oversized/malformed-кадра, порядок
Monitor Enter/Exit mutation-guard, исключение `log` из логирования, fallback неизвестного
kind) не тронуты. Конвенция K-004 (`SafeGetPayload<T>`) не нарушена. Добавлен новый вид
сообщений впредь локализуется в файле домена. Тест механизма реестра добавлен. Запись в
CHANGELOG.md. Approval-request `apr-07231f759efb2d7798db693d204ec1a8` подан на аудит
(архитектурная граница) — не блокировал публикацию.

# Активная задача

Статус: выполнена
Исходная задача: [T-081] Включить проверку орфографии в редакторах и полях комментариев

Оценка сложности: низкая
Оценка ответственности: низкая
Риск: low — узкая локальная правка атрибутов ввода в существующих UI-компонентах.

Ни одна поверхность ввода прозы не проверяла орфографию: CodeMirror и ProseMirror не выставляли
атрибуты `spellcheck`/`lang`, хотя WebView2/Chromium несёт встроенный проверщик.

Итог: реальный фикс. `formatted.ts` (ProseMirror) и `editor.ts` (CodeMirror
`contentAttributes`) теперь выставляют `spellcheck`/`lang`; поля комментариев (`comment-thread.ts`)
и диалоги с текстовым вводом (`dialogs.ts` — version-note, PR title/body) тоже. Временная
константа-тумблер `spellcheck.ts` с явным `TODO(T-077)` (перенос в хранилище предпочтений,
когда оно появится). Юнит-тесты во всех 4 требуемых файлах. Layer 2-верификация отложена
(среда без Windows-раннера) — задокументировано как допустимый fallback. Запись в CHANGELOG.md.

# Активная задача

Статус: выполнена
Исходная задача: [T-084] Завести property-based round-trip тесты block-splice и word-diff на fast-check

Оценка сложности: средняя
Оценка ответственности: средняя
Риск: low — тестовая инфраструктура, продакшн-код не меняется; добавляется одна dev-зависимость.

Принятая планка PoC-12: no-op round-trip block-splice байт-в-байт идентичен исходнику, а дифф
одиночной правки локален своему блоку — сегодня проверено только example-based тестами.

Итог: реальный фикс. `fast-check` подключён как dev-зависимость; генераторы Markdown-документов
(заголовки/списки/таблицы/код/цитаты/hard breaks) в `webview/tests/editors/property/`; три
свойства — no-op round-trip байт-в-байт, локальность правки одного блока, обратимость
word-diff. Тесты воспроизводимы (зафиксированный seed). Запись в CHANGELOG.md.

# Активная задача

Статус: выполнена
Исходная задача: [T-085] Завести перфоманс-набор для больших документов (BenchmarkDotNet + Layer 1 бюджеты)

Оценка сложности: высокая
Оценка ответственности: средняя
Риск: low — аддитивная инструментальная работа, продакшн-пути не затрагивает; опциональная/nightly
стадия, не блокирует обычный конвейер.

В проекте не было ни одного перфоманс-теста, при этом известные горячие точки (reparse в
block-splice, LCS в word-diff, AST-дифф) растут с размером документа.

Итог: реальный фикс. Новый `bench/` (BenchmarkDotNet), зарегистрирован в `SpecDesk.slnx`;
бенчмарки reparse (`SpecDesk.Markdown`) и AST-диффа на сгенерированных документах (5–10 тыс.
строк); новый Layer 1 e2e-сценарий с большим документом и бюджетами реконсиляции/скролл-синка,
без квадратичного роста; обе части — опциональная nightly-стадия, не замедляющая обычный CI.
Пороги задокументированы с обоснованием. Запись в CHANGELOG.md.

# Активная задача

Статус: выполнена
Исходная задача: [T-087] Запускать Layer 2 full-app e2e по расписанию на windows-раннере CI

Оценка сложности: средняя
Оценка ответственности: средняя
Риск: low — только CI-инфраструктура, продакшн-код не меняется.

Пятая ступень лестницы верификации (full-app e2e поверх реального `SpecDesk.Host.exe`) сегодня
Windows-only и запускается только вручную; регрессии нативных эффектов CI не ловит.

Итог: реальный фикс. Новый `.github/workflows/layer2-nightly.yml` на `windows-latest`: nightly
по расписанию + `workflow_dispatch`; сборка host, `npm run e2e:app`, выгрузка артефактов при
любом исходе. Эмпирическая проверка Этапа 1 (создаёт ли Photino/WebView2 окно на hosted-раннере)
недоступна исполнителю (нет push-доступа для реального запуска workflow) — честно
задокументировано в `docs/testing.md` вместе с self-hosted-раннер fallback. CHANGELOG
намеренно не тронут — чисто внутренний CI-артефакт, не пользователь-видимое изменение.

# Активная задача

Статус: выполнена
Исходная задача: [T-088] Добавить проверку форматирования Fantomas в CI (с манифестом dotnet-tools)

Оценка сложности: средняя
Оценка ответственности: низкая
Риск: medium — широкий радиус (переформатирует почти все `.fs`-файлы), но чисто механическое
изменение.

AGENTS.md объявляет Fantomas каноничным форматтером F#, но манифеста dotnet-tools не было, а
CI формат F# не проверял — дрейф форматирования копился незаметно.

Итог: реальный фикс. `.config/dotnet-tools.json` закрепляет fantomas 7.0.5; новая джоба
`fantomas` в `ci.yml` (`dotnet tool restore` + `dotnet fantomas --check src tests`); все
существующие `.fs`-файлы в `src/`/`tests/` переформатированы (24 файла) — подтверждено ревью
как чисто механическое (идиомы Fantomas, никакой логики не менялось). `dotnet build`/`dotnet
test` проходят после форматирования. Запись в CHANGELOG.md.

# Активная задача

Статус: выполнена
Исходная задача: [T-089] Автоматизировать выпуск релиза: тег → сборка exe и GitHub Release с release notes

Оценка сложности: высокая
Оценка ответственности: высокая
Риск: high — production-релизная инфраструктура (новый CI/CD workflow, публикующий артефакт и
создающий публичный GitHub Release); категория «производственная инфраструктура» обязательного
human review.

Выпуск был локальным (`release.cmd`, без GitHub Releases); версия оставалась 0.1.0 без
воспроизводимой автоматизации срезов.

Итог: реальный фикс. Новый `.github/workflows/release.yml`, триггер по тегу `v*`: полная
сборка+тесты, `dotnet publish` профилем `win-x64`, release notes из соответствующего раздела
CHANGELOG.md (fallback на `git-cliff`), публикация GitHub Release с приложенным exe.
Safeguards: tag-injection safety (чтение через `$env:GITHUB_REF_NAME`, не интерполяция в
скрипт), version-integrity gate (`Directory.Build.props` версия должна совпадать с тегом),
artifact-size guard и empty-notes guard (оба fail closed), `gh release create` — строго
последний шаг (частичный/сломанный релиз невозможен). Все actions запинены на полный SHA.
Документация процедуры среза версии в `docs/release-process.md`/`CONTRIBUTING.md`. Запись в
CHANGELOG.md. Approval-request `apr-3a180bdef003f47bcf53be04c4ed2635` подан на аудит
(production-инфраструктура) — не блокировал публикацию.

# Активная задача

Статус: выполнена
Исходная задача: [T-073] Добавить действие «Опубликовать»: merge одобренного PR с гейтом allow-author-publish

Оценка сложности: средняя
Оценка ответственности: высокая
Риск: high — сливает одобренный PR в основную линию и удаляет ветку черновика (необратимое
git-действие в чужой истории), плюс расширяет публичный native↔webview IPC-контракт новым
видом сообщения (`action.publish`) — категория «изменение архитектурных границ и публичных
контрактов/API» из `.work/constraints.md`.

Машина жизненного цикла уже определяла переход Approved → Publish → Published
(`src/SpecDesk.Core/Lifecycle.fs`), но в UI действия «Publish» не существовало, а протокольная
строка `action.publish` была помечена в docs/design/09-ipc-protocol.md как «not yet built».

Итог: реальный фикс. Новая webview-команда `doc.publish` (protocol.ts/decoders.ts +
SpecDesk.Contracts IpcMessage/Payloads); host-обработчик `OnPublish` в
`HostController.Review.cs` (зарегистрирован в `RegisterReviewHandlers`, K-012) сливает
одобренный PR через `SpecDesk.GitHub`/`GitHubHttp` (K-006) с переякорением к approved head
SHA (отказ при устаревшем одобрении), удаляет ветку черновика best-effort
(`DeleteMergedBranchBestEffort`, не откатывает merge при сбое), переводит lifecycle → Published.
Действие в lifecycle-chrome гейтится конфигом `allow-author-publish`
(`WorkflowConfig.allowAuthorPublishForHost`, авторитетно перечитан host-side, не доверяя
webview) и переучитывается только простоязычной формулировкой. Ошибка merge не двигает
lifecycle, автору — понятная ошибка. Тесты (host + webview) покрывают гейт конфига,
успешный merge, ошибку merge. CHANGELOG.md обновлён. Approval-request
`apr-80ccd6b305cf5f7b64db73433f19d55a` подан на аудит (архитектурная граница/публичный
контракт) — не блокировал публикацию.

# Активная задача

Статус: выполнена
Исходная задача: [T-076] Автоматическая фоновая синхронизация зарегистрированных локальных копий

Оценка сложности: высокая
Оценка ответственности: высокая
Риск: medium — автоматически мутирует локальное git-состояние без явного действия
пользователя (fetch + fast-forward), но строго ограничено чистыми основными линиями,
без выхода на публичные контракты/публикацию.

Ручные «Get updates» / «Share changes» были удалены «in preparation for automatic
synchronization», остался только ручной Refresh на панели репозиториев; роадмап PoC-4
планировал background auto-fetch.

Итог: реальный фикс. `SpecDesk.Git` `FetchAndFastForwardCleanLine`/
`TryFastForwardCleanDefaultLine` — консервативный safe fast-forward: пропускает detached
HEAD, не-дефолтную линию, конфликты индекса, грязное дерево, отсутствие upstream-трекинга и
любую дивергенцию; всегда фетчит для обновления индикаторов. Host-цикл в
`HostController.Workspace.cs` (`OnAutoSyncRepositories`) — троттлинг 45с + focus-gated
webview-poll (180с, `document.hasFocus()`), single-flight, отменяемость по sign-out/teardown
рабочей области. Ручной Refresh не сломан (делегирует в общий `SyncRepositories`). Тесты
(host: троттлинг/focus-gating/отказ от merge при грязном дереве/отмена; webview:
обновление индикаторов) покрывают критерии. CHANGELOG.md обновлён.

Примечание: в процессе интеграции этой задачи в батч был обнаружен и исправлен
инфраструктурный дефект коммита (bookmark ветки задачи по ошибке указывал на пустой коммит
из-за VCS cwd-carryover при коммите оркестратором) — исправлено оркестратором напрямую
(коррекция топологии bookmark'а), без изменения кода задачи; после коррекции реализация
прошла полное независимое ревью без находок.

# Активная задача

Статус: выполнена
Исходная задача: [T-079] Дать ассистенту read-only контекст документа и AI-подсказки заметок версий и текстов PR

Оценка сложности: высокая
Оценка ответственности: высокая
Риск: medium — новые read-only AI-инструменты (getCurrentDoc/getDiff) в hardened SDK-режиме
с явным allowlist, немутирующие; содержимое документа и вывод инструментов трактуются как
данные, а не инструкции. Обязательный откат на детерминированные шаблоны при недоступности/
падении провайдера ограничивает блок-радиус.

Ассистент получал контекст только через явные вложения; заметки версий и тексты PR
генерировались детерминированными шаблонами (`WorkflowSeeds.cs`).

Итог: реальный фикс. Новые read-only hardened-SDK инструменты `getCurrentDoc`/`getDiff` в
`src/SpecDesk.Ai` (explicit allowlist, немутирующие — только геттеры/immutable-снимок).
`suggestVersionNote`/`suggestPrDescription` заменяют шаблоны в диалогах «Save a version»/
«Send for review» как редактируемое предложение (не автоприменение), с гарантированным
детерминированным откатом на шаблон при недоступности/ошибке/таймауте провайдера
(`SuggestionTimeout=12s`). `Program.cs` намеренно передаёт `null` агент — ships template-only
by construction, seam полностью подключён и покрыт `FakeSuggestionAgent`. Контент документа
и вывод инструментов встраиваются как данные с явной prompt-injection-границей. K-004/K-005
соблюдены (чтения `_currentPath`/`_repoRoot`/`_text` под `_sync`). Тесты (host + webview:
happy-path, откат, немутирующая природа) покрывают критерии. CHANGELOG.md обновлён.

# Активная задача

Статус: выполнена
Исходная задача: [T-086] Добавить автоматизированный аудит доступности axe-core в Layer 1 e2e

Оценка сложности: средняя
Оценка ответственности: низкая
Риск: low — test-only добавление, не затрагивает production-код/контракты; единственный
эффект — новый исполняемый CI-гейт.

Дизайн-концепт требует WCAG AA и полную доступность, но автоматической проверки не было —
только точечные ручные e2e-проверки.

Итог: реальный фикс. Новый Layer 1 e2e-сценарий `accessibility-audit.e2e.ts` (`@axe-core/
playwright` как dev-зависимость `e2e/`) сканирует редактор (raw/rendered/split), панели
рабочей области, диалоги, документ проверки PR, вход GitHub — в светлой и тёмной теме;
падает на нарушениях serious/critical. Сознательно допущенные пред-существующие исключения
(dock-rail role=radio+aria-expanded, CodeMirror `.cm-content` без accessible name, несколько
color-contrast случаев) зафиксированы явным списком `KNOWN_EXCEPTIONS` с обоснованием —
проверено позитивным контрольным тестом, что список действительно узкий (гейт падает на
новых несанкционированных нарушениях). `npm run typecheck` проходит, сценарий запускается
через `npm run e2e --project=webview-mock`. CHANGELOG.md обновлён.

# Активная задача

Статус: выполнена
Исходная задача: [T-074] Построить простоязычное примирение конфликтов без git-маркеров (PoC-10)

Оценка сложности: высокая
Оценка ответственности: высокая
Риск: high — правит rebase/конфликт-логику поверх реального рабочего дерева и черновика
автора; ошибочное разрешение конфликта может молча потерять несохранённые изменения.
Дизайн явно называет этот срез "the genuinely dangerous part" продукта.

Конкурирующее изменение того же файла на удалённой стороне при "Отправить на ревью"/
"Обновить ревью" заканчивалось сырой ошибкой git; конфликтный путь (PoC-10) не был построен.

Итог: реальный фикс. `SpecDesk.Git` — `DetectShareConflict` (in-memory
`ObjectDatabase.MergeCommits`, не трогает рабочее дерево/индекс) обнаруживает конфликт и
извлекает обе чистые стороны без git-маркеров; `ReconcileShareConflict`
(`MergeFileFavor.Ours/Theirs` → merge-commit → hard reset) применяет одну сторону локально.
Новый плейн-language диалог "Someone else changed this too" (Keep mine / Keep theirs /
Combine / Ask for help) в webview, переиспользующий существующий дифф-движок для Combine —
никакой git-терминологии/маркеров нигде. Двойная защита от потери данных: in-memory dirty-guard
+ git-слой `DirtyWorkingTreeException` до любой мутации. Новые IPC-виды
`review.conflict`/`review.conflict.resolve` зарегистрированы в своём домене (K-012), пара
фикстур регенерирована (K-008). Тесты покрывают обнаружение на send/update, все 4 пути
разрешения, отсутствие регрессии на бесконфликтном пути. CHANGELOG.md обновлён.
Approval-request `apr-9a2dff92bf71d1a81dcbc35921d8ea14` подан на аудит (публичный
wire-контракт + данные-теряющий радиус) — не блокировал публикацию.

# Активная задача

Статус: выполнена
Исходная задача: [T-077] Сохранять пользовательские предпочтения (тема, перенос, режим, окно) между запусками

Оценка сложности: средняя
Оценка ответственности: средняя
Риск: low — локальная, не разрушительная персистенция UX-предпочтений в новом host-стороннем
JSON-sidecar; безопасный откат к дефолтам при ошибке чтения/записи.

Тема каждый раз бралась из OS-схемы, перенос строк сбрасывался, окно всегда стартовало
1280×800 по центру — предпочтения не переживали перезапуск приложения.

Итог: реальный фикс. Новый host-сторонний `PreferencesStore` (JSON-sidecar в `AppPaths`,
атомарная запись, по образцу `WorkspaceStore.cs`) хранит тему/перенос/режим/геометрию окна;
`WindowGeometryValidator` проверяет сохранённую геометрию против текущей конфигурации
мониторов перед восстановлением. Новый IPC для чтения/записи через `IpcHandlerRegistry`
(K-012), пара фикстур обновлена (K-008). Ревью нашло реальный regression + скрытый краш:
`SetMaximized(true)` до создания нативного окна синхронно триггерил `RegisterMaximizedHandler`
до инициализации, а `SendWebMessage` там бросал необработанный `ApplicationException` —
исправлено (`try/catch` + однократный replay состояния через первый `"ready"`), плюс добавлен
новый Layer-2 e2e-тест (`restore-maximized-window.e2e.ts`) на реальном хосте через CDP.
CHANGELOG.md обновлён.

# Активная задача

Статус: выполнена
Исходная задача: [T-078] Добавить поиск по всем спецификациям рабочей области

Оценка сложности: средняя
Оценка ответственности: средняя
Риск: low — новый host-сторонний срез только на чтение, ограниченный по объёму/времени; не
мутирует документы, не выходит на security/данные/инфраструктуру.

Поиск в титульной строке находил только первое совпадение в открытом документе — не было
поиска по всей рабочей области.

Итог: реальный фикс. Новый host-сторонний `WorkspaceSearch` — обход Markdown-файлов
авторизованного корня (тот же периметр, что и Folder-панель), с лимитами по числу файлов/
времени/размеру/результатам, сниппеты (файл, строка, фрагмент). Новая панель результатов в
левом рейле (`search-panel.ts`), клик открывает документ и прокручивает к совпадению. Новый
вид IPC `search.request`/`search.results` зарегистрирован в своём домене (K-012), пара
фикстур обновлена (K-008). Ревью нашло пробел в документации (docs/design/09-ipc-protocol.md
не упоминал новые виды) — исправлено. Существующий toolbar-поиск не сломан. Тесты покрывают
многофайловый поиск, все три оси обрезки, периметр авторизованного корня. CHANGELOG.md
обновлён.

# Активная задача

Статус: выполнена
Исходная задача: [T-080] Реализовать гейтованное предложение правки от ассистента (proposeEdit + подтверждение)

Оценка сложности: высокая
Оценка ответственности: высокая
Риск: high — новая мутирующая способность ассистента в доверительной модели продукта; ошибка
в гейтинге означает, что агент может изменить документ автора в обход подтверждения.

Дизайн требует, чтобы любое мутирующее действие агента проходило через подтверждение, как
ручное — инструмента proposeEdit и confirm.request не существовало.

Итог: реальный фикс. Новый инструмент `ProposeEditTool` (`src/SpecDesk.Ai`) структурно
немутирующий — владеет только `IEditProposalSink`, может лишь поставить предложение. Host
(`OnConfirmResult`) применяет только подтверждённую (возможно отредактированную) правку через
тот же путь, что и ручное редактирование (`_text`/generation-инкременты под `_sync`) — не
отдельный мутирующий путь. Проверка актуальности (`IsEditProposalCurrentLocked`) зеркалит
`TryClaimDocumentMutation`: конкурентная правка во время ожидания подтверждения отклоняется.
Новая поверхность подтверждения в webview переиспользует существующий word-diff. Новые IPC-виды
`confirm.request/result/applied` зарегистрированы в своём домене (K-012), пара фикстур
обновлена (K-008). Тесты покрывают предложение→подтверждение→применение, отклонение,
редактирование перед подтверждением, конкурентную правку. CHANGELOG.md обновлён.
Approval-request `apr-946471071a693a0a067dfb088876bcc3` подан на аудит (публичный
wire-контракт + граница доверия ассистента) — не блокировал публикацию.
