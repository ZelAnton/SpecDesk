# Активная задача

Статус: в работе
Исходная задача: [T-021] Проверить M-08: инвариант LineMap↔data-line для списков определений

Оценка сложности: низкая
Рекомендуемый effort: medium

`src/SpecDesk.Markdown/Renderer.fs:115-136` — неясно, сохраняется ли инвариант количества
LineMap↔data-line для эмиссии атрибутов `<dt>`/неявного `<dd>`-параграфа Markdig; это единственное
включённое семейство блоков без регрессионного теста.

Критерии готовности:
- Добавлен регрессионный тест, прогоняющий списки определений и проверяющий инвариант количества
  LineMap↔data-line (по аналогии с покрытием других включённых семейств блоков).
- Если тест выявляет реальное расхождение — `Renderer.fs` исправлен так, чтобы инвариант выполнялся.
- Запись в `CHANGELOG.md` — только если потребовалось исправление.
